# Decision records — queuepilot (was plex-channels)

One decision per file, `YYYY-MM-DD-<kebab-slug>.md` (date = when decided). Newest first.

| Date | Decision |
| --- | --- |
| 2026-08-14 | [A weight is SLOTS PER ROUND, not odds](2026-08-14-weights-are-slots-per-round-not-odds.md) *(feature / playback semantics — any entry a randomized set draws from can carry `weight: 1-20`, meaning how many slots it takes per round; 1 is never stored. Three carriers, each written like its neighbour: a queue entry's own key, a member mapping's key, and a channel's `weights: {ratingKey: n}` map (the mirror of `starts`). Slots not odds because a rotation exists so consecutive items are DIFFERENT shows — a 3/1/1 die deals the same show three times running, which is what the round-robin was built to prevent. Smooth weighted round-robin (nginx's): 3/1/1 deals A B A C A. With every weight 1 the interleave IS the old round-robin, asserted against the old loop kept as an oracle, so an unweighted channel cannot change. Two exceptions where a weight is odds: a curated CHANNEL (plays each member once, so a weighted shuffle) and the rewatch card (one film, so it multiplies the 1/n² bias). **Also fixes:** `nextQueue` only shuffles when handed an rng and the provider never handed it one — every curated channel had played in file order since the Python retirement)* |
| 2026-08-14 | [Entry settings are TAGS plus one panel, and the queue view has a density](2026-08-14-entry-settings-are-tags-plus-a-panel.md) *(frontend — supersedes the always-on per-tile `episodes` and `batch_stops_at` dropdowns. A default entry now shows nothing; every tag (`3 eps`, `3x as often`, `Ends at season`, a start point) is a deviation, and one panel edits all four. A View control switches Posters / Cards / Rows over the SAME `li.tile` DOM (CSS-only reshaping, so every e2e selector holds). Filtering a queue is split from searching Plex: the add box filters by type/library and calls out hits already in the queue (picking one jumps to it), while a new toolbar row filters what you already have — including "Completed / fully watched" for pruning passes — always showing "showing N of M" + Clear filters. Counts read `1 / 2 / Custom…` with a real number field, clamped to the engine's own cap. Density + filters persist per queue. Bulk episodes/weight/batch-stop/reset in ONE request, from a bar flush to the bottom edge. Mock-then-pick: the owner reviewed three served layouts and chose C's cards with B's tags)* |
| 2026-08-13 | [A queue draws from exactly ONE provider — mixing is not a goal](2026-08-13-a-queue-draws-from-exactly-one-provider.md) *(architecture / scope — **closes** the open "what does a mixed queue hand off" question rather than answering it: the owner scoped it away while live-testing (*"I only really care about Plex and Kavita for now, and it's either-or for me"*). `materialize`/`handoff` stop being ambiguous, and `resolveSingle()`'s throw + the launcher's 501 become the RIGHT behaviour instead of a provisional refuse-to-guess. Storage deliberately stays a LIST of blocks — two Plex profiles on one queue is not mixing and still needs N blocks, so `isMixed()` is what's forbidden, not `blocks.length > 1`. Jellyfin/Emby/Kodi are what the seam permits, not what anyone is asking for)* |
| 2026-08-13 | [`SelectListbox` adopts the shared Charcuterie `Picker`](2026-08-13-selectlistbox-adopts-the-shared-charcuterie-picker.md) *(UI / dependency — the hand-assembled trigger (`useVisibility` + `Button` + `Listbox` + chevron) becomes a thin adapter over `Picker`, new in `@charcuterie/ui@2.15.0`; requires `logic@^1.5.0` too, since the `ui` barrel now pulls `QueryBuilder`. Extends — does not reverse — the 2026-08-07 Listbox decision: only who assembles the trigger changes. Name and props kept, so all NINE call sites are untouched. Fixes a WCAG 2.5.3 failure ours had drifted into: the accessible name is now `"Add to: Top (plays next)"`, not a bare `"Add to"`. Renders are BYTE-IDENTICAL before/after in both states. Keeps two app-owned details: `data-value` inside option labels (what `e2e/pick.mjs` picks by) and the `id`→`data-testid` swap. ⚠️ The `pick.mjs` contract has NO CI coverage — the Playwright step is Plex-gated and skipped on every PR — so it was verified by hand against a scratch harness, which was then promoted to a permanent no-Plex CI step (`e2e/pick-contract-test.mjs`, 11 assertions on every PR))* |
| 2026-08-13 | [A PR merges as soon as CI is green](2026-08-13-prs-merge-as-soon-as-ci-is-green.md) *(process — repo-local restatement of the workspace decision "the AI merges its own PRs, the owner never merges"; green `ci` is the only gate. Squash + delete branch, and merge over `gh api -X PUT …/merge` because `gh pr merge` trips `'main' is already used by worktree` in this concurrently-worktree'd repo — a failure that reads like a merge block and isn't. Merging only builds/pushes the GHCR image; the live app still needs a deliberate `app.redeploy`, so green-merge cannot move the family's cards. Exception: an explicit hold)* |
| 2026-08-13 | [The provider block repeats, and its control is chosen by measurement](2026-08-13-provider-block-repeats-and-picks-its-control.md) *(ui / architecture — the whole `{provider, profile, libraries}` block repeats N times; mixing falls out of composition rather than out of a multi-select control. The owner withdrew his own Listbox/Combobox call, so the control went through the mock-then-pick procedure and came back **"Option B if they fit; otherwise Option A"** + **"C1 is good when only 1 provider"**. "If they fit" was MEASURED against the real `#setmodal` box, not eyeballed: segmented needs 264px at two providers (fits desktop AND a 390px phone), 371px at three (fits desktop, **overflows the phone**), 470px at four; `SelectListbox` is a flat 258px at any count. So the rule is evaluated at RUNTIME — providers are added from the connector surface — as none / segmented / listbox at 1 / 2 / 3+. One provider renders no control and no "Add another source" at all, so every existing Plex-only install sees no change whatsoever. The profile field is provider-scoped and branches on `delivery` (`push`|`pull`), never on the provider's name: "Plays under profile" + the Shield copy for Plex, "Reads as user" for a pull provider, where building a list as the wrong account fails SILENTLY. A single Plex block still writes through the legacy `sections`/`requires_profile` fields so an existing set is byte-identical on disk after an unrelated edit, and `sections` is kept in sync with the Plex blocks so the editor and playback can never disagree. What a MIXED queue hands off remains undecided and still refuses to run)* |
| 2026-08-12 | [`plex-channels` becomes **queuepilot**](2026-08-12-plex-channels-becomes-queuepilot.md) *(naming / product — `queue` is the data model, `pilot` is both autopilot and a first episode; free on npm/PyPI/GitHub (the HYPHENATED `queue-pilot` is taken on npm). The old name was misleading twice over: no longer Plex-only (Kavita verified, Jellyfin/Emby/Kodi wanted), and "channel" collides with real Live TV features in those same backends. Rejected + why: `tuner` (HDHomeRun OTA hardware in all three), `rabbit-ears`, `cue-stack`, the omakase family (DHH-branded AND inaccurate — nobody else is choosing), anything on `list` (Playlist/Reading List). Carries the full rename checklist, incl. three traps: `T_CMD_CAST_PLAY` is hardcoded publisher-side, `DISCOVERY_OBJECT_ID` renames `sensor.plex_channels_status`, and `PLEX_CLIENT_IDENTIFIER` must NOT change. **Decided, not executed**)* |
| 2026-08-12 | [A queue is ONE object with orthogonal mode knobs — not a named "queue vs channel" type](2026-08-12-queues-have-orthogonal-mode-knobs-not-named-types.md) *(data model / playback semantics — **supersedes the 2026-07-21 taxonomy** (point 1 only; its play-first IA and the anime rotation+batch behaviour stand). Five enum/count knobs — `pick_order` / `repeats` / `repeat_scope` / `batch` / `stop_after` — express all five requested behaviours as combinations, including the reading case (`rotate` + `batch 3`) with no new concept. **Corrects the record twice:** a channel is a strict ROUND-ROBIN, not random (the shuffle only picks which show leads); and it does not play forever — the lineup caps at `ROTATION_LENGTH=12` but `continuous: 1` lets the Plex CLIENT append its own Up Next. So "one each then stop" is reachable today via `max_items`; "stop after one pass" is not, because `max_items` is a count, not a pass. Design only)* |
| 2026-08-12 | [Backends are providers behind a media-neutral seam](2026-08-12-backends-are-providers-behind-a-media-neutral-seam.md) *(architecture — the engine's `client` (`container(path, token)` → a Plex `MediaContainer`) is Plex's wire format as an interface, which Kavita could only implement by emulating it. Widens at the same injection point into `buckets` / `progressState` / **`materialize` + `handoff`** — the split that makes Kavita fit, since Plex pushes a playQueue while Kavita returns a 302 URL. `buildRotation` + `routing.js` are already backend-neutral and reused as-is; `buildRotation` also gains the `batch` knob. **Corrects the drafted sequencing:** it assumed the port was parked at D2 behind `ENGINE=python`, but the port is CLOSED — `queue_builder/` is deleted, so the two-engine hazard cannot occur and the work is unblocked now. Design only)* |
| 2026-08-12 | [Provider definitions go in plaintext config; provider TOKENS go in a separate `/config` file](2026-08-12-provider-tokens-live-in-a-separate-config-file.md) *(architecture / security — connector definitions (id, label, kind, base_url) stay plaintext beside `sets.yaml` so they stay diffable and reviewable; tokens live in `/config/providers.secrets.yaml`, 0600, write-only in the UI, excluded from the undo-history and `.bak-*` machinery, resolved `env > file > NOT CONFIGURED`. An unset token must FAIL LOUDLY — the two placeholder outages (2026-08-06, 2026-08-10) both shipped a working-looking app that talked to nothing. **Corrects the drafted mechanism:** `SECRET_PATHS` is the agent-SANDBOX container's allowlist (`agent-sandbox-base/entrypoint.sh:56`) and does not exist for this TrueNAS app — only the *principle* transfers. Design only)* |
| 2026-08-12 | [Python is deleted — except the cast sidecar](2026-08-12-python-is-gone-except-the-cast-sidecar.md) *(architecture / cleanup — completes the 2026-08-03 retirement plan: `queue_builder/`, the Python tests, the corpus generator, the live-corpus recorder and the `ENGINE`/`PLAYBACK_ENGINE` switches are gone; Node is the only implementation. `cast_sidecar/` stays (owner's call) because `playback.js` delegates `PLAYBACK_MODE=cast` to pychromecast, so the image keeps python3 + 3 packages. The four Node↔Python parity gates keep running against the deleted engine's RECORDED answers in `e2e/fixtures/golden/` — a contract, not a snapshot to refresh — and the seven Python-only engine tests were ported to `.mjs` 1:1 via `node:module` resolve-hook stubs. Casualty: the unused soundtrack resolver)* |
| 2026-08-12 | [A multi-episode batch may be stopped at a season / member boundary (`batch_stops_at`)](2026-08-12-batch-stops-at-season-and-member-boundaries.md) *(feature — the batch cap was a plain COUNT, so `episodes: 2` at a season finale queued S1E12 + S2E01, and inside a `Collection:` it queued show A's finale + show B's episode 1. Adds an orthogonal knob: the count says how many, `batch_stops_at` says where the batch may END — `none` (default, unchanged) / `member` / `season`. Per-set in sets.yaml, per-entry override in queues.yaml, entry > set > global. Only ever shortens, never below ONE item (an empty batch is the FINISHED signal). The uncapped rotation callers are untouched. Gates: `e2e/batch-stops-at-test.{py,mjs}`)* |
| 2026-08-11 | [A `Collection:` entry contributes ONE batch, exactly like a show entry](2026-08-11-collection-entries-contribute-one-batch-like-show-entries.md) *(bugfix — `resolve_member`/`resolveMember` sliced the show branch to `episodes \|\| default_batch` but returned `collection_items()` raw, so a collection emptied its children's whole unwatched run into one scan while a plain show entry beside it yielded one episode. The anime channel built a 12-item "rotation" of 9 consecutive Chaika + 2 Nadesico + 1 Gleipnir; after the fix the same 12 slots hold 12 distinct shows. Capped EVERYWHERE (episode and movie collections alike, no per-type special case); the rotation/member-bucket callers pass no default batch and stay uncapped. Clarifies — does not reverse — the 2026-07-21 collections decision, whose stated intent was "the same footing as show entries". Gate: `e2e/collection-batch-cap-test.mjs`; a Node↔Python parity gate structurally cannot catch this class)* |
| 2026-08-08 | [Curated queue flags (`keep_completed` / `reel` / `remove_completed_after`) are editable in the Set editor via Charcuterie Checkbox](2026-08-08-set-modal-queue-flags.md) *(frontend + server — SetModal gains a Playback & completion fieldset; API normalize/create/update round-trip the three knobs for curated queues only; requires `@charcuterie/ui@^2.6.0`; supersedes the "Web UI needs no change" clause of the keep_completed decision for the flags themselves)* |
| 2026-08-07 | [The leaves cache re-validates on read against the show's live watch aggregate](2026-08-07-leaves-cache-revalidates-on-read.md) *(bugfix / caching — `plex.allLeaves` now validates cached episodes against the show-node `(updatedAt, viewedLeafCount)` every read, so an episode finished OUTSIDE the app's flow (manual Plex play / another client) self-heals the tile's next-up instead of staying stale up to the 24h TTL. Root cause: the display path passed no validator AND the `allLeaves` container omits those fields, so stored `viewedLeafCount`/`updatedAt` were always 0. One light show-node call (no episodes); Plex-down → the old TTL path unchanged. Real case: `.hack//SIGN` stuck on E19 In-Progress after a manual finish. Regression test `e2e/leaves-revalidate-test.mjs`)* |
| 2026-08-07 | [Curated queues can choose their `requires_profile` in the web editor](2026-08-07-choose-profile-for-queues.md) *(frontend + server — a "Plays under profile" select in `SetModal` writes the play-gate; options from `/api/profiles` with owner→plex.tv username, managed users→title, "Any"→ungated; rotation channels reject it since they are profile-driven)* |
| 2026-08-07 | [plex-channels pickers are a themed `Listbox`, not the native `Select`](2026-08-07-plex-channels-pickers-are-listbox-not-native-select.md) *(UI / component choice — EVERY picker becomes a Charcuterie `Listbox` via the `SelectListbox` adapter (tier picker, channel/profile, Add-to position, move-to, eps-per-play, and the in-modal set-kind/behavior/plex-profile/start-series·season·episode); reverses the app's native-`Select` default now that `@charcuterie/ui@2.x` ships `Listbox`. Required rebuilding the app `Modal` on Charcuterie's body-portalled base `Modal` (was native `<dialog>`+showModal, whose top layer occluded the dropdown) — API/DOM preserved, `[open]`→`data-open`. e2e drives via `data-testid` + `[role=option] [data-value]`)* |
| 2026-08-07 | [A channel can name a default profile the Play/Channels dropdowns start on](2026-08-07-default-profile-per-channel.md) *(UI / data model — a rotation channel's top-level `default_profile` (a binding's `plex_user`) seeds the Play row + Channels dropdown so it plays the right tier without re-picking; set from the ⚙ Configure editor when ≥2 bindings, blank clears it. A pure UI-seed hint: the Python engine ignores it and still plays what the play menu passes; a stale/renamed value falls back to `profiles[0]`)* |
| 2026-08-07 | [Episode counts exclude trailers/OP-ED by Season-0 index range; only real episodes + regular specials + "other" count](2026-08-07-specials-count-excludes-op-ed-trailer-extras.md) *(refines 2026-07-17 — one shared predicate `isExtraOrPromo`/`is_extra_or_promo` excludes Season-0 leaves at index 200–399 (trailers/OP-ED) plus Plex clips/extraType, keeping index 1–99 specials + 400+ "other". Node counts computed from `allLeaves` (missing `viewCount` = unwatched); `collectionNext` now advances past an ED-only series (Saiki → S4 E1). Deterministic by index — NOT duration/title. ⚠️ index 100–199 unspecified → INCLUDE, owner to confirm; composes with PR #13's `_keep_episode` specials-only keep)* |
| 2026-08-07 | [An in-progress queued item is never "finished" — and a stale `done` is revived](2026-08-07-in-progress-queue-items-are-never-finished.md) *(bugfix — a queued item whose next-up leaf/movie is mid-playback (`viewOffset>0`, `viewCount` 0/absent) must never read finished/done: missing `viewCount`=unwatched everywhere, a specials-only OAD stays playable, an in-progress leaf survives a watched-history hit, a stale `done:true` is revived + cleared (`queues.clear_done`) before the TTL sweep, shuffled anime channels lead with it, and the web tile shows an "In Progress" badge. Root case: the "Prison School: Mad Wax" Season-0 OAD read "Completed" while mid-episode)* |
| 2026-08-07 | [A `keep_completed` queue flag makes a set non-consuming (a re-showable playlist)](2026-08-07-non-consuming-keep-completed-queue-flag.md) *(feature — `source: queue` sets may set `keep_completed: true`; `plex.next_queue` then never marks entries `done`, so the whole lineup re-shows every scan (e.g. the Demo Reel). Decoupled from `reel`, which governs play-all-every-scan; `reel: true` implies `keep_completed`. Writes no state → inherently sweep-exempt; web "Completed" badge mirrored by construction)* |
| 2026-08-07 | [Completed queue entries auto-remove after a TTL (kept, then swept)](2026-08-07-ttl-auto-remove-completed-queue-entries.md) *(feature — `mark_done` stamps `done_at`; `queues.sweep_completed` removes entries past the set's window on each scan (wired into `plex.next_queue`), reusing `prune`. Default is OPT-IN/keep-forever (`REMOVE_COMPLETED_AFTER=never`); a set turns it on with `remove_completed_after: 24h` — movie queues opt in, anime channels stay kept; `keep_completed`/`reel` exempt. Node mirror in `server/src/queues.js`)* |
| 2026-08-03 | [Crossing `@charcuterie/ui` 2.x is clean; the scheme switcher goes neutral](2026-08-03-charcuterie-ui-2-crosses-clean-neutral-switcher.md) *(dependency — bump ui `^1.1.0`→`^2.2.0` for the neutral `intent` default; the 2.0.0 `Modal`→`Dialog` overlay rebuild touches nothing we import (own modal; Accordion/Select/Badge/EmptyState/Switcher unchanged); measured clean — typecheck/tests/build green, no source change)* |
| 2026-08-03 | [Follow the OS light/dark scheme via Charcuterie's `ColorSchemeSwitcher`](2026-08-03-follow-the-os-colour-scheme-via-charcuterie-switcher.md) *(frontend behavior — `<ColorSchemeSwitcher>` in the header, default `system`, cycle light→dark→system; `buildFirstPaintScript` injected head-prepend for no-flash; inline sun/moon/monitor SVGs, no new icon dep. **OPEN:** owner must decide whether the kids' always-on wall display should pin dark before this ships — do NOT merge/deploy until then)* |
| 2026-08-03 | [The `app.css` `@layer` fix is a separate, screenshot-gated change — not yet shipped](2026-08-03-app-css-layer-fix-is-a-separate-screenshot-gated-change.md) *(frontend convention — F6 reorders the whole cascade below Tailwind's utilities; deferred because its only real gate is a scheme/density screenshot review, and the batch it would ride in had none)* |
| 2026-08-03 | [SQLite is a derived Plex cache, not the store — YAML stays the source of truth](2026-08-03-sqlite-is-a-derived-plex-cache-not-the-store.md) *(architecture / storage — `/config/cache.sqlite` is deletable, gitignored, schema-versioned and wiped on mismatch; clarifies the 2026-07-21 "no SQLite" decision without reversing it, and forbids a later config migration into it)* |
| 2026-08-03 | [Retire `queue_builder/` to Node — keep a minimal Python cast sidecar](2026-08-03-retiring-python-except-the-cast-sidecar.md) *(architecture / scope — supersedes in part the 2026-07-21 deferral, whose blocker was account-token minting; that is already ported. MQTT stays for HA; the internal round trip goes)* |
| 2026-08-02 | [CI runs on GitHub Actions and the image is built-on-push to GHCR](2026-08-02-ci-and-deploy-on-github-actions-to-ghcr.md) *(infrastructure — ported `.gitea` CI to `.github/workflows/`; added a mux-magic-style `workflow_run` deploy that builds+pushes `ghcr.io/sawtaytoes/plex-channels`; the LAN-registry→GHCR production cutover is DONE — the live app pulls `ghcr.io/...:latest` and serves the Vite build)* |
| 2026-08-02 | [Adopting a `@charcuterie/ui` component means DELETING the app's skin for it](2026-08-02-adopting-a-component-means-deleting-its-skin.md) *(frontend convention — `app.css` is unlayered and outranks every Tailwind utility, so a kept `padding`/`color` makes the component's props a silent no-op; app keeps layout + selectors only)* |
| 2026-08-02 | [An uncontrolled `@charcuterie/ui` control is keyed on its SECOND writer, never on its value](2026-08-02-uncontrolled-components-are-keyed-on-their-second-writer.md) *(frontend convention — `value`/`expandedKeys` are initial-only; keying on the value remounts under the user's own pick)* |
| 2026-08-02 | [Five `@charcuterie/ui` components plex-channels cannot adopt yet, and why each is a library gap](2026-08-02-components-that-cannot-be-adopted-yet.md) *(migration scope — `Field` overwrites the control's `id`; `Menu` has no point anchor and no non-item states; `Modal` has no form; `Badge` has no `asChild` and no two-part shape)* |
| 2026-07-31 | [The web frontend is React 19 + TypeScript + Vite + Tailwind v4 on `@charcuterie/tokens`](2026-07-31-frontend-is-react-typescript-vite-tailwind-on-charcuterie-tokens.md) *(architecture / build — charcuterie M6d phase 1; adds a build step, keeps the hash routing + the whole DOM contract, applies Plex amber as the accent intent)* |
| 2026-07-31 | [The start episode is PICKED in a modal — no inline control on the tile](2026-07-31-start-episode-is-picked-in-a-modal.md) *(UI — supersedes the inline "Start" field; adds the collection series+season+episode picker and the tile context menu)* |
| 2026-07-31 | [A Collection tile shows the MEMBER that plays next — the collection moves to the badge](2026-07-31-collection-tiles-are-member-first.md) *(UI — member poster/name + episode-only line; also drops the "Play" label before the eps dropdown)* |
| 2026-07-31 | [A show entry can pin a manual START episode — a floor for "next", never a watched-write](2026-07-31-per-entry-start-episode-override.md) *(direction — implementation pending; season+episode, single-season → episode-only, collection → series+season+episode)* |
| 2026-07-31 | [Curated members are additive includes on top of the rule pool — a channel can be a mix](2026-07-31-curated-members-are-additive-includes.md) *(behavior — supersedes the "curated vs dynamic, no convergence" point; member search spans all libraries)* |
| 2026-07-29 | [Each binding card renders its OWN ratings, not the active profile's scoped list](2026-07-29-binding-ratings-render-per-profile-not-shared-scope.md) *(bug fix — a non-active profile's binding came up blank in Configure)* |
| 2026-07-29 | [A rewatch channel's pool follows the libraries IT names — and a show library contributes its one-episode films](2026-07-29-rewatch-pool-follows-the-channels-own-libraries.md) *(selection logic — drops the SEC_MOVIES hardwire; anime films reachable; supersedes the "hide the inert pickers" clause)* |
| 2026-07-29 | [The eligible pool lists every short by name — a library bucket is not a show](2026-07-29-shorts-preview-lists-each-short.md) *(UI / preview payload — per-short tiles + per-short Exclude)* |
| 2026-07-29 | [Every dynamic channel is a first-class UI entry, and rotation channels are deletable](2026-07-29-dynamic-channels-first-class-and-deletable.md) *(UI / data model — per-channel rows + Delete button; rewatch hides its inert library pickers; supersedes the no-delete clause)* |
| 2026-07-29 | [Drop `set:"auto"` ("Shield pick") from the UI — every play is explicit](2026-07-29-drop-set-auto-from-ui-every-play-explicit.md) *(UI / interaction — backend auto path unchanged)* |
| 2026-07-27 | [Younger Kids gets Shows and Shorts as two cards; Older Kids keeps them combined](2026-07-27-younger-kids-shows-and-shorts-are-two-cards.md) *(channel + card model — adds the `shows`/`shorts` channels; a rotation channel may now have no show library)* |
| 2026-07-26 | [Spoken status is a sentence, not a diagnostic — and it only speaks when something is actually wrong](2026-07-26-spoken-status-is-a-sentence-not-a-diagnostic.md) *(UX — `state.error` is read aloud verbatim; no timeouts/jargon, and the wait announcement fires at 25s not 6s)* |
| 2026-07-26 | [A card names a Plex profile, and the scan waits for the Shield to really be on it](2026-07-26-cards-name-a-profile-and-the-scan-waits-for-it.md) *(playback gating — the per-tier cards; extends `requires_profile` from per-SET to per-SCAN, plus the ADB auto-switch)* |
| 2026-07-25 | [A set can require a Plex Home profile (`requires_profile`); the scan waits for the on-screen switch](2026-07-25-sets-can-require-a-plex-profile.md) *(playback gating — the demo reel's libraries are invisible to the kid profiles)* |
| 2026-07-23 | [Live migration: `younger`/`older` tiers → `shows_shorts` + `movies` function channels (soak + `channel_for` routing)](2026-07-23-live-tier-migration-to-function-channels.md) *(migration — v3 PR 4)* |
| 2026-07-23 | [`sets.yaml` rotation channels carry a `profiles[]` binding array + a `behavior` field (back-compat reader)](2026-07-23-sets-yaml-profiles-array-schema.md) *(schema — v3 PR 2a)* |
| 2026-07-21 | [Human-readable YAML is no longer a goal; stable IDs (ratingKeys) are canonical entry identity, not title strings](2026-07-21-drop-human-readable-yaml-canonical-ids.md) *(direction; supersedes the hand-editable goal)* |
| 2026-07-21 | [Every interactive element must have obvious hover, focus (keyboard), and motion states](2026-07-21-ui-interaction-states-standard.md) *(UI standard)* |
| 2026-07-21 | [Defer the queue_builder Python → Node port (revisit later, not now)](2026-07-21-defer-python-to-node-port.md) *(scope)* |
| 2026-07-21 | [Channels are function-first with a profile selector; members generalize (show/collection/movie/short) and behavior is progress-vs-rewatch](2026-07-21-channels-function-first-generalized-members.md) *(direction — v3)* |
| 2026-07-21 | [Dynamic (rotation) channels are created + fully configured in Node, not hardcoded in Python](2026-07-21-dynamic-channels-fully-configurable-in-node.md) *(v2)* |
| 2026-07-21 | [Channels split into "Dynamic Channels" vs "Curated Channels"; consistent nav + config surface, fix the "queue" noun](2026-07-21-dynamic-vs-curated-channel-categories.md) *(v2)* |
| 2026-07-21 | [Plex Collections are first-class queue entries, expanded in collection order](2026-07-21-plex-collections-as-ordered-queue-entries.md) *(v2)* |
| 2026-07-21 | [Finished queue entries are kept + marked `done`, not pruned — removed only on "remove all completed"](2026-07-21-finished-queue-entries-marked-done-not-pruned.md) *(v2; supersedes the prune half of the movie-queue wishlist)* |
| 2026-07-21 | [Queues vs channels taxonomy (anime sets are channels, random rotation; Movies is its own channel) + play-first entry IA](2026-07-21-queues-vs-channels-taxonomy-play-first-ia.md) *(shipped; **taxonomy superseded 2026-08-12** by [orthogonal mode knobs](2026-08-12-queues-have-orthogonal-mode-knobs-not-named-types.md) — the play-first IA and the anime rotation+batch behaviour stand)* |
| 2026-07-21 | [Shelf/tile UI conventions: reserved scrollbar space, edge shadows, zoom-proof glyphs, 480px posters](2026-07-21-shelf-ui-conventions.md) *(shipped)* |
| 2026-07-21 | [Sets are data (sets.yaml): immutable ids, renameable labels, per-set libraries, global excludes](2026-07-21-sets-registry-immutable-ids.md) *(shipped)* |
| 2026-07-21 | [The editor is a real live webapp (SSE, no Refresh); monorepo server/+web/; YAML stays, no SQLite](2026-07-21-real-webapp-sse-yaml-not-sqlite.md) *(shipped)* |
| 2026-07-20 | [`queues.yaml` carries no per-set label comments; audience→key map lives in the header](2026-07-20-queues-yaml-no-per-set-label-comments.md) |
| 2026-07-20 | [Queue web UI: Plex-style UX, ratingKey writes, docs count as movies](2026-07-20-queue-web-ui-ux-and-write-format.md) *(shipped)* |
| 2026-07-20 | [The queue web UI is a monorepo in one container, not a separate app](2026-07-20-queue-web-ui-monorepo-single-container.md) *(shipped)* |
| 2026-07-20 | [Queue entries are human-readable title strings, not ratingKeys](2026-07-20-queue-entries-are-title-strings.md) *(shipped)* |
| 2026-07-20 | [The queue config web UI is a Node.js app (like mux-magic), not embedded Flask](2026-07-20-queue-web-ui-is-nodejs-not-flask.md) *(packaging amended → monorepo/one container)* |
| 2026-07-17 | [A queued/rotated series never plays specials — Season 0 is excluded entirely](2026-07-17-anime-series-never-open-on-specials-exclude-season-0.md) |
| 2026-07-16 | [The queue config web app is embedded in plex-channels, and edits the same YAML](2026-07-16-config-web-app-embedded-in-plex-channels.md) *(stack + process model amended 2026-07-20 → Node.js)* |
| 2026-07-16 | [Anime becomes three curated queues; the On Deck set is retired](2026-07-16-anime-queues-retire-ondeck-set.md) |
| 2026-07-16 | [Movie-queue sets are a hand-edited YAML wishlist, pruned on finish, played as Bob](2026-07-16-movie-queue-sets-yaml-wishlist.md) |
| 2026-07-09 | [Bob's "anime" set = On Deck (Continue Watching), not whole-library](2026-07-09-anime-continue-watching-set.md) (superseded; specials rule survives) |
| 2026-07-08 | [Kids' channels draw from Shows + Shorts only — no Anime](2026-07-08-kids-channels-shows-and-shorts-only-no-anime.md) |
