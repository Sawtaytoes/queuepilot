# Decision records — queuepilot (was plex-channels)

One decision per file, `YYYY-MM-DD-<kebab-slug>.md` (date = when decided). Newest first.

| Date | Decision |
| --- | --- |
| 2026-08-30 | [The watch-history source is a provider capability, and QueuePilot is the fallback](2026-08-30-the-watch-history-source-is-a-provider-capability-and-queuepilot-is-the-fallback.md) *(architecture / playback semantics / durable data — watch history has three possible sources: the provider, QueuePilot, or an external history service. A provider declares whether it can report watch history; one that cannot has no `provider` option and its queues own their history by default. `watch_history` gains `external`. Catalog and history stay separate seams.)* |
| 2026-08-30 | [A streaming service is a catalog read and a launch hand-off, not an API integration](2026-08-30-a-streaming-service-is-a-catalog-read-and-a-launch-hand-off.md) *(architecture / provider seam / scope — a subscription streaming service has no public API and no obtainable key. Its catalog is read from TMDB with a per-deployment key and mandatory JustWatch attribution; playback is a deep link published over MQTT and opened by something else. No private application API, no extracted token, no stream control.)* |
| 2026-08-30 | [Provider watch history is the default, and entries can opt out](2026-08-30-provider-watch-history-is-the-default-and-entries-can-opt-out.md) *(playback semantics / durable data / UI / correction — a Picks queue defaults to provider history; an entry can opt out independently of its start point; QueuePilot-owned entries can mark, undo or attach the Plex play that is already running.)* |
| 2026-08-30 | [A Picks entry can shuffle its playable items](2026-08-30-a-picks-entry-can-shuffle-its-playable-items.md) *(playback semantics / data model / UI — a Plex show or Collection entry can set `item_order: shuffle`; it draws from all playable watched and unwatched items, resumes one in-progress item first, shuffles the rest without replacement, then applies the existing per-visit count. The Picks Random pool remains the outer entry-order axis.)* |
| 2026-08-30 | [The README is an entry point, not a decision log](2026-08-30-the-readme-is-an-entry-point-not-a-decision-log.md) *(documentation — keep the README short: product summary, prominent Docker setup link, run commands and focused documentation links. Provider configuration moves to `docs/setup.md`; naming rationale, history, rejected alternatives and architecture rationale stay in focused docs and decision records.)* |
| 2026-08-30 | [Plex decides completion, and the queue owns the resume point](2026-08-30-plex-decides-completion-and-the-queue-owns-the-resume-point.md) *(playback semantics / correction / durable data — QueuePilot stores the queue entry's live position, seeks from it, and observes Plex's play-count/viewOffset result instead of copying its configurable percentage-and-credits completion rule.)* |
| 2026-08-30 | [A manual start can own its progress](2026-08-30-a-manual-start-can-own-its-progress.md) *(playback semantics / durable data / UI — a start chooses a per-queue completion ledger or the original provider-history filter; new starts default to queue-owned progress, older mode-less starts remain compatible, and saving a queue-owned start begins a new run.)* |
| 2026-08-30 | [Pending series configuration includes what plays and drag order](2026-08-30-pending-series-configuration-includes-what-plays-and-drag-order.md) *(UI / interaction — Pending configures the start and What plays before Add to; collection rows use drag handles with Up/Down keyboard movement instead of Earlier/Later buttons.)* |
| 2026-08-30 | [Rules queues share manual includes and exclude search](2026-08-30-rules-queues-share-manual-includes-and-exclude-search.md) *(product / UI / playback correction — Shows and Movies Rules queues both expose an exclusion search and manual includes; movie members join the rewatch candidates instead of being saved and ignored.)* |
| 2026-08-30 | [Queue entries sort by queue arrival, and global search can pick a start](2026-08-30-queue-entries-sort-by-queue-arrival-and-global-search-can-pick-a-start.md) *(queue data / UI — every add stamps `queued_at`; Recently added sorts both lanes newest-first; global search can hold a show or collection start choice until a real add.)* |
| 2026-08-30 | [The shell has one vertical scroll owner](2026-08-30-the-shell-has-one-vertical-scroll-owner.md) *(UI / layout — `Main` owns work-page scrolling; obsolete body clearance cannot create a second document scrollbar or dead space.)* |
| 2026-08-30 | [Skipped items follow the queue view](2026-08-30-skipped-items-follow-the-queue-view.md) *(UI / correction — Skipped moves to the bottom and reuses Posters, Cards or List with artwork, Plex links, source badges and Restore in the play position.)* |
| 2026-08-30 | [Selection mode owns item clicks and Shift selects the range](2026-08-30-selection-mode-owns-item-clicks-and-shift-selects-the-range.md) *(interaction / correction — after one entry is selected, any item's non-control surface toggles it in every density; Shift-click adds the inclusive visible range from the prior anchor.)* |
| 2026-08-30 | [Changing a queue default preserves every existing lane](2026-08-30-changing-a-queue-default-preserves-existing-lanes.md) *(queue semantics / correction — `add_as` controls future additions only; the writer materialises inherited lanes before changing it, so existing Priority and Random assignments do not move.)* |
| 2026-08-30 | [Pending collections can pick a start point before the add](2026-08-30-pending-collections-can-pick-a-start-point.md) *(UI / behaviour — collection cards get the same start picker as show cards; choose the member, then its episode when applicable, before adding.)* |
| 2026-08-30 | [Rules follow Picks and share the queue filters](2026-08-30-rules-follow-picks-and-share-the-queue-filters.md) *(UI / correction — the people and provider filters and counts cover both queue kinds; Picks precede the less frequently changed Rules.)* |
| 2026-08-30 | [A collection order can override and reset to Plex](2026-08-30-a-collection-order-can-override-and-reset-to-plex.md) *(product / data model — a collection entry can override Plex member order; Reset deletes the override; new Plex members append in Plex order.)* |
| 2026-08-29 | [Rules and Picks use the same collapsible queue region](2026-08-29-rules-and-picks-use-the-same-collapsible-queue-region.md) *(UI / correction — replaces the separate Rules card grid with the same collapsible horizontal shelf used for Picks; expanded Rules shelves show a read-only eligible-title preview and the page-wide collapse control covers both kinds.)* |
| 2026-08-29 | [A touch hold is the drag, and it scrolls](2026-08-29-a-touch-hold-is-the-drag-and-it-scrolls.md) *(touch interaction / correction — a poster hold always picks up the tile, a later context-menu event cannot replace the drag, edge scrolling reaches off-screen destinations, and the Priority number owns its touch events.)* |
| 2026-08-29 | [New Picks land at the top](2026-08-29-new-picks-land-at-the-top.md) *(queue ordering / correction — all three add-to-queue paths insert at the top, where the new title is visible beside the search that added it; the direct Priority number and lane controls remain.)* |
| 2026-08-29 | [Queues is one index, and creation starts with kind](2026-08-29-queues-is-one-index-and-creation-starts-with-kind.md) *(product / routing / UI — `/queues` lists Picks and Rules together; creation chooses the kind first; Rules detail has no queue selector; `/picks` and bare `/channels` rewrite to the index.)* |
| 2026-08-29 | [Collection is a picker with one route per maintained collection](2026-08-29-collection-is-a-picker-with-one-route-per-maintained-collection.md) *(product / routing / UI — `/collection` selects a QueuePilot-maintained collection and `/collection/board-games` opens the existing shelf.)* |
| 2026-08-29 | [Picks queues keep the existing people and provider filters](2026-08-29-picks-queues-keep-the-existing-filters.md) *(UI / reuse — move the existing `LandingFilterBar` from the retired overview to the active Picks shelf list; preserve its multi-select people, provider, URL and group-aware filter semantics.)* |
| 2026-08-29 | [Condensed controls use stable rows, and Rules filters stay first](2026-08-29-condensed-controls-use-stable-rows-and-rules-filters-stay-first.md) *(responsive UI / layout — the Picks toolbar uses stable container-based rows, the shelf collapse control aligns with the queue-name row, and Rules eligibility filters stay above the generated result at one-column widths.)* |
| 2026-08-29 | [New Picks land at the end, and Priority order is directly editable](2026-08-29-new-picks-land-at-the-end-and-priority-order-is-directly-editable.md) *(interaction / queue ordering — search additions append, the Add-to-position controls and redundant Rules link leave, Priority tiles show editable positions, and checked entries get direct group lane actions.)* |
| 2026-08-29 | [Navigation names the two queue kinds, and Rules show eligible titles](2026-08-29-navigation-names-the-two-queue-kinds-and-rules-show-eligible-titles.md) *(product naming / UI — primary navigation and page headings say Picks queues / Rules queues; the Rules result is Eligible titles and its panel is Eligibility filters; Random pool remains the Picks lane.)* |
| 2026-08-29 | [The home is task-based, and work pages use the navigation rail](2026-08-29-the-home-is-task-based-and-work-pages-use-the-navigation-rail.md) *(product / routing / UI — `/` has two primary tasks and five focused management destinations; work pages use Charcuterie's responsive rail, icon rail, and one-menu Narrow View; `/admin` becomes a legacy address.)* |
| 2026-08-29 | [Queue shelf headings omit the audience chevron](2026-08-29-queue-shelf-headings-omit-the-audience-chevron.md) *(UI — `/queues` shelf headings keep the avatar badges and audience names, omit the provider label and the redundant audience chevron, and keep one baseline.)* |
| 2026-08-29 | [The Picks page is `/picks` and starts collapsed](2026-08-29-the-picks-page-is-picks-and-starts-collapsed.md) *(product / UI / routing — `/picks` is canonical, `/queues` redirects, every shelf starts collapsed before a preference exists, and the existing bulk toggle offers Expand all or Collapse all for the current state.)* |
| 2026-08-29 | [Queue shelf headings use avatar badges and one baseline](2026-08-29-queue-shelf-headings-use-avatar-badges-and-one-baseline.md) *(UI — `/queues` shelf headings keep the queue title, count, lane summary, chevron, avatar badges and audience names, omit the provider kind label, and baseline-align the heading contents.)* |
| 2026-08-29 | [A release year is shown once](2026-08-29-a-release-year-is-shown-once.md) *(display rule — an upstream title that already ends in the item's release year keeps that one suffix; QueuePilot does not print it again.)* |
| 2026-08-29 | [Queue shelf headings use audience names only](2026-08-29-queue-shelf-headings-use-audience-names-only.md) *(UI — `/queues` shelf headings keep the queue title, count, lane summary and audience names, but omit the provider kind and initials face marker because the heading is already dense. Landing cards and editor surfaces keep face markers.)* |
| 2026-08-29 | [People groups use explicit rules and the queue audience is one vertical list](2026-08-29-people-groups-use-explicit-rules-and-vertical-audience-editor.md) *(UX / data model — keep people groups as saved rules, show their rule in plain language, edit queue audiences in one vertical Must / Nice / Everyone else list, and restore group create/edit/delete.)* |
| 2026-08-28 | [Specials are skipped by default and selected one at a time](2026-08-28-specials-are-skipped-by-default-and-selected-one-at-a-time.md) *(product / playback / data model — supersedes the July exclude-all rule. A normal show's regular Season-0 rows appear after the normal seasons in **What plays**, unticked by default; ticking one writes its leaf id to `included_specials:`. Trailers, OP-ED, clips and `extraType` rows remain unavailable. A selected special uses Plex's original availability date when present and otherwise follows the normal run, so Season 0 never front-loads an undated choice. The Start list remains the playable list and does not expose unselected specials.)* |
| 2026-08-28 | [QueuePilot owns its top-up timer and publishes the result over MQTT](2026-08-28-queuepilot-owns-its-top-up-timer.md) *(architecture / correction — supersedes the schedule-ownership clause of the 2026-08-17 refill decision. The five-minute live-session check and fifteen-minute pull-list check are application-lifecycle timers inside QueuePilot. Each result still publishes on `queuepilot/resp/topup`, and the empty `queuepilot/cmd/session/topup` command remains the manual compatibility seam. Home Assistant observes failures and still owns finished-sitting power decisions; it no longer loops a time-pattern command back into the app.)* |
| 2026-08-28 | [Surprise Me narrows to Media, Games or Reading](2026-08-28-surprise-me-narrows-to-media-games-or-reading.md) *(product / Tonight — the second step has exactly three scopes: Media = Movies + Shows + future YouTube, Games = Video Games + Board Games, Reading = Reading. It chooses an activity uniformly inside the scope, then delegates to that activity's existing queue or shelf engine; an empty activity falls through to the remaining activities. The result stays bound to the activity that won, and a bare Surprise Me preset remains refused because it cannot answer the scope question.)* |
| 2026-08-27 | [The Groups editor is retired](2026-08-27-the-groups-editor-is-retired.md) *(product scope / frontend — remove the Groups modal, its landing and People-editor entry points, its client overlay and editor snapshot loading. Keep the server-side group model, API and live data because groups still carry queue audience rules and provider-profile resolution. A full removal needs a separate migration.)* |
| 2026-08-27 | [The route table is react-router, not a parsed pathname](2026-08-27-the-route-table-is-react-router-not-a-parsed-pathname.md) *(architecture / frontend — supersedes the "what is left of the hand-rolled router" half of the 2026-08-16 paths record. The app had the fleet's router and not its route TABLE: a real `<BrowserRouter>`, real `<Link>`s and the Charcuterie link seam since 2026-08-16, and then `useLocation().pathname` handed to a hand-written `parsePath()` whose tagged union drove nine `hidden` attributes and a second switch called `computeChrome()`. Defensible at four routes; it answered NINE plus three legacy addresses, and carried a `// Longest first:` comment because nothing but the reading order kept the bare `/what-to-watch-play` route from swallowing its own two steps — route ranking done by reading order, which fails silently in the direction that looks like it worked. So: a `<Routes>` table in `App.tsx`, paths as constants in `lib/routePaths.ts` that the pure test matches through react-router's own `matchRoutes`, a view mounted only on its own route (nine `<main>` elements per page → one, and 57 `isHidden` references gone), and each page stating its chrome as props to `components/Page.tsx`. `AppFrame` is the pathless layout route and holds what outlives a page. The three moved addresses — `/g/<id>`, `/collection`, `/tonight` — each get a legacy route that PAINTS the page they moved to and rewrites the URL underneath, because a `<Navigate>` would blank the screen for a frame. One gate broke, loudly and correctly: `routing-test` asserted `#goqueues` was still an `<a href>` AFTER clicking it, which only passed while Admin stayed mounted. `tile-menu-test` fails on this branch and fails identically on unmodified `main`.)* |
| 2026-08-27 | [A revalidate never overwrites a write it did not see](2026-08-27-a-revalidate-never-overwrites-a-write-it-did-not-see.md) *(bugfix / client state — the read cache put a seven-second `/api/queues?fresh=1` pass behind every page load, and its answer describes the files as they were when it ASKED. A demote made inside that window was missing from the answer, and `setState({data})` painted the entry back into the lane it had just left — while `queues.yaml` kept the move, so the screen undid a write the file had honoured. Measured: the request goes out at t+2.96s and lands at t+5.8s, inside the second `e2e/tile-menu-test` spends demoting a tile, which is why that suite failed on the first CI run carrying both #234 and #227 and on nothing before it. The pass now records `writeCount()` before its fetch and DROPS the payload if the number moved, and refuses to commit while `uiBusy()` — the rule `state/live.ts` already followed. It drops rather than retries: a retry costs 566 provider calls to buy a fresher poster on a page that has one. The live path's two existing guards cannot cover this pass — `?fresh=1` re-reads the providers so it never 304s, and a tap on a menu row leaves nothing busy. Also recorded because it hid the failure for an hour: the e2e harness serves `web/dist`, so a suite run against a bundle built before the cache landed tests a frontend with no phase 3 in it.)* |
| 2026-08-27 | [A lane change writes the order too, because the file is one sequence](2026-08-27-a-lane-change-writes-the-order-too-because-the-file-is-one-sequence.md) *(data write / consistency — the tile's ↑ / ↓ arrow and the tile menu's lane rows were built on branches that were open at the same time, and each grew its own order helper: `promotedOrder` for the arrow, `orderAfterLaneMove` for the menu. Rebasing put both in one tree, where their gates asserted opposite things about one operation — `tile-lane-test` pinned "a demote writes NO order", `tile-menu-test` pinned "placement, THEN the order". Both passed alone; neither could pass on both. Resolved toward writing it: the file is ONE sequence, an entry demoted but left in the priority run leaves the file saying two things, and the next promote re-sequences it anyway because `orderAfterLaneMove` rebuilds priority-then-random from the placements it reads — so the saved write was deferred, not avoided. The drag across the divider has always written both, which is the precedent the arrow claimed to follow and did not. Nothing on screen changes either way (`splitLanes` re-derives the lanes from `placement`), which is exactly why it could drift unseen. `moveEntryLane` is a toggle wrapper over `setEntryLane` now and computes nothing; `promotedOrder` is deleted.)* |
| 2026-08-27 | [The tray move handle wears the gesture that can succeed](2026-08-27-the-tray-move-handle-wears-the-gesture-that-can-succeed.md) *(UI / affordance — supersedes the blanket "never `≡`" rule of the record below, the same day, on the owner's correction: **"I think the drag handles were fine, but now you have it in a 3-column mode, so dragging would work, but it has this 'move' button instead. The 'move' button would be best in that mobile view I showed you in the screenshot."** The two reports are not a contradiction; they are two halves of one rule, and the first fix took one half. `PeopleTrays` passes `moveIcon="≡"` again and `@charcuterie/ui` paints it only while the three trays are side by side — under the board's `cq-lg` it swaps itself for the word "Move", because there is nothing on screen to drop onto and the glyph would teach the one gesture that cannot work. The rule lives in the LIBRARY, not here: whether another lane is on screen is a fact about the `Board`, measured against the board's own box, and Docket has the same defect with `⋮`. Fixing it in this app would have been a width check an app must not own. `#dynmodal` stays `min(920px, 92vw)` — part 2 of the superseded record still stands, and narrowing it again puts the trays back in one lane. Needs `@charcuterie/ui@^3.24.0`; against an older one the glyph comes back at every width. Gate: `e2e/shot-tray-move.ts` asserts the handle's visible text at BOTH board widths rather than once.)* |
| 2026-08-27 | [The tray move handle says "Move", it does not wear the drag glyph](2026-08-27-the-tray-move-handle-says-move-it-does-not-wear-the-drag-glyph.md) *(UI / affordance — **"I can't seem to drag 'n drop the names from Everyone Else anywhere else. There's no right-click or anything. How do I move these?"** Two causes, both introduced by the changes that put trays in front of him. (1) `PeopleTrays` passed `moveIcon="≡"`, and `≡` in this app means DRAG ME — the shelf grip in `QueuesView`, the card grip in `PlayView`, documented in those words in `useHomeDrags`. The tray handle is a MENU BUTTON first, which is why `@charcuterie/ui` ships the word "Move" as its default; wearing the drag glyph taught the one gesture that cannot work in a one-lane board. The word costs ~55px of row, which the library prices, and a control nobody can find costs more. (2) `#dynmodal` was still `min(520px, 94vw)`. `#setmodal` was widened to 920px on 2026-08-25 precisely because the board picks three-lanes vs one-lane from a CONTAINER query at `cq-lg`/768px, and `#dyn-people` landed the next day without the width moving with it — so the rules editor had ONE tray on screen and nowhere to drop, at any window width. Measured 520px/1 lane before, 920px/3 lanes after. Neither was caught because every screenshot was taken at the width that hides it. Gate: `e2e/shot-tray-move.ts` asserts the lane count, the modal width, the handle's visible text and no horizontal scroll at 390px.)* |
| 2026-08-26 | [The tile menu carries what the card cannot, and Remove is not one of those things](2026-08-26-the-tile-menu-carries-what-the-card-cannot.md) *(UX / interaction — the lanes shipped the day before and left the promote reachable ONLY as a drag across the divider, while the long-press menu on a queue tile held exactly one row, "Remove from this queue", six pixels from the ✕ that already does it: "touch holding on an item gives the option to remove from the queue, but I'd prefer options that aren't on the card such as 'make priority' or 'deprioritize'". So the menu's test for a row is not "is this useful" but "can the CARD already do it" — Remove fails it everywhere, because the ✕ rule is fleet-wide — and what it carries instead is **Play this next**, **Move to the Priority queue / Random pool**, the start point and Skip. "Play this next" is its own row because the lane row APPENDS: a promote must never displace what is already promoted, and putting an entry first is the other thing somebody wants. A promote lands at the END of its new lane rather than at its file position, because the pool is displayed alphabetically and shuffled at playback, so a file position means nothing to anyone looking at the screen. An entry with no rows at all — a movie member of a rules channel — opens NO menu rather than an empty box. The **selection bar** gains a Lane field for the same two moves, which closes the other half of the report: "You can't switch priority of a group or remove a group from the pool." One function decides the resulting order for the menu and the bar alike (`orderAfterLaneMove`), and a lane is deliberately NOT cleared by "Reset to defaults" — a lane is where the entry IS, not an override of how it plays.)* |
| 2026-08-26 | [A long press is the menu or the drag, never both](2026-08-26-a-long-press-is-the-menu-or-the-drag-never-both.md) *(bugfix / interaction — "It also messes up the UI when you close that menu." At 200 ms the hold called `beginDrag()` and the tile lifted out of its card; at ~500 ms the browser fired its own long-press `contextmenu` and the menu opened ON TOP of a tile that was mid-drag, with the card it came from left empty. One hold, two gestures, both half-done. The pick-up is DEFERRED to the first move past the threshold, so a moving finger is the drag and a still finger is the menu, and the `contextmenu` ends the press outright — which also stops the `pointerup` behind it settling as a tap and opening the entry sheet under the menu. The 200 ms arm stays, because it is what lets a vertical swipe scroll the grid. Two more defects in the same press, fixed here because they are the same press: `pointerdown` now ignores the non-primary button, so a RIGHT-CLICK on a poster opens the menu instead of the entry sheet it used to settle into; and the menu closes on a scroll that MOVES the page rather than on a zero-delta scroll event, which Chromium fires in the frame the menu opens and which made the long press look like it did nothing at all.)* |
| 2026-08-26 | [The promote window is a QUEUE setting, and it stays a rolling timer](2026-08-26-the-promote-window-is-a-queue-setting.md) *(product rule / playback semantics — "Trapped in a Dating Sim is in the QueuePilot priority queue, but it didn't play today when I scanned the card." Nothing was broken: the entry led at **00:06 that same morning** (Plex marked S2E7 watched at 00:28), and a promoted entry defaults to `lead: once` on a 24h ROLLING timer, so its next eligible moment was 00:06 the following day — 1 h 36 min after the 22:30 scan that was reported. The failure is structural rather than a one-off, because a sitting that crosses midnight always stamps its lead LATER on the clock than the next night's scan, so a flat 24h makes a promote skip a night every time. A day boundary at 04:00 local and a 2h debounce were both offered and both declined — "just lemme configure the reset time. 24h is good, but causes issues. 20h is probably best" — so the window becomes a QUEUE setting and the timer stays rolling. The engine already read the queue level (`leadWindowMs` is entry > set > 24h default) and `PATCH /api/sets/:id` already accepted the field; what was missing was a control, so the knob existed and no screen could reach it. `kevin_anime` is on `20h`. The entry sheet stops hardcoding "once a day" and names the queue's actual window, because a panel that says one thing over an engine doing another is the bug this record exists to stop. Blank = the 24h default, `never`/`0` = no window; the OFF spellings drop the key rather than storing `0`, which would read back AS the default. Gate: `e2e/priority-lane-test.ts` case 9, the first test to assert the middle rung of the precedence.)* |
| 2026-08-26 | [A queue's name is OPTIONAL, and the activity fills in](2026-08-26-a-queue-name-is-optional-and-the-activity-fills-in.md) *(product / UI / data model — **"the titles are auto-generate[d] unless they're specified like Manga & Webtoons"**. WP-5 built "every movies queue is called Movies" as a display rule that ALWAYS printed the activity, so the Picks page called the one queue the owner had deliberately named **Reading**, and all seven fixture shelves read "Movies & Shows", "…2", "…3", "…4", "Movies & Shows", "Movies & Shows" and "…2" — numbering that does not even distinguish them. The activity is a FALLBACK now: a typed name wins verbatim and is never numbered. `queueTitle(set, number)` is the one function that answers it, and it reads `has_explicit_label` — `label` cannot, because the registry makes it printable by falling back to the ID, which would put `movies_shows` on a card. The Name field is optional in both editors; a nameless create slugs its immutable WIRE id from the ACTIVITY (not from the provider — the body is a half-built set, and a wrong id is permanent in a way a wrong display is not); `PATCH {label: ""}` DELETES the line rather than storing a blank. Renaming the live queues is data, not code. Gate: `e2e/queue-name-test.ts`, 18 assertions.)* |
| 2026-08-26 | [The landing filters by PEOPLE, and the group chips go](2026-08-26-the-landing-filters-by-people-and-the-group-chips-go.md) *(UX / routing — **"this selection of 'All', 'Kevin', 'Older Kids', etc can go away. … The current groups are no longer needed!"** The chip row on `/admin` is a chip per PERSON and is MULTI-select; the provider chips stay as the second axis; both live in the query string (`?people=ada,grace&only=kavita`) so every chip is still a real `<a href>` that keeps the other filter. `/g/<id>` retires and redirects to `/admin`, the `play` route goes with it, and nothing is remembered in `localStorage` — a remembered people filter is a search field that comes back pre-typed. **A GROUP IS NOT GOING AWAY**: it still holds "at least one of the kids", it is still what a queue's Must-be-here tray points at, and it still resolves the one provider profile a queue signs in as. It stops being a shelf label and an address, which are the two jobs that pulled against the first. The filter itself already existed and was better — `membersMatchPeople` is now one function shared with What to Watch/Play, so the two screens cannot answer differently. Costs, stated: a group page is no longer bookmarkable and old links land on `/admin` untranslated; `group-create-test.ts`, `shot-groups.ts` and `shot-group-create.ts` are deleted; `lib/setLabel.ts` goes because both of its display rules existed only to undo the group page's own repetition.)* |
| 2026-08-26 | [A Rules queue carries people too](2026-08-26-a-rules-queue-carries-people-too.md) *(product / UI — reported against the live app: **"No way to add people to a Rules in QueuePilot. I can't add them here to Shorts nor Movies."** The Picks editor grew WP-5's three trays and the Rules editor did not, and the landing card passed `people={null}` on purpose, so no screen in the app could file a person on a `source: rotation` pool. The rows were there the whole time — migration day joins a queue to the groups whose `sets:` list claims it, and the live Younger Kids group claims `shorts` — so the fixture went from zero faces on a Rules card to four with no migration and no write. The prop comment's argument was that a filtered pool is bound to one provider ACCOUNT, which its meta line already names: true, and the conclusion does not follow. An account is which Plex profile the Shield signs in as; a tray is who the pool is FOR. `Shorts` signs in as Younger Kids AND is for one child. Nothing on the server changed — `PUT /api/sets/:id/people` never consulted a set's kind — and `e2e/queue-people-test.ts` now gates a rotation pool so it never learns to.)* |
| 2026-08-26 | [A Picks queue lives on the Picks screen, whichever lane it defaults to](2026-08-26-a-picks-queue-lives-on-the-picks-screen-whichever-lane-it-defaults-to.md) *(UX / information architecture — the LAST piece of the three-way Ordered Queue / Curated Pool / Filtered Pool split, three days after `kind` was merged to picks|rules. The kinds merged in the DATA; the two SCREEN lists did not. `queueIds` fed the Picks page and `channelSetIds` fed the Rules page, each one an `add_as` filter — so the household's ten `add_as: random` Picks queues were listed in a dropdown on a page headed **Rules**, between the two rules queues: *"This is the 'Picks' dropdown, but it includes 'Rules' entries."* Both selectors are gone; each screen lists its own `source`. That could not ship alone, and the owner said why in the same breath — *"the 'Picks' screen needs to be capable of showing priority vs pooled items"* — because the Picks page had never had to draw a random-lane queue and had no way to say what lane anything was in. So a shelf is now two RUNS in its one strip (Priority, an `li.lanesplit` divider, then the pool) with the split in words beside the count, and its tiles carry the same promote arrow the queue page's do. **One strip and not two**, unlike `/q/<id>`: `useHomeDrags` hit-tests one `.strip` per shelf and rebuilds file order from `strip.querySelectorAll("li.tile")`, so a second `<ul>` would PATCH half the queue's keys — which is also why a shelf drag reorders and does not promote. Found by running it: `/api/shelves` had to start carrying `placement`, or every entry falls into the set's default lane on first paint and the whole strip moves when `/api/queues` lands — the exact layout shift that endpoint exists to prevent. Also drops the Rules page's `＋ Picks queue` button (a page that cannot show you what you made must not offer to make it), renames its picker label off "Pool" (the page says Rules, and `pool` is already a lane), and widens Move-to to every other Picks queue. **And the people**: a queue's displayed name is its ACTIVITY now, so a list of them reads "Movies & Shows", "Movies & Shows 2", "Movies & Shows 3" — *"These options should show some badge or something of who's involved in them because it's not clear now that we're using auto-names."* `queuePeopleLabel` puts a chip on both Add-to menus and the Move-to picker, required members only, three names then `+n`. Gates: `api-v2-test` pins the skeleton's lane, `ui-test` pins that every Picks queue has a shelf and every non-empty shelf names its lane, `channels-test` pins that no Picks queue is in the rules picker.)* |
| 2026-08-26 | [QueuePilot starts with a mode landing, and the picker is What to Watch/Play](2026-08-26-queuepilot-starts-with-a-mode-landing.md) *(routing / naming / UI — `/` offers the two top-level choices **Admin** and **What to Watch/Play**, the existing management landing is at `/admin`, and the activity picker has the canonical path `/what-to-watch-play`. `/tonight` remains a compatibility redirect, but `Tonight` is no longer user-facing copy.)* |
| 2026-08-26 | [A provider read is cached, and the page revalidates after it paints](2026-08-26-a-provider-read-is-cached-and-the-page-revalidates-after-it-paints.md) *(performance / cache architecture — "Honestly, I'd like the whole page to load faster in general. Queues take FOREVER to load." A warm `/api/queues` made **566 provider calls** and took **5.1 s**, confirmed from inside the container so it is the app and not the proxy: 339 Plex `/library/metadata/<rk>`, one per queue ENTRY every single load, 24 section-collection listings, and 189 Kavita reads. The derived cache covered episode lists, collection children, title lookups and section listings and missed the hottest path in the app — turning one entry into one item. Three tables fix it (`item_meta`, `section_collections`, `kavita_item`), read-through with **no TTL**: a clock long enough to be useful is long enough to be wrong, one short enough to be safe brings the 566 calls back, and Kavita has no webhook to hang an invalidation on. The BROWSER revalidates instead — `store.load()` ends with a `?fresh=1` pass and a thin indeterminate `ProgressBar` on the header's bottom edge, which is the shape the owner asked for over a faster-but-silent one: "it would load from cache and fix itself quickly and show something to the user that it's fixing, so it doesn't just pop in with new content randomly." **5.1 s → ~0.1 s warm**, and a cached payload compared field by field against a fresh one over all 340 tiles differs in zero rows. An in-process memo in front of the Kavita rows is load-bearing and not a micro-optimisation: reading 188 whole-series DTOs back out of SQLite cost **17 s of cumulative blocking time**, more than the network calls it replaced, while the chapter walk they feed is 2 ms — `DatabaseSync` blocks the loop, so those reads never overlap. A MISS is cached (`{v: null}`) because a dead rating key otherwise costs a round trip for ever; a THROWN read is not, because "Plex was unreachable" stored as "does not exist" never repairs. ⚠️ **Skipping the leaves validator was tried and REVERTED the same hour** — `isFresh` cannot separate the browser's first paint from the ENGINE building a lineup, and there a stale answer is queued and played rather than merely displayed; `leaves-revalidate-test` caught it in minutes. The one-call-per-section version of that validator is real, is not built, and needs its own record. A 5.2 MB un-checkpointed WAL was suspected and DISPROVED — checkpointing changed nothing. Gate `provider-cache-test.ts` counts calls rather than timing anything.)* |
| 2026-08-26 | [The roster is edited in the app, not in a config file](2026-08-26-the-roster-is-edited-in-the-app-not-in-a-config-file.md) *(product rule / people — the roster arrived only through the owner-confirmed mapping file in `/config`, so changing a name meant an agent editing YAML on the appliance and restarting: **"There are now two Kevins here. Can we please fix this so I can edit the names somewhere?"** Nothing was broken — a PERSON called Kevin and a GROUP called Kevin are different objects and the trays correctly draw both — but no screen let him rename either. So `PeopleModal`, reached from ⚙ Edit people beside ⚙ Edit groups, with add / rename / remove for people and label-only editing for groups (a group's SETS stay in `GroupsModal`; two editors for one field is two places to change it). A person id is `slugify`'d once and never moves, because `queue_people` and `group_people` both store it and `PersonFace` hashes it into a hue — an id that moved on rename would empty a Must-be-here tray AND repaint the person. A blank name is refused rather than stored (`display_name` is NOT NULL with a `''` default, and a blank one paints a `?` face on four surfaces at once). A delete cascades all three tables and the app SAYS what it took, counted before the delete, because removing somebody changes which queues come up and that is invisible from the row. The mapping file keeps working and is never written — it owns only the rows it names, which is what stops the next restart importing over an edit made in the app. Gates: `e2e/roster-editor-test.ts`, 27 assertions on every PR.)* |
| 2026-08-26 | [A collection RE-ORDER is invisible to the cache, so the panel re-reads Plex](2026-08-26-a-collection-re-order-is-invisible-so-the-panel-re-reads.md) *(bugfix / cache invalidation — "I re-ordered these so the anti-DNR one is first. I already added this to QueuePilot, but it doesn't appear to have updated the ordering of the collection." Measured live: Plex answered `/children` in the NEW order with `collectionSort: "2"`, and the `collection_children` row was four hours old, held the OLD order, and had twenty hours of TTL left. **No freshness test the cache owns can see a re-order**, and there are three: the `(updatedAt, childCount)` validator is dead code because `/children` answers with a container carrying `size` and nothing else, so the stored `updated_at` is always 0 and can never match a real one; the collection's OWN `updatedAt` read more than a year older than the change, because Plex does not move it for a re-order; and `childCount` is identical on both sides, since a re-order adds and removes nothing. That leaves a 24 h TTL, and a clock misses it too — the cached copy is not old, it is wrong, and it became wrong at a moment nothing reports. So the trigger is a PERSON opening "What plays": the one moment the answer is about to be acted on, for the price of one container read. Cached rows paint first and the re-read corrects them, which is the shape the owner asked for over a blocking live read — "it would load from cache and fix itself quickly and show something to the user that it's fixing, so it doesn't just pop in with new content randomly" — and the **Checking Plex… / Updated from Plex** chip is the second half of that sentence, not decoration. A changed order also bumps the cache generation and refetches the grid, because the tile names the next member BY POSITION. Ticks survive the swap: a row is keyed on its ratingKey, so the checkbox moves with its member. A shorter TTL and a manual Refresh button were both offered and both declined. `cache.dropCollectionChildren` is the missing twin of `dropLeaves` — nothing could bust this table at all. Gate `e2e/collection-reorder-test.ts` fails pre-fix on exactly the reported shape.)* |
| 2026-08-26 | [A collection entry plays in PLEX's order, and a local override is deferred](2026-08-26-a-collection-entry-plays-in-plexs-order-and-a-local-override-is-deferred.md) *(product boundary / deferred — "can we customize the ordering of a collection in QueuePilot? Not sure why, but it might be necessary in the future" → "Don't worry about custom ordering for now. Put it as low priority for the future." Plex is where a collection is arranged and the app reads that arrangement; a second order stored here means two answers to "what plays next" and a rule for which wins. The two controls a collection entry already has are a START FLOOR (begin at a member) and a SKIP LIST (never play these leaves) — neither is an order, and `collection_members: split` is a FILTERED POOL setting that does not reach an ordered queue. **Deferred, not rejected**, and cheap to add later: the entry mapping carries open-ended extras that rewrites preserve verbatim, so an `order:` list of ratingKeys needs no format change. Two things must be settled before it is built — where a member ADDED later goes (an override that silently drops new films is the trap), and how the override is CLEARED, or re-arranging in Plex quietly stops working. Do not build it as part of another task.)* |
| 2026-08-26 | [A tile carries a lane control, and the select mark actually paints](2026-08-26-a-tile-carries-a-lane-control-and-the-select-mark-paints.md) *(UI / bugfix — two reports in one message, both about the same stack of chrome. **"Checkbox icon isn't working"**: the click always worked — the tile got `.selected` and the bar said "1 selected" — but the PAINT never did. `.tile.selected .check` is 0-2-1 and is outranked by `.editable .tile .tilechrome .check` at 0-3-1, so both of its declarations were dead and the mark under them was a text `✓` in `color: transparent`. The circle looked identical checked and unchecked, in every density and both schemes, which is exactly how it was reported. Measured on the live app before the change: a tile carrying `class="tile selected"` computed the scrim background and a transparent colour. Fixed by stating every state of the circle at the SAME specificity as the rule that draws it — not with `!important`, which would leave the next person the same trap — and by making the mark an SVG, for the reason `.remove`'s × already gives in this file. The control is a `<button aria-pressed>` now rather than a `<span aria-hidden onClick>`: it holds state, so it announces state, and the app's only multi-select stops being invisible to a screen reader. **And a third control under it**: ↑ into the Priority queue, ↓ back out to the Random pool, pointing the way the tile moves on screen. The drag across the divider stays — this is the same write for a pointer that would rather press than drag, and the only way across on a touch device without a long press. A promote lands at the END of the Priority lane (`promotedOrder`), never at its head: a drag says where, a button cannot, and the lane plays in FILE order, so dropping an entry wherever its stored position falls can silently make it the next thing that plays. A demote writes no order at all, because the pool is shuffled. Offered on EVERY queue page — a random-order queue's Priority lane is precisely where a promote goes, and the first cut gated it on the queue's own default, which hid it on the queues that need it most. Gate: `e2e/tile-lane-test.ts`, 16 checks on every PR, and it asks the BROWSER what it painted because nothing else can — tsc does not read CSS, Biome sees a string, and axe passes an invisible glyph.)* |
| 2026-08-26 | [An entry lists what is inside it, and you tick what plays](2026-08-26-an-entry-lists-what-is-inside-it-and-you-tick-what-plays.md) *(UI / product rule — extends the 2026-08-22 skip record without touching its data model. Skipping shipped as ONE tile-menu action that drops the item the entry is ABOUT to play, which answers "not tonight's episode" and nothing else: "I don't see the skips/ignore feature in QueuePilot. I added 'Man with No Name' trilogy, and it won't let me skip the duplicate 'Good Bad Ugly' ones and select only the one I want." Two defects in one sentence — the control was reachable only by right-clicking a tile, and it could not address any leaf but the next-up one. The live collection holds five members, three of them the same film in three Plex EDITIONS, so "play this one, never those two" was two skips in a fixed order, each behind a re-resolve, and the Skipped panel then listed the same title twice. So an entry with items inside it opens a MEMBER LIST — members for a collection, episodes under season headings for a show, ticked plays, unticked is skipped, **Save writes once** (every other control in that sheet writes on change; this one must not, because each skip costs a Plex re-resolve and would reorder the rows under the pointer). The panel owns only the keys it SHOWS — `mergeSkipped` carries every other entry's keys through, because the list is per SET like a pool's `blocklist` and a naive replace would silently clear them. Made visible three ways: the tile menu, a **What plays** field on the entry sheet a poster tap opens, and an `N skipped` tag on the tile itself, counted by the new `Tile.skippedCount` (direct members only, and no extra Plex I/O — both reads are the cached ones the next-up lookup already made). A duplicate is told apart by its EDITION, now carried on `CollectionChild`, `ItemLabel` and `NextEp.memberEdition`, so the rows, the Skipped panel and the collection TILE each name the cut. A row with no leaf id carries no control rather than an inert box; Kavita's `listUnits` names its chapter ids, so a reading queue gets the list too. ⚠️ `cache.ts` SCHEMA_VERSION 3 → 4 — a cached payload's shape is a schema change, and without the wipe every pre-existing children row reads back with no edition, which looks exactly like Plex not having one.)* |
| 2026-08-26 | [The queue page is two lanes, and the drag across the divider is the promote](2026-08-26-the-queue-page-is-two-lanes-and-the-drag-is-the-promote.md) *(UX / interaction — the lanes shipped in the DATA the same day and the page did not change, so a promoted entry was a chip on a tile somewhere in one flat grid: "I can't reorder the priority items. I was hoping they'd show up at the top of the list, and I could organize them, then there'd be another section for that unordered ones, and I could drag 'n drop between the two." Every part of that was already true underneath — the engine takes the Priority lane in FILE order — and none of it was reachable. So: two stacked sections, drag within Priority to reorder, drag across the divider to promote or demote (`placement` first, then the order). Both lanes always draw, because a lane you cannot see is a lane you cannot drag into, and the empty one is a slim dashed STRIP rather than an `EmptyState` box — an empty `<ul>` is zero pixels tall, which leaves the first promote nothing to aim at. The pool is NOT hand-orderable ("lock the pool to a sort"): its order is shuffled at playback, so a gesture that saved one would be a lie the file then keeps. Stacked and not side by side because each lane holds POSTER tiles and wants the full container width to gain columns in. Charcuterie's `Board` was surveyed first and is the wrong component — `BoardCard` is a text row in three shapes, and a queue tile is a poster.)* |
| 2026-08-26 | [A Priority entry's lead window belongs to a PROMOTE, not to an ordered queue](2026-08-26-the-lead-window-belongs-to-a-promote-not-to-an-ordered-queue.md) *(playback semantics / defaults — settles the one table row the [lanes ADR](2026-08-23-kind-is-picks-or-rules.md) left dangerous. It says `lead` is sparse-defaults-to-`once`; implemented literally that breaks every Ordered Queue in the house, because an Ordered Queue is `add_as: priority` and so has EVERY entry in the Priority lane by inheritance, with nobody having promoted anything. Give each of them a 24h window and sitting two leads with entry 2 — "play this list in order" becomes "play a different entry each night", invisible on a movie queue and a real change on a show entry playing one episode a night. So the default follows HOW the entry got into the lane: inherited ⇒ `always` (what ordered has always meant), promoted ⇒ `once` (what the owner asked promote for — "guaranteed first tonight, then not again until tomorrow"). One place decides it, `kind.normalizeLead(raw, {isPromoted})`, and an explicit `lead:` outranks both.)* |
| 2026-08-26 | [A lead window is stamped when playback STARTS, not when the lineup is built](2026-08-26-the-lead-window-is-stamped-when-playback-starts.md) *(playback semantics / data — `nextQueue` REPORTS which entries led (`QueueResult.led`) and writes nothing; `session.startSession` stamps them after the handoff succeeds. A lineup is built by more callers than the one that plays it — the preview endpoint, a profile gate that never opens, a cancelled scan, a Plex error before `playMedia` — and stamping at resolve time spends a 24-hour promise on a sitting nobody watched, then fails silently and slowly, because the entry is simply not first tomorrow either. It also keeps the engine free of a database handle: the lead GATE is injected as `resolve.LeadGate`, bound in `providers/plex.ts`, so the parity corpus still replays with no SQLite near it, and a null gate means "nothing has ever led".)* |
| 2026-08-26 | [A scan logs the lineup it built, and playback logs what Plex did with it](2026-08-26-a-scan-logs-the-lineup-it-built.md) *(observability — the owner reported an ordered queue "kept picking a random movie" with the film at the top of the list, and the container log could not tell a wrong ORDER from Plex reordering the playQueue from something else being on screen: a scan said only what it had finished and what it could not resolve, and never named a single title. So `[lineup]` writes the lane split, the head, and the first ten titles in order on every curated scan, and `[play]` writes what was asked for and then READS THE PLAYQUEUE BACK — `createPlayQueue` posts a multi-key uri and reads back only an id, so a queue Plex reordered or partly dropped looked identical to one it took verbatim. Unconditional, because a scan is a button press rather than a timer, and because a `DEBUG` flag is no use for a report that arrives the next morning about a container that has since restarted.)* |
| 2026-08-25 | [The board-game shelf is `/board-game-collection`, not `/collection`](2026-08-25-the-board-game-shelf-is-board-game-collection.md) *(routing / naming — **narrows the two-routes item in the same day's [play-logging record](2026-08-25-a-play-cannot-be-logged-without-answering-who-played.md)**, which shipped the shelf at `/collection`. "`queuepilot.octen.dev/collection` should be `queuepilot.octen.dev/board-game-collection` right? There may be others like Steam in the future." A generic path claimed by one specific shelf is a name the next feature cannot use — and the word is already taken in the other direction, since `type: "collection"` is a row of FILMS in `plex.ts`, `tiles.ts`, `sets.ts` and thirty other places. The internal view moved with the path (`boardGameCollection`) for exactly that reason: renaming the URL and leaving `route.view === "collection"` one line from `item.type === "collection"` would have kept the trap that made the change risky. `/collection` REDIRECTS rather than 404ing — it was live for a few hours, so a link to it may exist — and it redirects with `replace: true`, because a pushed redirect makes Back land on the old path, redirect forward again, and read as a dead button. It also still PARSES to the shelf, so the frame before the rewrite paints the right page instead of flashing the landing. The SPA fallback needed nothing, and that was verified rather than assumed. Gates: `route.test.ts` over `canonicalPath` and both paths, `routing-test.ts` over the cold GET, the deep link, a RELOAD, the rewrite and Back out of it.)* |
| 2026-08-25 | [Plex tile links use the server URL, not app.plex.tv](2026-08-25-plex-tile-links-use-the-server-url-not-app-plex-tv.md) *(correction / config — **supersedes the `app.plex.tv` host choice in `2026-08-22-a-tile-links-to-its-item-in-plex-or-kavita`**; title-as-link, show-not-episode, and server-built URL still stand. "the Plex links on QueuePilot go to app.plex.tv instead of plex.octen.dev." Built from `PLEX_API_SERVER_URL` as `{PLEX_URL}/web/index.html#!/server/{machineId}/details?key=…` — one source of truth with the API host, public-repo placeholder kept, no new env.)* |
| 2026-08-25 | [✓ stacks under ✕; Edit is a pencil pill by the labels](2026-08-25-checkmark-under-x-edit-by-the-labels.md) *(UI / tile chrome — **supersedes** the same-day pencil-in-chrome record.** "I don't like those 3 icons next to each other… put the checkmark under the X and move the edit down by the labels… so I don't accidentally click the X." `.tilechrome` is ✕ over ✓ at the trailing edge; Edit is an outline `BadgeButton` pencil pill in the badge row again. Away from ✕ on purpose.)* |
| 2026-08-25 | [Edit is a pencil icon in the tile chrome, not a text chip in the badge row](2026-08-25-edit-is-a-pencil-icon-in-the-tile-chrome.md) *(UI / tile chrome — **superseded same day** by the checkmark-under-x record. Kept the pencil glyph and `.editbtn` handle; moved the placement.)* |
| 2026-08-25 | [A queue nobody is filed on is never filtered out](2026-08-25-a-queue-nobody-is-filed-on-is-never-filtered-out.md) *(bugfix / product rule — the Tonight surface's **Queues** half never applied the people filter. WP-5 wrote and tested `queueMatchesSelection()`, WP-7 wired it into the DRAW, and nothing wired it into the form, so ticking two people narrowed Pick and left the list beside it offering queues those people are not on. Two empties are not the same empty and both are exceptions to the rule as stated: **nobody ticked is no filter at all** (a filter with nothing in it matches everything, and the strict reading hides every queue that names anybody), and **a queue NOBODY is filed on is never filtered out** — a queue no group claimed comes up empty by design, several live ones are in that state, and hiding them makes them unreachable. The second was found by the new parity gate on its first run, not by reading: the SERVER hid a peopleless queue as soon as anybody was ticked, so Pick could not draw one either, and nothing had ever asserted it either way. Both branches belong in the filter and not at either caller, because both halves read it. A **group stays ONE member carrying its own count** — flattening "at least one of them" into its person ids turns it into "all of them", which is the rule inverted and would mean the queue never comes up — so `TonightQueue` carries resolved members rather than two flat id lists. The filter is now written twice, once per workspace, the same way the activity → backend map is, and `e2e/tonight-routing-test.ts` §5b compares the two over one fixture: ten selections across two tiles, the server through a real draw and the browser through `queuesForTonight()`. The empty state says WHICH empty it is — nobody ticked and no matches means the activity has no queue at all, and "untick somebody" would send the host hunting for a tick that is not there.)* |
| 2026-08-25 | [The collection absorb is a ONE-WAY DOOR, and the source file is retired](2026-08-25-the-collection-absorb-is-a-one-way-door-and-the-source-file-is-retired.md) *(architecture / absorb / data safety — WP-4d, the package that closes the two-books-of-record problem. Once `board_games` holds rows the absorb never runs again: `store_meta('boardgames','retired_at')` latches at the end of the boot hook, `importBoardGames()` refuses, and `board-game-picker-import.sqlite` is renamed to `.retired-<timestamp>`. **`force` does not open it** — it was safe while the tables were a read-only copy of another app's data, and the person most likely to reach for it is somebody debugging a collection that looks wrong, which is exactly when losing the writes is worst. The latch is what makes the retirement TRUE and the rename is what makes it VISIBLE: a latch survives a snapshot restore and a `BOARD_GAME_IMPORT_PATH` pointed elsewhere, neither of which a rename does, and a rename that fails is logged rather than thrown because a read-only `/config` is not a reason to leave the door open. Deleting the file instead would have been worse than doing nothing — absent is a DIFFERENT fingerprint, so the absorb would want to run again over an empty source with only the assertions between that and twelve emptied tables. **The grouping SEED is not retired with it**, and treating the two inputs as one thing was the easy mistake: the source file is a second book of record carrying rows that REPLACE ours, while every seed insert is `ON CONFLICT DO NOTHING` and can only ADD — retiring it too would have left no way at all to add a grouping rule, because the screen [the rules are rows](2026-08-23-the-collections-grouping-rules-are-rows-not-source.md) promises does not exist yet. Retirement fires on ROWS EXISTING rather than on an absorb happening, because the live system absorbed days ago and its next start absorbs nothing; a rolled-back absorb deliberately does not latch. This is what let `logPlay()` come home in the same change — `boardGamesRepositoryClient` now builds no HTTP client at all, and it records `personIds: []` stated rather than defaulted. ⚠️ One correction to the record: WP-8 (#210) was already writing plays and known-how, so the window had been open one package longer than `AGENTS.md` assumed; nothing was lost, because the staged file was never re-staged. Two guards proved BY MUTATION — removing the one in `importBoardGames()` fails the `force` test, removing both fails `🐞 A SYNC'S ROWS SURVIVE A RESTART`; removing only the boot-hook branch fails nothing, which is defence in depth. `board-game-transport-parity` §5 was INVERTED, not weakened. Retiring the source FILE is not retiring the APP — that is WP-10, behind an owner gate.)* |
| 2026-08-25 | [A preset card is an ADDRESS, and a card that names nobody is refused](2026-08-25-a-preset-card-is-an-address-and-a-card-that-names-nobody-is-refused.md) *(feature / product boundary / routing — WP-9. The absorb brief's NFC table has four rows and three already worked: a kids rotation card and a curated per-audience card are `{"set": "<id>"}` on `queuepilot/cmd/session/start`, and a solo reading card is `/go/<setId>`. The fourth — a **Pick preset**, people and filters baked into the card — had **no address at all**, so the only thing a card could carry was `/tonight`, which is the form the preset exists to skip. WP-8 had built the destination; nothing could reach it. `/tonight/go?activity=…&people=…&guests=…&<filter>=…` is that address, it runs the SAME draw the Go button runs (`lib/tonightDraw.ts`, extracted from inside the button — exactly where a second caller cannot reach it, and a copy would be two places for "which engine does this activity use" to drift from `routeFor`), and it replaces itself with `/result`. `people` carries **person ids and never display names**, the same rule `sets.id` has always had, applied to the other half of the address. Every exit uses `replace: true` because a card is a one-shot instruction and leaving its address in the history makes Back re-draw. **A card that names NOBODY is REFUSED**, and that is the table's fourth row — *"bare board games needing live who's-here: use the app"* — made machine-enforced rather than written down: a card is a fixed string on plastic and cannot see who walked in, and the tempting "fix" of reading an empty roster as everybody produces a confident pick for a table whose size nobody stated, because a shelf pick is chosen BY table size. Guests count, so an anonymous seat still satisfies it. **Nothing invents a value**: an unknown filter id is dropped and an unknown value falls back to that filter's own default, because a card is written once and read for years and `light=onn` must never become a third state nothing handles. Surprise Me cannot be baked in either — it narrows on a second screen and the narrowings are not settled. A refusal lands on the form with everything the card DID say applied, and its reason is rendered in the view (`#tonight-preset-note`) rather than toasted: the first attempt used `setStatus` and the sentence was gone before anybody read it, because the form under it is mid-load and publishes its own status. Gates: 21 browser assertions in `e2e/tonight-preset-test.ts` plus 20 unit tests over the grammar and the draw.)* |
| 2026-08-25 | [The wire ids are a CONTRACT, and the gate drives the broker](2026-08-25-the-wire-ids-are-a-contract-and-the-gate-drives-the-broker.md) *(testing / privacy / operations — WP-9. A physical NFC card carries a set id, and a card whose id stopped resolving is SILENT: somebody taps it, the theater does not start, and the only evidence is a container log line nobody reads. Eight work packages have rebuilt the path between the broker and the row since 2026-08-23 and nothing gated it end to end. `e2e/nfc-wire-contract-test.ts` boots the real server against a real aedes broker and publishes the exact payload `script.control_plex` puts on the wire. It pins twenty set ids and sixteen queue ids BY NAME in both directions — a count of twenty passes even if all twenty were replaced — the four command topics read off the **broker's own subscribe event** rather than off `env.ts`, the MQTT discovery config the top-up automation's sensor is built from, twelve card payloads, that an unknown id IS still refused by name, that `set: "auto"` still reaches the profile-driven router, and that an empty `{}` top-up tick is answered. **The fixture does not name the live ids**, and that is the deliberate half: five of the twenty carry household first names and the 2026-08-17 history rewrite exists because they were once here. It mirrors the live registry's SHAPE — twenty ids, sixteen queues, the same profile gates, an underscore and a trailing digit and a three-word id — under the repo's placeholder cast, so the CONTRACT is gated publicly and the live twenty are verified privately.)* |
| 2026-08-25 | [Pick draws a QUEUE, not an item — and the activity → backend map is written twice](2026-08-25-pick-draws-a-queue-not-an-item.md) *(architecture / product — WP-7 connects the four activities that said "Pick is not connected": Movies, Shows, Reading and Video Games. Pick draws one **queue** for the activity and the people at the table, and the queue's own engine chooses the ITEM when it starts — the only thing that already knows what is left to watch, read or play. A draw that re-derived it would be a second rule that can drift, which is the defect `queuesRoutes.tagFinishedMovies` already warns about by name in its own header. What the card DOES carry is what would come up next, whenever that costs nothing extra: the real head of `pullLineup()` for a pull queue, the first unfinished entry off our own store for a curated Plex queue, and a NAMED reason otherwise — a null up-next never leaves a blank space. Board Games keeps its own door because a board game is on a shelf and not in a queue, and **Surprise Me is refused by name** because it narrows before it picks and the narrowings are not settled. The three filters the plan's table names were MEASURED rather than assumed and are reported on the card as not applied: `GET /api/pending` throws `duration` away and no export lists a movie library, there is **no age→rating logic anywhere in the server** (`birthYear` is read by nothing), and **there is no video-game known-how table** — WP-8 built the wording split and left the rows waiting, and a play count may never become a claim. One session talks to ONE backend: the draw binds a backend first and then draws inside it, `boundBackend` keeps a reroll there, and a mixed queue is never a candidate. WP-6's `activityForSet()` is DELETED — nothing derives an activity from a `provider_kind` any more — and its one surviving line is the residue that `watching` is one activity and two tiles. The map is written twice, once per workspace, because neither can import the other; `e2e/tonight-routing-test.ts` compares the two field by field and is the only thing that can see a drift.)* |
| 2026-08-25 | [A play cannot be logged without answering who played](2026-08-25-a-play-cannot-be-logged-without-answering-who-played.md) *(bugfix / data model / UI — WP-8, and it starts from a defect in the LIVE data: the absorbed app had **3 plays and 0 participant rows**, and that table had never been written to, for any play, ever. The write was never missing — its `logPlay` inserted participants correctly and every screen called it with `[]`, one of them with a comment explaining why and the picker's own result card whenever the form was in by-count rather than by-people mode, which is the mode somebody uses standing at a table. So `logBoardGamePlay({gameId, personIds})` takes the people as a REQUIRED argument, `POST /api/board-games/plays` refuses a body without them and checks every id against the roster, and the button says what it is about to record — `Log it · 2 people`, or `Log it · nobody named`. An empty list stays a legal answer; what is gone is a play that quietly means nobody while looking like it means everybody. Known-how is **marked by default on finish, with Change and Undo**, not a yes/skip prompt — and the default is a PROPOSAL a person confirms in front of the undo, never an inference: the refresh is an `UPDATE` guarded on `confirmed_at <` with no `INSERT` on that path, the only door that creates a claim is one person per `POST /board-games/:id/known`, and **Undo takes back exactly the claims that finish created** rather than a year-old fact somebody typed by hand. Wording is per activity — "Knows the rules" / "Knows how to play". Two routes: `/collection` is the shelf as a card GRID with "we played this" on every title (asked for directly, and it routes around a lost pick, which is why it came first), and `/result` is ONE card with a reroll and a shortlist of three behind a control — drawn WITH the first card, because a second request would re-draw and change the card under the finger that asked to see more. **`/result/<gameId>` is a queue arrival and has no reroll**: the queue chose. The pick, the table and reroll's memory are written to `localStorage` and expire after twelve hours — not the URL, because a pick is a result and not an address, and Tuesday's card shown as tonight's is worse than an empty screen. ⚠️ The three historical plays are NOT back-filled and never will be; the migration test pins the empty table and the new gate re-pins it. Gates: `store/db/boardgamePlays.test.ts` (14 cases) and `e2e/board-game-play-test.ts`, which drives the control in a browser and then reads the table with plain SQL.)* |
| 2026-08-25 | [The choice-tile exception is closed — Charcuterie owns the box](2026-08-25-the-choice-tile-exception-is-closed-charcuterie-owns-the-box.md) *(UI / component adoption — WP-6 painted a choice tile here, in a `.actgrid > [role="radio"]` rule, under the 08-21 record's **"a shape the library cannot express yet"** exception, and wrote in the CSS that the rule would be DELETED rather than adjusted once `@charcuterie/ui` had the shape. It does: `RadioGroup itemShape="tile"` in `ui@3.16.0`, built there after a fleet survey found the same hand-painted tile in four more apps — bambuddy six times, spoolbuddy, points-market and mux-magic once each — and **not one of the ten carried `aria-checked`**. The whole block goes, and with it `.actgrid`, `.queuegrid`, `.actlbl`, `.actname` and `.acthint`; nothing in `app.css` paints a Charcuterie control any more. The activity tiles, the Surprise Me scopes and the queue chooser are all the one component now, differing only by `minTileInlineSize` — 200px for two-word activities, 260px for whole queue titles. Two things that will read as regressions and are not: the hint is **bigger** (13.94px → 15.94px), because `0.82rem` was exactly the sub-13px magic number the 2026-08-10 type-ramp rebuild exists to remove, and do not put it back with an app override; and `tonight-test.ts` reads the tile's name **structurally** through one `TILE_NAME` constant instead of the deleted `.actname`, with every assertion — six names, the settled order, Surprise Me last, no provider brand — unchanged. The implied single-match queue is deliberately NOT a tile: one match is shown rather than asked about, so it is not a control and keeps its own `.qcard`.)* |
| 2026-08-25 | [A group PLAYS AS its own account, not its roster's union](2026-08-25-a-group-plays-as-its-own-account-not-its-rosters-union.md) *(architecture / data model — WP-5 lets a QUEUE be keyed on a whole group, because "Older Kids" is one card carrying its own **at least one of them** rule and flattening it to its people turns "either of them" into "both". That puts new load on something that had been true by accident, so a group's provider accounts now answer **two** questions through two functions that must never be merged. MEMBERSHIP (`accountsForGroup`) stays a generous UNION — dropping either half loses sets out of a group with no error. IDENTITY (`groupPlayProfile`) is SINGULAR: the group's **own** `accounts[kind]` when it names exactly one, otherwise the roster union only when it is unanimous, otherwise `ambiguous` — and `PUT /api/sets/:id/people` **refuses** it with every candidate named rather than choosing. The union cannot answer identity and this repo's own comment says why: the mapping is deliberately N-to-M, "Carol is two Plex accounts (Older Kids, Younger Kids) and one Kavita user", so a three-person roster offers three answers and "at least one of them" means the app cannot know in advance which subset arrived. A union-based identity would sign into whichever sorted first — plausible, silent, and it reads a kid's queue out of the owner's watched history. The group's own `accounts:` is right precisely because it does NOT vary with who turned up, which is what `requires_profile` has always meant. Refusing beats resolving: an ambiguous group is a one-line household question, and writing the member anyway moves the failure from a save button, where somebody is looking, to a Shield at 8pm, where nobody is. Gated by `e2e/queue-people-test.ts` — a 400 naming both candidates, and nothing written.)* |
| 2026-08-25 | [The board-game provider reads ROWS and still POSTS a play over HTTP](2026-08-25-the-board-game-provider-reads-rows-and-still-posts-a-play-over-http.md) *(architecture / absorb / transport — WP-4e. `providers/board-game-picker-client.ts` answers `games` / `game` / `plays` / `categories` out of `store/db/boardgames.ts` and reads covers off the staged `board-game-images/` directory; **`providers/board-game-picker.ts` did not change by one line**, which is the proof the seam was drawn in the right place — the provider was written against a client interface, not against a URL. The old type name `BoardGamesHttpClient` survives as an alias for exactly that reason. `logPlay()` is the ONE call left on the wire, and deliberately: two books of record are open, the absorb REPLACES all twelve `board_game_*` tables on a fingerprint change, and a play written here is erased by the next start — WP-4d lands the writers and retires the source file in the same change. A read of stale data is a stale answer; a write into a table the absorb will replace is a lost answer with no error. Cost stated rather than hidden: a play logged through `POST /api/providers/:id/progress/:itemId` lands in the sibling app and is invisible here until the collection is staged and absorbed again, and no UI in this app calls that route. The wire SHAPE was kept rather than improved — `game()` returns the object `GET /api/games/:id` returned and `plays()` the same three keys that endpoint hand-wrote — so the privacy rule did not retire with the transport, it MOVED into the client, and the two transports stay comparable object for object. Nothing is cached: a full read of the live 147-title shelf is 5.4 ms measured, and WP-4d's writers would make a cache with no invalidation hook into a restart. `BOARD_GAME_TRANSPORT=http` is the rollback, the way `STORE_BACKEND=yaml` is the store's, and keeping the HTTP client runnable is what lets `e2e/board-game-transport-parity-test.ts` compare the two on every CI run rather than once — one invented shelf expressed twice, the HTTP side transcribed and never imported, every answer compared value for value including the cover bytes, nine lineup scenarios with `queued_at` checked either side of the one recent play, and proved by mutation: dropping the `since` bound fails 11 cases, stripping the game DTO fails 8, a wrong cover directory fails 3. ⚠️ `board_game_play_people` holds 0 rows against 3 plays on the live system and the gate PINS that zero — a logged play records nobody, WP-8 owns the fix, and this package left the defect exactly as visible as it found it.)* |
| 2026-08-23 | [The collection's grouping rules are rows in the book of record, not a table in the source](2026-08-23-the-collections-grouping-rules-are-rows-not-source.md) *(architecture / privacy / absorb — **blocks WP-4b**: the rules that collapse a shelf of physical boxes into a list of playable titles are DATA, so they are seeded into `/config/queuepilot.sqlite` and edited from a screen, and this repo keeps the grouping ALGORITHM plus the SHAPE of the two rule tables and none of their contents. The numbers decide it. The source file is 718 lines, of which the two rule tables are 176 — 18 merge rules and 4 kept-separate answers, naming 30 collection titles between them, 8 of them beside dated quotes of a household ruling. The runtime table already holds **150** owner-made rules of the same kind over 150 distinct box labels, and the importer applies those rows FIRST. Replaying all 18 merge rules against all 150 rows: **103** rows no source rule matches at all, **19** where a rule matches and **disagrees**, 28 where it agrees — so the two halves already disagree about 19 boxes, silently, with the rows winning. Across 562 boxes the split is 152 decided by rows, 116 by the source table, 294 by the generic algorithm with no rule at all. **One of the 18 has already gone stale**: it names a title id that no longer exists, 35 owner rows reformed that family under another id, and nothing failed — because a rule that matches nothing does not fire and a title that is never offered is never missed. They only look like code: 17 of 18 match one literal prefix, the 18th two, exactly one carries a literal exception, and the reader is a six-line `Array.find` — so the column is `prefix TEXT` and the store never compiles a pattern. A row carries a `source` (`migration` vs `owner`) or a re-run doubles a merge; a row does NOT carry a `reason`, because each ruling already has a dated record in the private workspace repo. Rejected: an App-Configs data file (splits one fact across two stores with an invisible precedence, and `groups.yaml` is not the precedent — no screen writes it), a private sibling repo (not mounted in the container; a rule only an agent can edit is a regression against a screen he has), and a sanitized placeholder overlaid at runtime (two schemas for one table, and a committed table that lies). A fresh container is EMPTY, not broken — the algorithm still decides 294 of 562 and reports rather than guesses the rest. Costs stated: no screen writes a title-prefix rule yet, four titles in the algorithm's own comments need re-authoring, and the 19-case suite names 51 real titles and must be rewritten before CI can gate the port at all.)* |
| 2026-08-23 | [A group is a saved set of people, and the identity match is manual](2026-08-23-a-group-is-a-saved-set-of-people-and-the-identity-match-is-manual.md) *(data model — `people` + `person_accounts` + `group_people` join the book of record, and a group stops being a second kind of thing: it keeps its wire id, its `/g/<id>` URL, its label and its `sets:` list, and gains a roster. The accounts move onto the PERSON and the group's own `accounts:` is **unioned**, not retired — a group may stand for a profile no household member holds, and dropping either half loses sets out of a group silently. `group_people` carries **no foreign key on `group_id`**, because `writeDoc()` and the YAML importer both replace the whole `groups` table with DELETE + INSERT and a cascade would empty every roster on the next rename; the orphan is reported instead. And the one that outranks the rest: **the identity match is MANUAL**. A tool proposes with its evidence, the owner confirms once, and the import writes nothing without an explicit `confirmed: true` — a confirmed file that fails validation writes nothing either. The play log is 2 rows, so a wrong merge costs nothing there; `player_known_games` is what it would corrupt, and a known-how claim on the wrong human is on no screen.)* |
| 2026-08-23 | [`promote.sqlite` folds into the book of record](2026-08-23-promote-sqlite-folds-into-the-book-of-record.md) *(storage — the `lead_cooldown` table moves into `/config/queuepilot.sqlite` and `PROMOTE_PATH` is deleted; `promote.ts` keeps its queries and stops owning a file, a schema and a version check. **Supersedes only the choice of FILE** in [promote-cooldowns-live-in-promote-sqlite](2026-08-23-promote-cooldowns-live-in-promote-sqlite.md) — everything that record says about a lead timestamp being a user decision that must not sit in a wipeable cache is what this one carries forward, and `cache.sqlite` is untouched. Four reasons. It had **never been created on disk** — the code shipped the morning WP-2 started and the lead:once path had not fired, so the fold is zero rows migrated and it will never be that cheap again. A cooldown row is keyed on `(sets.id, an entry key inside queues)`, which is a foreign key with a file boundary in the way; the FK and its cascade land in the change that removes the YAML reader, because through the bridge release `STORE_BACKEND=yaml` leaves `sets` empty and every insert would be refused. The old file's schema bump **DROPped both tables** and logged loudly about it — the cache's contract, applied to data its own header says the app may not lose — where the book of record migrates and refuses a file written by a newer build. And "two files, never one" was an argument for keeping the DELETABLE file deletable; three durable files was never that rule, just one more thing to remember to back up.)* |
| 2026-08-23 | [The data layer is `node:sqlite`, not `better-sqlite3`](2026-08-23-the-data-layer-is-node-sqlite-not-better-sqlite3.md) *(architecture / dependencies — every SQLite database this app opens goes through `node:sqlite`'s `DatabaseSync`, the book of record included, so Board Game Picker's data layer is PORTED rather than bringing `better-sqlite3` into the container. The reason is already in `cache.ts`'s header and is about the deploy, not the benchmark: a native build inside `npm install --omit=dev` on a slim image turns a missed prebuild into a deploy-time compiler hunt, and the first sign of it is an app that built yesterday failing after a Node or base-image bump. The absorb makes that worse, not better — a container that will not build is now a household that cannot start anything. The surface was measured so nobody re-litigates it against BGP's code: 104 `.prepare()` call sites (same names, same shape), 11 `.transaction(fn)` (no equivalent — a ~10-line `withTransaction()` helper), 3 `.pragma()`, and **zero** `.function()` / `.aggregate()` / `.backup()` / `loadExtension`, which is the load-bearing row. The one place it is not a rename is the pragma that READS: `exec()` returns nothing, so `PRAGMA table_info(…)` in `addMissingColumns()` becomes `prepare().all()` — verified against `node:sqlite`, not assumed. `promote.ts` is the precedent that this driver already backs data the app may not lose. The compat shim is `prepare` / `exec` / `withTransaction` to spare `repository.ts` a line-by-line rewrite; it is **not** a driver abstraction, nothing may branch on what is underneath, and BGP's own `db/*.test.ts` move over UNCHANGED as the gate — a port that passes new tests and not the originals has proved nothing.)* |
| 2026-08-23 | [SQLite is the book of record, and `cache.sqlite` stays derived](2026-08-23-sqlite-is-the-book-of-record-and-cache-sqlite-stays-derived.md) *(architecture / storage — durable state (sets, queues, entries, groups, people, the absorbed board-game collection, plays, known-how) moves into `/config/queuepilot.sqlite`, which is backed up and migrated rather than wiped. **Supersedes only the "YAML stays the long-term store" clause of [2026-08-03](2026-08-03-sqlite-is-a-derived-plex-cache-not-the-store.md)** — everything that record says about `cache.sqlite` being derived, DROPped on a schema mismatch and safe to `rm` is restated here unchanged. The book of record and the derived cache are two files, never one, and the reason is one sentence: merging them makes the deletable file undeletable — a schema bump would delete the household's queues without asking, and the documented `rm` recovery step would delete them by hand, so the only way out is to stop wiping the file, at which point there is no cache left, only a database with a stale-Plex problem. Not a performance argument: the 2026-08-03 measurement stands and YAML parsing was never the slow thing. What buys it is relations (the collection has foreign keys), append-heavy history (plays and known-how), and two writers (HTTP and MQTT) needing atomicity a whole-file YAML rewrite does not have. The four costs 2026-08-03 demanded an answer to are answered: git-diffability was never true of files that live in `/config`; SMB hand-editing is spent deliberately, which makes a missing admin screen a bug in the migration rather than a reason to keep a YAML writer; comment round-tripping is the one real loss (79 comment lines, per-row ones migrate to a `note`, file-level ones do not); and the title-string format was already spent by [2026-08-21](2026-08-21-a-queue-entry-is-an-object-and-carries-its-rating-key.md). YAML survives as a one-shot, copy-aside, idempotent import bridge and nothing else. `promote.sqlite` stays durable; whether it folds in is WP-2's call and is not settled here.)* |
| 2026-08-23 | [Ending the activity is an MQTT command, behind a confirmation](2026-08-23-ending-the-activity-is-an-mqtt-command-behind-a-confirmation.md) *(feature / boundary / UI — the Now-playing bar gains `⏻ End the activity and power off`, behind a confirmation modal. "There's no way to power-off the activity in the controls." The press STOPS Plex over HTTP first and THEN publishes `queuepilot/cmd/activity/off`, and that order is the contract: powering the room down first leaves a session playing to a receiver that is already off. It is the one control in the bar that is NOT HTTP — [transport control is HTTP](2026-08-22-transport-control-is-http-not-mqtt.md) because that is the app talking to Plex, while turning a room off is the app asking another service to touch things with power cables. A COMMAND topic and deliberately not `resp/finished` + `power_off`: HA gates that event on the set's opt-in, on `isComplete`, on a 45-second re-tap wait and on nothing having started since — four guards that exist because it fires on its own, and every one of them is wrong for a person pressing a button. A failed stop does not cancel the power-off. Confirmation because the other four controls are undone by pressing another one and this one is a dark room.)* |
| 2026-08-23 | [Lead cooldowns live in durable `promote.sqlite`, not `cache.sqlite`](2026-08-23-promote-cooldowns-live-in-promote-sqlite.md) *(storage — Priority-queue `lead: once` timestamps are a user decision, not regenerable Plex data, so they must not share the wipeable derived cache.)*
| 2026-08-23 | [`kind` is `picks` or `rules`; Ordered Queues merge into Picks](2026-08-23-kind-is-picks-or-rules.md) *(data model / naming / playback — Picks vs Rules; Priority queue / Random pool lanes. UI badges on the Play landing may land before the stored `kind` migration.)*
| 2026-08-23 | [A skipped item counts as DEALT WITH, so the entry can complete](2026-08-23-a-skipped-item-counts-as-dealt-with-so-the-entry-can-complete.md) *(correction — **reverses rule 4 of `2026-08-22-a-curated-queue-skips-items-…`**, one day old. "If you finish a show and the last episode is skipped, that will mark it complete, right? And if you have 3 movies in a collection, and you skip the second one, watching the first and third will mark it complete? That's what I'd expect." It does now: "nothing left to play" is the finished test and a SKIPPED item counts towards it exactly as a watched one does, so `emptiedBySkip` is gone. The rule is "nothing left", not "something was skipped" — an entry with E3/E4 still to play is not completed by skipping E2. The carve-out solved a problem that was already solved: `nextQueue` revives a `done` entry the moment it resolves to anything playable, and a `markDone` completion carries the `done_at` that revival keys on — so Restore → revive → flag cleared, with no new machinery. What it created instead was a state with no exit: an entry emptied only by skipping was never complete, never caught by "Remove all completed", and removable only by hand. ⚠️ The one genuinely one-way case is a queue with `remove_completed_after` (four live queues use `24h`), where the line is deleted once the window passes — which is what the TTL already means for every other completion there.)* |
| 2026-08-22 | [A curated queue SKIPS items, the way a filtered pool BLOCKS them](2026-08-22-a-curated-queue-skips-items-the-way-a-filtered-pool-blocks-them.md) *(feature / data format — a **queue** set gains `skipped:` in `sets.yaml`, the curated twin of a filtered pool's `blocklist`. "Add ignores/skips for random queues. We have excludes, but only for filtered queues and not selected." It addresses the LEAF — one episode of a show entry, one child of a `{collection: X}` entry — never the member: a movie ENTRY is its own leaf and Remove is its answer, so the tile offers Skip only where there is an item inside a member. Permanent until cleared from the queue's **Skipped** panel; one word everywhere (key, API field, menu row, heading). Rejected on a rotation channel, where `blocklist` is the same feature. The expensive rule is that a skip NEVER marks an entry done: "empty items" is the finished test, and finished is persisted and then TTL-swept, so retiring an entry whose last unwatched episode is skipped would make the skip one-way — hence `emptiedBySkip`, which withholds the write and not the report. Filtering runs before the batch cap, so skipping E2 on an `episodes: 2` entry queues E3 + E4. Plex and Kavita both honour it; the panel does not Plex-resolve a PULL set's keys, because the id spaces overlap and the lookup would sometimes name an unrelated film. Gates: `e2e/skipped-items-test.ts`, `e2e/shot-skipped-items.ts`.)* |
| 2026-08-22 | [Volumes read before loose chapters — and a sole-chapter volume is a volume](2026-08-22-volumes-read-before-loose-chapters.md) *(bug / playback order — **extends `2026-08-16-a-volume-is-not-a-chapter`.** "Volumes should be prioritized over chapters as they release later, but they're the _real_ read. Chapters are the brand spankin' new ones and 48.5 is much newer than Vol1." Two live failures, one root: (1) a MIXED series with Volume 1 unread and loose chapters 48.5/50/51 opened on 48.5 when the chapter batch was 3, because sort used `(volume?.minNumber ?? 0, chapter)` and every loose chapter sat at "volume 0"; (2) every tankobon of a series whose files parsed as `number: '1'` / "Chapter 1" (not the `-100000` sentinel) labelled itself Chapter 1, made worse by a loose-wins dedupe that stripped the volume off chapters Kavita also listed in `chapters[]`. Fixed: volumes sort before loose chapters; a volume with exactly one chapter IS the volume; the volume copy wins the dedupe; the bare continue-point launch shortcut is gone because it named a chapter without its volume. A webtoon with many chapters under volume 1 stays `unit: chapter`. Offline gate extended in `e2e/kavita-volumes-and-members-test.ts`.)* |
| 2026-08-22 | [The grid PREDICTS a revival, the same way it already predicts a completion](2026-08-22-the-grid-predicts-a-revival-the-way-it-predicts-a-completion.md) *(bugfix — `/api/queues` answers `isRevived`, the mirror of `isFinished`, and the Completed badge keys off both. Reported on the SAME collection `2026-08-15-a-done-entry-revives-when-there-is-something-to-play` was written about, the week its S2E7 aired: the tile greyed itself out and wore a Completed badge directly above its own yellow line reading "E7 · Episode 7". The engine was already right — `nextQueue` revives a `done` entry that resolves to anything playable — but `done` is only written by a scan, and an episode AIRING triggers none, so the flag outlived the truth and the grid reads the flag. The owner suspected the `2x as often` weight; it is read only by `weightedShuffle` and nothing in the done/revive path touches it. Shows and collections only, and a MOVIE is out on the failure mode rather than the feature: a next-up lookup that fails reports no `nextEp` and changes no badge, while a watched-history read that fails comes back as an EMPTY SET (`finished.watchedFor`) — indistinguishable from "nothing is watched", so predicting off it would un-badge every finished film in the app on one bad read. A hand-marked `done` (no `done_at`) keeps its badge, exactly as the resolver keeps skipping it. Nothing writes: the file is still the record of truth and the next scan is still what clears the flag.)* |
| 2026-08-22 | [Transport control is HTTP, not MQTT](2026-08-22-transport-control-is-http-not-mqtt.md) | The Now-playing bar's stop/pause/resume/next/seek go to `POST /api/control`, and **no `queuepilot/cmd/*` topic is added for them** — an agent reading the workspace's "services talk over MQTT" rule will try to move them, and this is why not. That rule governs service-to-service traffic across a process boundary; the browser is already talking to this server over the same origin, so publishing to a broker for the same process to subscribe to itself adds a hop and a failure mode. A press also needs an ANSWER — "stop failed: target client not found" belongs in a toast — while `cmd/session/*` gets away with fire-and-forget because its reply is a state the whole house cares about. Nothing in the house automates a pause, and the one house-facing event here already exists on the broker: `resp/finished`, which carries `power_off`. |
| 2026-08-22 | [Pending lists collections AS WELL AS their members](2026-08-22-pending-lists-collections-as-well-as-their-members.md) *(product / server — a Plex collection becomes a pending row of its own, beside the films inside it, and adding it writes a `{collection: "<name>"}` entry so the engine expands the ordered members. "Often, I wanna add the collection, not a single or set of movies to retain order." Both and not either: "add the franchise in order" and "add this one film" are both real, and a screen that decides which he meant is wrong half the time. A collection is not an arrival — it is a grouping that has existed for years — so what makes it news is that a child of it is pending, which is read off the items this pass already decided about rather than from a second question to Plex; a collection whose every member is covered is the case that must NOT be offered, and the same test excludes it. `addedAt` is the newest pending child's (Plex's own is the day someone made the collection), and the collection wins the tie it therefore always has with that child, so it sits directly above the film that put it there. One collections listing per library that has news, one children read per collection at `RESOLVE_CONCURRENCY`; the lister is a PARAMETER, so absent means no collections and every existing caller is unchanged. Gate: 11 new hermetic checks in `e2e/pending-test.ts`.)* |
| 2026-08-22 | [Pending can make the queue it is adding to](2026-08-22-pending-can-make-the-queue-it-is-adding-to.md) *(UI / behaviour — the Add-to menu ends with a separator and a "New queue…" row that opens the ordinary queue dialog, pre-ticked with the item's own library, and adds the item once the queue exists. "I also cannot add a queue from here either… It's not in the dropdown." In the menu rather than beside it, because "one that does not exist yet" is an answer to the question the menu is asking. The pre-ticked library is the fix and not a shortcut: a new queue draws from nothing, so without it the owner's own flow ends with a queue the film is not eligible for and nothing on screen saying why. `openSetModal` grew `presetLibraries` + `onCreated`; `addTo` takes an id and a label rather than a `RegistrySet`, because the queue exists on the server before the registry the view is rendering from has ever heard of it. `MenuProps["items"]` is used to name the entry union — Charcuterie declares `MenuEntry`/`MenuSeparator` but re-exports only `MenuItem` and `MenuProps`, an upstream gap worth closing there.)* |
| 2026-08-22 | [Pending picks the start episode BEFORE the add, and the add writes it](2026-08-22-pending-picks-the-start-episode-before-the-add.md) *(UI / behaviour — a pending SHOW carries "Start at…" (a clock in the poster view); the choice is held in the view and written by the add as a second call, `POST /items` for the entry then `PATCH /items/:key/start` on the key it answers. "I wanna add this show, but I'd like to specify the episode to start on. Doesn't seem to be possible from here." The submenu version was rejected by name — *"the 'right' solution but makes adding a bunch of these cumbersome"* — because two menu levels per add is the wrong cost on a screen whose job is getting through a list. Not a `start` field on the add: `PATCH …/start` is the ONE writer of a start point everywhere else, and a second door is a second place for them to disagree. Only on a real add (`added: false` means an existing entry with a start this screen has never seen), only on a show, and never persisted — a start chosen and not added is a choice abandoned. Reuses `StartModal` unchanged by pointing `EntryActions.save` at component state.)* |
| 2026-08-22 | [Pending has a poster view and a list view, and neither one clamps the words](2026-08-22-pending-has-a-poster-view-and-a-list-view.md) *(UI — the same Posters/List choice the queue grid has, persisted for the screen; icon controls in the poster view because a 158px column cannot hold three named buttons, named ones in the list view at a 400px minimum column (measured — at 330px they wrapped). Two views, not three: the grid's middle density exists for per-entry knobs a pending item does not have. The uniform-card layout drawn first was REJECTED — *"I don't like that because I'm losing content and we're creating gaps"* — so nothing is clamped and nothing is padded, and within a row the controls sit at their own heights. **The poster is the only fixed thing**, one 2:3 `object-fit: cover` box, because alignment was the goal and uniformity was only a way to buy it; `.pendingtile` drops `block-size: 100%`. Glyphs are inline SVG, not `＋`/`⏱`/`✕` characters, for the tofu reason the Watched chip already established. `key={density}` remounts the virtualized grid so its measured row heights do not carry across the switch.)* |
| 2026-08-22 | [An `unauthorized` adb record is DROPPED and re-handshaken, not retried](2026-08-22-an-unauthorized-adb-record-is-dropped-and-re-handshaken.md) *(bug fix / reliability — every profile-gated play died for hours behind 49 x `device unauthorized`, with a key the Shield still trusted. `adb connect` is idempotent against a stale `unauthorized`/`offline` record, so it reports "already connected" and never re-handshakes; the wedge then outlives every retry for the life of the container. `connect()` now reads the state WORD (`run()` mapped both to `null`, indistinguishable from a Shield that is off), disconnects, and connects once more)*
| 2026-08-22 | [A tile links to its item in Plex or Kavita, and the TITLE is the link](2026-08-22-a-tile-links-to-its-item-in-plex-or-kavita.md) *(UI / wire shape — every resolved tile carries a server-built `webUrl` and the title is an anchor to it. The **title**, not a chip: the tile already wears ✓ ✕ ▶ plus up to three chips, and the owner's objection to a new control was that there is nowhere to put it. The **show or the film**, never the next episode — the next-up line names the episode and is already the button that opens the start picker, so a link there would be two meanings on one control. Built **on the server**, per tile — and a PULL provider's tile carries a same-origin `/api/providers/<id>/open/<itemId>` that answers 302 at click time, because Kavita's base URL is credential-adjacent (its image endpoint takes the API key as a query parameter) and `e2e/kavita-covers-test.ts` gates on it reaching no response body; that gate caught the first draft. `null` renders as plain text, which is what an unresolved entry and an unread machine id both get. The `#!` address is Plex's shape, not a breach of the paths-not-hashes rule. **Host choice superseded 2026-08-25:** links now use `PLEX_API_SERVER_URL/web`, not `app.plex.tv`.)* |
| 2026-08-22 | [A tile names the runtime, on its own line](2026-08-22-a-tile-names-the-runtime-on-its-own-line.md) *(UI — how long the next thing runs, on a muted line of its own under the next-up line. Not appended to the next-up line, which already truncates on a long episode title — the owner's own screenshot shows "E21 · City Under Crackdown, Moist with…", so a runtime there is the first thing to vanish on the tiles that are hardest to read. Not a chip either: the badge row holds per-entry OVERRIDES, each a button into the editor, and a runtime is neither editable nor an override. Whole minutes, hours split past sixty; a batch multiplies the next episode's runtime and says **about**, because that leaf is the only runtime `nextEp` carries and a product presented as exact would be a number nothing measured. Display-only — `nextEp.duration` was already on the wire — and absent entirely on a tile with no runtime, so Kavita and board-game tiles keep their geometry.)* |
| 2026-08-21 | [Pending draws from an INCLUDE list of libraries](2026-08-21-pending-draws-from-an-include-list-of-libraries.md) *(product / reversal — **supersedes the "Other Videos unless a set draws from it" rule in `2026-08-17-pending-is-what-nothing-will-play`**. "Pending is for new additions not in a queue, not watched, from specific libraries (not the inverse). So instead of exclude, just have it be include." The page held **2,162** rows and the subtraction rules were all working; the number came from two other places. `seen_through` is still `0`, so "new" means the whole library history — left alone, because the owner never asked to lose the backlog. And the conditional hatch admitted a Personal Media library whenever any set drew from one, which the `demo` and `betterman_qc` queues do for sections 2 and 7 — so **1,097 rows, 51% of the page, were clips and test encodes**. That hatch read as conservative and was not: a queue that plays out of a scratch library says something about THAT QUEUE and nothing about whether a new file there is news. Include beats exclude on what happens to a library nobody has thought about — under exclude a new Plex library joins the screen and must be noticed and named to remove; under include it stays out until asked for, which on a screen whose whole job is subtraction is the correct default direction. "Don't hardcode them that way" is why `Demos` and `Movie Clips` appear nowhere in the code: the default is the PROPERTY *has this library a metadata agent*, and `pending.yaml`'s `libraries` overrides it in **both** directions — naming a clips library puts it back. `[]` and unset are kept apart and are different writes: `[]` is a deliberate blank page, unset falls back to the default and omits the key from the file rather than writing an explicit null, so "Back to default" and unticking the last box cannot collapse into one another. `video` is enforced even against an explicit choice, since nothing this app builds can queue a photo section. Live: 2,162 → **1,065**. Gate: `e2e/pending-test.ts`, 52 offline checks, 17 new.)* |
| 2026-08-21 | [A control is a Charcuterie component configured by PROPS, not an app class name](2026-08-21-a-component-configured-by-props-not-a-borrowed-class.md) *(UI / component adoption — **extends `2026-08-21-a-class-name-is-not-a-style` and is the stronger of the two.** "I'd prefer not to use borrowed classes rather than component library components which have all the Tailwind classes built in. No className props required. It's all props." That record says a borrowed class ships unstyled markup; this one says what to reach for instead, and it binds a class that DOES match as well as one that does not — `.playbtn`, `button.ghost` and `.badge` are all invisible to the auditor and are the bulk of what is left. A pressable control is a `Button`/`ButtonLink`/`IconButton` with `appearance`/`intent`/`size`; a pill is a `Badge`; a label over one control is a `Field` and over several a `FieldGroup`; **a `className` on a Charcuterie component is a smell**, because `app.css` is unlayered and Tailwind's utilities are in `@layer utilities`, so ANY app rule outranks the component it lands on. All five open findings fixed by adopting a component, never by un-scoping a rule: `#newdyn`/`#newcurated` → `Button outline/accent` (with `#chresample`/`#chconfigure` for the same row, or four Charcuterie buttons would stand beside two app-CSS ones at a different height); `#dyn-lineup` → `FieldGroup label="Playback"`, which gives it the flex column its `flags` class never had; `#dyn-on-complete` → `Field`; the second `fieldselect` was decoration with nothing to restore; and the WHOLE selection bar → eight `Button`s with the five `#selbar button…` skin rules deleted. That bar had a second defect the finding never mentioned: **a `Picker` trigger is a `<button>`**, so `#selbar button` was painting both pickers solid indigo against this repo's own outline rule, and the count picker's `Default` chip with them. Apply is now the one `solid` in the bar — what `primary` was asking for and never got. `#ch-alllibs` got **no** component and says why: its hint belongs to three sibling groups, `Field`/`FieldGroup` attach to exactly one, and `FieldGroup` renders a `<fieldset>` that `#chfilters fieldset` would paint as a fourth box around the other three — so it wears `.hint`, the panel's own correctly-scoped class. `Field`'s `description` slot was declined for the same class of reason: `Checkbox` has no description slot, so one hint of three cannot follow and the box would carry two typographies. Two upstream gaps found: `FieldProps`/`FieldGroupProps` spread NO rest props (no `id`, no `hidden`, no `data-testid` — unlike `Button`/`Badge`/`Picker`), which cost `#dyn-lineup` its `id` and blocked `#dyn-collections` entirely; and `Checkbox` has no `description`. Audit: 23 class/element pairs → 16, and the 16 left are state classes and Tailwind's own `peer`/`divide-*`.)* |
| 2026-08-21 | [A queue entry is an OBJECT, and it carries its rating key](2026-08-21-a-queue-entry-is-an-object-and-carries-its-rating-key.md) *(data format / reversal — **completes `2026-08-21-a-queue-entry-names-an-item-not-a-line`, which fixed the symptoms in code and deliberately left the data alone**, and supersedes the string-entry half of `2026-07-20-queue-entries-are-title-strings`. “Backfill rating keys. Let's upgrade everything to the new object version. No old string versions supported in YAML anymore.” Every entry is a MAPPING now: an item is `{ratingKey, title}`, a collection is `{collection: “…”}`, an unresolvable title stays `{title: “…”}` — never deleted, never guessed at, and named in a section of its own for a hand fix. On the live file: 327 entries, 225 already objects, **80 titles backfilled, 22 collections reshaped, ZERO unresolved**; 247 of 327 keys byte-identical and every one of the 80 that moved is `title:<text>` → `rk:<key>`, which IS the backfill. The backfill uses the ENGINE's own resolver, so it freezes what each entry plays TODAY rather than choosing something new — a title is otherwise re-resolved every scan against a library that changes, and `resolveTitle` breaks a two-item tie on the LOWEST rating key. Why the string form had to go rather than be discouraged: a scalar cannot carry a sibling field (so `markDone` rewrote entries into mappings and `entryNode` collapsed them back, oscillating on ordinary edits), seven readers each carried a scalar arm, and `Collection: <name>` puts an entry's KIND in a text prefix every one of them re-parses — `plex.resolveValue` had no branch for the `{collection}` MAPPING at all, so that shape painted as an UNRESOLVED tile in the grid while playing perfectly. Choosing the target shape is what found it. **The refusal is per ENTRY and never per file**: this app runs unattended on the household TV, and a hard read error would take every queue off the air over one stale hand-typed line, with nobody at a keyboard — so `loadEntries()` drops that entry by name, the rest of the queue plays, and the entry is left visible (listSet keeps it, the grid paints it with its existing red **unresolved** border) rather than silently skipped. A warning that still played the string was rejected the other way: “supported, with a log line” is still supported. `entryKey()` is **untouched** — all five parity oracles pass — and so is the HTTP surface, which still takes a bare title and normalizes it at the WRITE boundary. **Order matters: the data migration runs BEFORE the deploy**, because the old code reads mappings fine and the new code refuses strings.)* |
| 2026-08-21 | [A class name is not a style — a borrowed one ships unstyled markup, silently](2026-08-21-a-class-name-is-not-a-style.md) *(UI / CSS architecture — "In the image, that page still doesn't use Charcuterie. Dismiss isn't a button, the dropdown isn't valid, and the edition is directly next to the text without a space." Three complaints, ONE defect: `PendingView` borrowed three class names from views it does not live in. `.editionbadge` and `.addto` are only ever written `.results .…` and a Pending tile has no `.results`; `.exclude` is only ever `.tile .exclude` and the list item is `.pendingtile`. A fourth turned up in the fixing — the YEAR wore `.y`, whose one rule is `.results .y`, so it rendered bold and full-contrast inside `.ptitle` and welded itself to the title. `.pendingtile .pendingactions` sets three layout properties and nothing else, so **nothing in the stylesheet styled either control on that page at all**. ⚠️ **#157's `Menu` conversion was correct and was not the fix**: `Menu` CLONES its trigger, and it was handed a raw `<button className="addto">` — a correct panel behind a trigger that paints as text still reads as "doesn't use Charcuterie". Now: Dismiss and the Add-to trigger are Charcuterie `Button`s (`Button` spreads `ComponentPropsWithRef<"button">` and `useClonedChild` COMPOSES `ref`/`on*`, so the clone keeps both writers — read in the source, not assumed); the badge is the shared `EditionBadge`; and `.editionbadge` / `.y` lose the `.results` prefix, because **a shared component whose CSS needs one ancestor is not shared**. Appearances argued rather than copied — Add-to is `outline`/`accent` because a GRID of `solid` is a wall of indigo, Dismiss is `outline`/`neutral` because `ghost` is transparent-until-hover, which is the reported defect rather than a fix for it, and `danger` would claim a deletion that never happens. **No linter can catch this** — a class name is a string, CSS is not in tsc's program, and unstyled markup passes axe — so the countermeasure asks the BROWSER: `e2e/borrowed-class-audit.ts` matches every element against every rule for each class it wears. It reports and does not gate, because a state class on an ancestor is a legitimate no-match. **Five real cases stay open on other pages** (`#newdyn`/`#newcurated` `.accent`, `#ch-alllibs` `.subhint`, `#dyn-lineup` `.flags`, two DynModal `.fieldselect`, `#bulkapply` `.primary`) — listed rather than fixed, since each is a visual change on a page this did not touch and four are a design question, never having HAD the look their name asks for. Found in passing: `PlayView.tsx` held a literal NUL byte, so `grep`/`rg` skipped it as BINARY and every search of this repo silently omitted it — the first audit pass "proved" `.playlinks` was dead CSS while `<p className="playlinks">` sat on line 519.)* |
| 2026-08-21 | [A queue entry names an ITEM, not a line — and Pending subtracts what you have watched](2026-08-21-a-queue-entry-names-an-item-not-a-line.md) *(fix / semantics — **extends `2026-08-17-pending-is-what-nothing-will-play` without changing what it settled**. "That page shouldn't show stuff I've watched already nor stuff already in another queue. I was able to double-add [a show] to my … anime queue (and removed one) because I had 2 copies in there." Three symptoms, ONE cause: an entry has two possible identities on disk — a rating key or a bare TITLE — and nothing reconciled them. Coverage read rating keys and collections only, so **84 of the 327 live entries covered nothing**; the add path deduped on `entryKey`, which keys `title:<text>` and `rk:<n>` differently, so the same show landed twice; and watch state was never subtracted at all. Fixed with the ENGINE's own resolver, so "covered" keeps meaning "is going to play" — the case that settles it is two library items sharing a title, where `resolveTitle` breaks the tie on the LOWEST rating key, so a same-titled new arrival is genuinely not what that entry plays and must stay listed. A title index would have hidden it. The full section listing is used as a cheap over-inclusive PRE-FILTER instead, so a normal page load resolves nothing: 1.446 s → 1.501 s with a 14-day watermark; 1.470 s → 1.843 s in the owner's worst case, where his watermark is 0 and every item in the library is "new". That case is also the measurement: **2595 rows → 2162**, of which **366 were already watched** and **67 already named by a title-only entry**. WATCHED means any view activity for a movie (Completed or a resume point) and `viewedLeafCount >= leafCount` for a show — a series with one episode left is still something to queue — judged as the ADMIN, which is flagged for the owner rather than settled. `entryKey` is **not changed** (the Python prune addresses the same lines by it and the golden oracles record it); the duplicate test is a second, looser check at the add ROUTE, with a time budget that FAILS OPEN so a Plex hiccup never freezes an add. An unresolvable title covers nothing, so a broken entry leaves its item visible rather than hiding it. Deliberately NOT done: a queued collection still does not block adding one of its films, and the 84 live entries are **not** backfilled — that is the owner's call, proposed in the PR.)* |
| 2026-08-21 | [A queue created on a group page joins that group, and the server does the filing](2026-08-21-a-queue-created-on-a-group-page-joins-that-group.md) *(UX / data model / API — **answers the question `2026-08-21-the-play-landing-creates-a-queue-without-un-hiding-the-toolbar` deliberately left open**, and builds on `2026-08-17-a-group-is-who-is-watching-not-a-plex-profile` without weakening it. "It should join wherever I added it." The gap is older than the button that exposed it: until #154 the only create button was in the Ordered Queues toolbar at `/queues`, where no group is on screen, so nothing was ever missing. Filing means appending to the group's stored `sets:` — the EXPLICIT half — because the resolved `setIds` is derived from `accounts:` at read time and has nowhere to be written. The SERVER files it, in the create request: the client alternative is POST then PATCH, and a closed tab between the two leaves a queue that exists and belongs to nobody, with nothing left running to notice. Two YAML files are still two writes, so the ORDER is the safety — the set is written first, the surviving failure is an UNFILED queue (in `All`, in `unassigned`, one tick from fixed), and a failed filing is a **200 with `groupError`**, never a 400 that would invite a retry making a second queue. `all` is not a destination and not an error: it is synthesized, in no file, and already means "no filter" everywhere else. An ACCOUNTS-only group **is** a destination — the append makes the set named, and explicit-beats-derived then keeps it there, which is the settled rule working rather than an exception to it. The active group is read off the URL inside `SetModal`, not passed through `openSetModal`, so `#newqueue` on `/queues`, `＋ Curated pool` on `/channels` and `openDynModal`'s `＋ Filtered pool` are unchanged by construction — `parsePath` yields a group on the play route alone. The modal SAYS where it lands, as a LINE and not a picker: the destination is a consequence of the URL already open, and a control would offer a choice the route has made.)* |
| 2026-08-21 | [A tile names the edition Plex gave it, in ONE chip](2026-08-21-a-tile-names-the-edition-plex-gave-it.md) *(fix / ux — "Queue tile should show the edition like it renders 'Collection'." The pickers learned to name the edition in #153 and the TILE did not, so a pick that was made correctly landed in a queue as one of two identical captions. The data died one layer short of the wire: `plex.posterFields()` sets `editionTitle`, `tiles.resolveTile()` copied four fields off that object and not this one, and both routes already spread `...core` — so the resolver was the whole server change. The chip is ONE label, not the Collection chip's two-part `kind` + `name`: "Director's Cut" is self-describing where a bare collection name is not, and a fused "Edition" half would spend most of a 132px Narrow View tile on the constant word and ellipsise the variable one. Neutral, because `2026-08-15-badge-intent-means-one-thing` reserves accent/success/info/danger for the watch-state axis — which is also why this is NOT a second caller of the search row's accent `EditionBadge`. It cannot re-open the noise the type cull closed: it fires only where Plex actually tagged the item, six of eight fixture tiles render nothing, and the two that do are the two that were indistinguishable.)* |
| 2026-08-21 | [An "Add to" menu is a `Menu`, not a `Picker` — the picker rule does not reach it](2026-08-21-an-add-to-menu-is-a-menu-not-a-picker.md) *(UI / component choice — "Is this QueuePilot page for Pending items using the correct Charcuterie dropdowns? Doesn't look like that's a listbox or non-native Select (Picker) or anything." He was right that it was wrong, and the useful part is HOW: all three `.qmenu` menus were hand-rolled `<div>`s of `<Button>`s with no role, no arrow keys, no Escape and no outside-press dismiss — and **no lint rule could have caught it**, because the picker ban added in #152 looks for a native `<select>` and this code never had one. A hand-rolled overlay is invisible to that rule by construction. The Pending tile's and the toolbar's Add-to become Charcuterie `Menu`s and NOT `Picker`s: a `menuitem` DOES something, an `option` IS something, and choosing a queue here POSTs an add and keeps no selected value — while the Add-to POSITION control one element away holds a value and is correctly a `SelectListbox`. Deleted rather than ported: the document-level Escape listener, ~30 lines of hand-rolled arrow-key walking, and the `.pendingtile .qmenu { left: 0 }` viewport override. `PlayMenu` is the last hand-rolled menu and is deliberately left — `Menu` clones a TRIGGER and that one is a singleton opened from six call sites by DOMRect.)* |
| 2026-08-21 | [The profile gate verifies the ACCOUNT Plex is playing as, not the keypress it sent](2026-08-21-the-profile-gate-verifies-the-account-plex-is-playing-as.md) *(bug / playback correctness — "a bunch of shorts for 'Younger Kids' account were watched on my account… **It should error instead**." It could not: every check in the chain verified an INTENTION, never an outcome. `adb.switchTo` returned `true` on the `DPAD_CENTER` **keypress** — a profile PIN, a pick that does not take, or a re-rendered picker all leave the right tile lit and the sign-in unchanged, and every one of them reported success. `driveProfile` then wrote that unverified claim into `profiles.LAST_SEEN`, and `onRequired` read it back to SKIP the picker walk — a cache confirming its own last guess, so once wrong it stayed wrong on every later play, silently. `profiles.ts` had warned in a comment since the port that LAST_SEEN must be "never anything that clears a profile gate"; the FSM did exactly that. Reconstructed to the minute: 2026-08-18 15:58 the Shield was on **Older Kids** mid-short, 15:58:45 QueuePilot published `awaiting profile:Younger Kids`, 15:59:01 `playing / profile: Younger Kids / played: true`, and Plex scrobbled all three shorts to the **owner**. The `shorts` set was never misconfigured — one binding, exactly as 2026-08-17 intended; the data was right and the gate was decorative. Three fixes: `switchTo` waits for the picker to GO AWAY; `LAST_SEEN` carries `isObserved` so only a profile the PMS log actually SAW may skip the walk (a `driveProfile` claim never can); and `driveToPlaying` now AUDITS `/status/sessions` after play, stopping a mismatch with a spoken sentence naming the set, the wanted profile and the one it really played as. ⚠️ **Attribution follows the Shield's sign-in, not the token that built the playQueue** — Younger Kids held near-complete `viewOffset` with **no `viewCount`** while the owner held the completions, so getting the token right (which `playToken()` already did) could never have prevented this. **Abstention is not failure**: no session yet or Plex unreachable passes the play through, because killing a play that is probably fine is worse than an audit with no opinion. The PMS-log detector is deliberately NOT repaired — its line carries no device identity, so re-pointing it at the proxy would match QueuePilot's own calls and confirm whatever the gate just asked for. 8 mis-filed shorts were moved back to Younger Kids by hand. Gate: `e2e/account-audit-test.ts`, 22 offline checks in CI, plus two new `fsm-wake-and-skip` scenarios. The `set: "auto"` NFC path is still blind and stays open.)* |
| 2026-08-21 | [Every picker names the edition, and the entry it writes carries it too](2026-08-21-every-picker-names-the-edition-and-the-entry-it-writes-carries-it.md) *(fix / ux — **extends the 2026-08-17 record, which settled the rule but landed it in one picker only.** "I also swear we were supposed to get the edition to display here in this list when searching items to add to queues." #139 touched `ChannelMembers.tsx` and never `QueueView.tsx`, `Toolbar.tsx` or `ChannelFilters.tsx`, so `editionTitle` was on the wire and `.results .editionbadge` was in the stylesheet while three of the four pickers drew neither — two rows reading `Ulysses 1954` and `Ulysses 1954`. The label was copied per call site too, so the queue box stored both editions under one title. One `EditionBadge`, one `entryTitle()`. The stored title is display text only — `resolveValue` returns on the ratingKey branch before reading it and `entryKey` is `rk:<ratingKey>` — so carrying the edition needs NO migration and nothing re-keys. `parseTitleString` cannot round-trip the em-dash suffix; that path is unreachable from a picker and is recorded rather than fixed.)* |
| 2026-08-21 | [Any tile in an editable grid gets the ✕](2026-08-21-any-tile-in-an-editable-grid-gets-the-remove-control.md) *(bug / UX — "From the Ordered Queues view, I can't remove items either." The shelf renders the SAME `PosterTile` as `/q/<id>` and simply passed none of the write props, so the only route to a removal was to open the queue first. It was never a read-only page: `useHomeDrags` already reorders a title within a shelf and drags it into a DIFFERENT queue, and a page that can move a title but not remove it is incoherent. **Two independent gates had to line up and only one was visible in review** — the missing `onRemove` AND CSS that undid `.tile .remove { display: none }` only under an `.editable` ancestor the shelf's `<main>` does not carry; restore the prop alone and the button renders invisible, found by every `querySelector` and seen by nobody. So the ✕ now keys on being IN THE DOM, which `PosterTile` already gates on `onRemove`; the ✓ multi-select keeps its `.editable` gate because it belongs to one page. `removeTile` was a closure inside `QueueView` — lifted to `state/queueEntry.ts` unchanged, because reaching it meant reaching into a component and the alternative was a second copy of an optimistic write. Undo needed no client work (`undoSnapshot` middleware snapshots before every mutating request) and the new gate drives `#undo` anyway, so an "optimistic-only" shortcut cannot look correct. The shelf does NOT become `.editable`: that class carries `touch-action: pan-y`, which would stop a finger panning the horizontal strip.)* |
| 2026-08-21 | [The Play landing creates a queue from its own link row, and the toolbar stays hidden](2026-08-21-the-play-landing-creates-a-queue-without-un-hiding-the-toolbar.md) *(defect / UX — "Also, there's no 'add new' to add a new queue" and, from a group page, "Even here, I can't add a new queue." He was right, and the reason is older than the button: `＋ New queue` has lived in `#tools` all along, and both landing branches set `body.queue-view`, whose whole job is `#tools { display: none }` — so the only route to a new queue was the grey link row, then `Configure ordered queues ›`, then the header of that page. In the Narrow View it was worse than hidden: `#tools` mounts into `#gslot-narrow` INSIDE `QueuesView`, so the landing never had the element. The hide is not deleted — un-hiding it would drop the queue filter, Collapse all and the add-to-any-queue search into the Play and Pending headers, which is the leak the class exists to stop — so the landing gets a local affordance, exactly as `ChannelsView` does with `＋ Filtered pool` / `＋ Curated pool`. Three details are load-bearing: its own id (`#playnewqueue`, because two suites `click('#newqueue')` and `PlayView` renders first, so a duplicate id would hand them the hidden one); the seeded `movies` kind, which the modal's Type picker then ASKS about one field in; and a Filtered Pool staying behind `Configure pools ›`, since it is rules rather than a member list. Known and NOT silently decided: a queue made from a group page does not join that group.)* |
| 2026-08-19 | [queuepilot is yarn workspaces, and Playwright is a real dependency](2026-08-19-queuepilot-is-yarn-workspaces-and-playwright-is-a-real-dependency.md) *(toolchain — **implements the fleet's 2026-06-18 "use yarn, never npm/npx" decision, which this repo had never complied with.** "???????? It should all be yarn. Please fix." Four package-lock.json files become one yarn.lock over web/server/e2e/e2e-broker; Playwright stops being an unpinned `npm install` CI ran TWICE per job and becomes a pinned devDependency of the e2e workspace, so the CLI and the module are the same copy at the same version — the property `e2e/playwright.ts` always assumed and never had. Three rules fell out of it, each caught by a real failure: `nmHoistingLimits: workspaces` (hoisting put web's `@types/react@19` where the SERVER's tsc saw it and satori stopped typechecking); the COMMITTED yarn release run through node, never `corepack enable` (Node dropped corepack in 25 and both CI and `node:26-trixie-slim` are past that — the Docker build said `corepack: not found`); and a `.dockerignore` that re-includes the workspace manifests it otherwise excludes, since yarn resolves the lockfile against every workspace. Image built and inspected against the shipping one, not assumed.)* |
| 2026-08-19 | [The whole card is the drag handle — on a fine pointer](2026-08-19-the-whole-card-is-the-drag-handle-on-a-fine-pointer.md) *(UX — **amends the same day's wrapped-grid record.** "The drag 'n drop is taking up precious space and leaving a huge margin, but it doesn't need to. If it's only on hover, then make the whole card draggable." Hiding a control with `opacity` does not give its space back: the glyph held ~22px of gutter on every card at every moment for something shown on hover, which on a 320px card is 7% of the width — the difference between "Bob & Alice — Movies" fitting on one line and wrapping. On a FINE pointer the glyph is `display: none` and the card itself starts the drag, with `cursor: grab` as the affordance; presses landing on a link, button or listbox are skipped, so the name still navigates and Play still plays. **Touch keeps the handle** — whole-card touch dragging needs `touch-action: none` on the card, and the card is the surface the page scrolls by, so the landing would stop scrolling under a finger.)* |
| 2026-08-19 | [The landing is ONE wrapped grid of typed cards, not three columns of one kind each](2026-08-19-the-landing-is-one-wrapped-grid-of-typed-cards.md) *(UX / layout — **supersedes the three-column LAYOUT half of the 2026-08-16 taxonomy record; its three NAMES are reaffirmed and now render on the cards.** "I really don't like this layout. I want it all combined in a way that makes a lot of sense, and the card itself shows you the type of queue it is. This 3-column model really doesn't work." Three columns are three columns whether you own 2 sets or 17 — one install rendered two thirds blank while the household's ran eight cards deep beside a column of three, and picking a group is exactly when a kind empties out. Kinds move onto the cards as a quiet MONOCHROME badge: colour on this page already means provider, and a coloured kind dot put amber beside an amber Plex button. One grid also means ONE order — a card can be dragged anywhere, not just within its own kind — which cost a rewrite of the drag hit-test, because a Y-midpoint test cannot tell apart cards that share a grid row. Four candidates were mocked at true width and the owner picked this one.)* |
| 2026-08-19 | ["All" is an address (`/g/all`), not the absence of one](2026-08-19-all-is-an-address-not-the-absence-of-one.md) *(bug / routing — "clicking 'Kevin' or any tag, then clicking 'All', it never goes back to 'All'. I think it keeps redirecting or something." He was right about the mechanism: All was spelled bare `/`, which is precisely the URL that "did not say", so the landing effect answered it from the remembered group and redirected back in the same frame. Once any group had been visited on a device, All was unreachable until localStorage was cleared. The memory rule is not weakened — All is now a URL that SAYS something, and landing on it clears the memory so a reload stays put. Also fixes the same latent bug in the provider chips, which hang off `basePath`.)* |
| 2026-08-17 | [The reading list crosses libraries again — the owner backs out of the reader instead](2026-08-17-the-reading-list-crosses-libraries-again.md) *(bug / reversal — **supersedes the 2026-08-16 library-boundary workaround.** Reported live: "Something in QueuePilot made it so Kavita reading list is only 1-2 items even though the default is still 12." Nothing to do with Playback Length: `sameLibraryPrefix` wrote only the leading run of items sharing the first item's library, and `buckets()` INTERLEAVES series while a `kind: anime` pool is `isRandomOrder` — so the two libraries alternate from item one and the cut landed after the first series or two, not after "the first library's batch" as that record priced it. Measured on `/lists/153`: **4 items** (two series x `episodes: 2`, all library 2) out of a 12-item lineup; earlier shuffles gave 1 and 2, and the tiles said 12 the whole time. Kavita#4859 is unchanged and still real — the owner handles it: "I can manually fix that by existing and coming back in if the pagination is wrong", which is the same fresh navigation the truncation bought, at the cost of a tap only when the profile is ACTUALLY wrong. Grouping by library was considered and rejected — it keeps the list full but silently un-does the interleave that stops a queue becoming a single-series binge. The 2026-08-15 rebuilt-not-appended record is untouched: what changes is the LENGTH of the rebuild.)* |
| 2026-08-17 | [The reading list is a MAINTAINED window: 12 up front, then add and remove](2026-08-17-the-reading-list-is-a-maintained-window.md) *(bug / feature — "Yes, make it 12, then add items." / "Add and remove*". `topupList` has appended-then-trimmed since it shipped and **had simply never run**: `topup()` refused a reading list three ways, each a rule about a PUSH lineup — `SESSION.set` (nobody starts a reading list, so every tick answered "no active session"), `source !== 'rotation'` (the live pool is CURATED), and `needsTopup` (correctly false for a set whose length is one window — right for a sitting that may end, meaningless for an artifact that IS a sliding window). HA's tick only fired while a session was named, too. So the list was seeded once per launch and then only shrank. Now the tick sweeps pull sets itself — the empty-payload wake-up contract is kept, because naming `manga_webtoons` in an automation would put a second copy of the set registry where it cannot be seen. Also: ONE lineup builder for launch and top-up (`pullLineup`), since top-up's own `buckets()` call omitted `entries` and would have served the library shelf instead of the owner's ninety-three series — the exact bug `buckets()` already records from the launch path; and the cooldown becomes per-set, because one shared timestamp let a reading top-up freeze the kids' Shorts channel for a minute.)* |
| 2026-08-17 | [Pending lists what nothing is going to play — the subtraction IS the feature](2026-08-17-pending-is-what-nothing-will-play.md) *(feature — "a 'Pending' or 'New' area to show if there are new movies or shows added and allow me to specify the queues to add them IF they're not already picked up by one", motivated by "In the future… I won't use filtered pools anymore and will switch fully over to curated pools" — curating everything only works if nothing arrives unnoticed. ⚠️ **The `if` is the whole feature**: a list of everything recently added is Plex's own Recently Added and needs no app, so the gate asserts what is ABSENT rather than what is present. "Covered" means what it means to the engine — a curated set names its ratingKey (queue entry or pool member) or names a COLLECTION containing it, or a filtered pool's rule would sweep it up (its section, past its rating cap, not blocklisted). Two pieces of state in `/config/pending.yaml` because they answer different questions: `seen_through` (one epoch second, so clearing the list costs one line not one per item) and `dismissed` (per-item, because skipping ONE film must not hide the twelve added after it). Not the SQLite cache — a dismissal is a decision, not recomputable from Plex. Both Plex reads are PARAMETERS rather than imports, the same seam the selection engine uses; forced early, since ES modules cannot be monkey-patched and the first draft's internal `import('./plex.js')` was untestable. Plex "Other Videos" libraries are skipped unless a set draws from them — not tidiness: on the first real run they were 7 of 11 rows, eleven `[Betterman QC] … x265-10bit` variants of one clip burying a film someone might want; conditional, because naming one in a queue makes its items candidates again. Verified live: 4 real rows out of thousands of items. Still open: no notification (you must open the screen), coverage judged per SET rather than per binding, and adding always appends to the bottom.)* |
| 2026-08-17 | [The Play landing reorders by drag, and a shelf's drop rewrites the whole order](2026-08-17-the-play-landing-reorders-by-drag.md) *(feature / ux — "I also have no way to reorder these items." Worse than it looked: drag existed only on the Queues CONFIGURATOR (`useHomeDrags`, whole shelves), so Curated and Filtered Pools could not be reordered ANYWHERE in the app, and the landing — the screen the household actually opens — had none at all. All three shelves get a handle; the gestures copy `useHomeDrags` deliberately because they are the same gesture (mouse past a threshold, touch on a ~200 ms long-press so a swipe still scrolls, transform-only, one `insertBefore` per crossing, and the dragged node restored to where React last rendered it before any state update — skipping that is what produces `NotFoundError` on a later commit). ⚠️ **A drop sends the COMPLETE order** with only that shelf's slots permuted: `reorderSets` ranks what it is told and APPENDS the rest, so a shelf-only list would sweep every other set to the end of `sets.yaml`. The same mechanism is what makes reordering correct under a group filter — a hidden row is not in the shelf's list, so its slot is never touched. The write is OPTIMISTIC and that is not polish: the drop restores the node to React's position, so the row visibly snaps BACK until new data lands, and waiting for `load()` holds that snap-back for as long as `/api/queues` takes (7–9 s warm). The handle is quiet until hover (always shown on a coarse pointer) and `aria-hidden` — a pointer affordance, not the only way anywhere. Gate `e2e/play-reorder-test.ts` seeds a filtered pool BETWEEN two ordered queues and asserts hardest about the row it did not drag; it drives the drag in twelve small steps because the hook swaps on crossing a midpoint, and reloads to prove the write rather than the optimistic DOM. Found in the making: the hook first kept its commit callback in a plain `{ current }` object rebuilt every render, so the listeners held the FIRST render's closure — the one where `GET /api/sets` had not answered — and the commit read an empty set list and returned. The drag worked, the drop did nothing, and nothing said why.)* |
| 2026-08-17 | [A search result says which library it is in, which edition it is, and whether this pool can reach it](2026-08-17-search-results-say-which-library-and-which-edition.md) *(fix / ux — "in search results, everything is mixed, so it's hard to tell which is which and where it came from… prioritize the selected ones first and put an `<hr>` style line between those selected and the rest", plus "there's no 'edition' listed, so I don't know which of these is which" over two rows reading `Big Buck Bunny 2008`, identical character for character. The member picker searches `scope=all` on purpose — a curated member is a manual include and need not come from a library the pool draws from — so the fix is telling you WHAT you are looking at, not narrowing the search back down. Results group into this pool's libraries, a rule, then Other libraries; every row names its library and its EDITION when Plex gave it one (verified live first: `267280` carries `editionTitle: "3D"`, `267281` carries none — two library items, same section, same title, same year; the plain one renders plain because absent is Plex's own shape, not a gap). ⚠️ **Collections still lead INSIDE each group** — the outer sort is the group and collections-first only breaks ties within one; that rule predates this and is load-bearing, since a franchise search turns up dozens of hits, the dropdown caps at 30, and a collection appended after the items was pushed past the cap and never shown. The in-pool heading is drawn only when a second group exists (a search entirely inside the pool is the ordinary case and gets no chrome), while "Other libraries" is drawn even when EVERYTHING is out of pool — that is exactly when the reason needs to be on screen. ⚠️ The separator lives INSIDE the first `<li>` of its group: `SearchDropdown` delegates clicks to the list and finds a row by its index among the children (itself a fix — a row-bound listener dies when a late response re-renders between mousedown and mouseup), so a separator `<li>` would shift every index after it and fire the wrong result's `pick`; `flex: 0 0 100%` makes it read as a rule anyway. Gate: 11 unit tests in `web/src/lib/searchGroups.test.ts`.)* |
| 2026-08-17 | [A collection member COVERS its shows, and a pool chooses whether it stays whole](2026-08-17-a-collection-member-covers-its-shows.md) *(fix / feature — "if I add the Batman collection which includes some of the shows, I want those shows to not show up in the list… I'd prefer the shows get added in-order via the collection rather than playing random parts of them." The screenshot showed Batman: The Animated Series TWICE: once as the collection member, again in the Eligible pool as a standalone show. `channelBuckets` deduped members against the rule pool by BUCKET ratingKey and a collection's bucket key is the COLLECTION's, which can never equal a child show's — so the pool played the same shows both ways at once, the collection in order and its shows again at random. The blocklist had always been right (`expandedBlocklist` expands `Collection: <name>` to its children); the include side simply never got the same treatment, so the exclude side and the include side disagreed about what a collection IS. ⚠️ **The cover is not optional and not a mode** — every child leaves the rule pool either way; what IS a choice is how the collection comes in, stored per pool as `collection_members`: absent/`whole` (**Use collections**) is one ordered member, `split` (**Don't use collections**) is one member per child, each taking its own turn and inheriting the collection's `weight`. Sparse storage (`whole` is absence), the EFFECTIVE value reported by `GET /api/sets` so the picker keeps no copy of the default, and strict-on-write / tolerant-on-read: `updateSet` throws on a typo (which must not silently leave a pool playing the shape you just tried to change) while the engine reads anything unrecognised as `whole` (so a hand-edited file still plays). The cover applies in BOTH modes deliberately — in `split` it is a no-op for any child that resolved, and one unconditional rule beats a rule with a mode-shaped exception. Labels are the owner's own words, chosen over two alternatives offered; the hint changes with the selection so each option explains itself. Lives in ⚙ Configure, so "filtered pools only" holds by construction, and is hidden on a rewatch pool for the same reason the Lineup box is. Gate `e2e/collection-covers-its-shows-test.ts` fails pre-fix with exactly the reported shape.)* |
| 2026-08-17 | [This repo is public, so people, hosts and IPs are always placeholders](2026-08-17-the-repo-is-public-so-people-hosts-and-ips-are-placeholders.md) *(privacy / naming — the owner asked whether the household's real first names had been committed here instead of the Bob/Alice placeholders, and whether the repo was leaking PII. It was, and it was a **regression**, not a gap: `sets.ts` and `plex.ts` have used `bob` / `bob_alice` / "admin (Bob)" since the ROOT commit, and real names were written in alongside them. A binary-safe scan of every blob found the owner's first name ~396 times, his partner's ~103, a child's ~63, the surname ~23, the household domain ~136, the NAS hostname ~28, plus live LAN IPs — present from the root commit, so there was no clean window to cut from. This record and its detail page deliberately do NOT repeat the removed strings; the mapping lives in `queuepilot-private`. ⚠️ The real damage was **158 committed screenshots**: captures of the LIVE app that rendered the names as **pixels**, including one named for a child in its filename showing that child's name over that child's actual viewing history. `--replace-text` works on blob bytes and cannot redact a picture — and a length-changing match INSIDE a PNG corrupts the deflate stream rather than redacting it — so images were excluded from the text pass and `docs/images/` was dropped from every commit instead. Whole history rewritten with git-filter-repo and force-pushed (new root; old SHAs orphaned). Fixed placeholder sets now: people **Bob / Alice / Carol / Dave / Erin** (`Bob Smith` for a display name), hosts **`*.example.com`** (`nas.example.com` for the NAS), IPs **RFC 5737 `192.0.2.x`** — matching the posture the 2026-08-06 sanitised-IP incident established, where the DEPLOY supplies real values and committed defaults are placeholders. Generic tier labels (`Younger Kids`, `Older Kids`, `Family`) stay — they name a tier, not a person. Verified from a FRESH clone of GitHub: 1601 blobs, zero name/host/IP hits. New screenshots must be captured against the offline `e2e/shot-*.ts` stub stack, never the live server, and looked at before committing.)* |
| 2026-08-17 | [The reading list is titled with the set LABEL, and renamed in place](2026-08-17-the-reading-list-is-titled-with-the-set-label.md) *(ui / bug-prevention — supersedes the "list TITLE is untouched" clause of the cover record earlier the same day. "Go ahead and redeploy and rename whatever." The slug was never for reading: a list is now `QueuePilot — Manga & Webtoons`, not `QueuePilot — manga_webtoons`. The earlier clause declined because the title is the lookup key and changing it would strand `/lists/153` — true of a rename done by changing the KEY, false of one done by renaming the LIST: Kavita has an update endpoint and the id survives it, so `materialize()` matches both names and renames the old one in place. ⚠️ **`update` takes the WHOLE DTO with no patch semantics** — renaming with `coverImageLocked: false` answers 200 and comes back `coverImage: ''`, so a careless rename would have silently deleted the artwork shipped hours earlier; the live list was also `promoted: true`, which a hardcoded `false` would have quietly demoted. `updateList()` therefore takes all four fields as REQUIRED and `materialize()` echoes what it just read. The lookup moved into one shared `findSetList()` because `topupList()` had its own copy and would have answered "no reading list for this set yet" about a list it was looking straight at — refilling stopping dead, silently. Live list 153 renamed by hand the same day, id + cover + promoted intact. The old-title tolerance is migration debt, not a feature.)* |
| 2026-08-17 | [QueuePilot puts its own artwork on the reading list it builds](2026-08-17-queuepilot-gives-its-reading-lists-a-cover.md) *(ui / provider seam — "can you add a nice cover image for it?". Kavita generates a list cover from the first item and REGENERATES it whenever the items change, and this list is rebuilt on every launch — so its artwork was a different interior page every time (that day, a "STRONG LANGUAGE" splash from Becoming the Monarch). `materialize()` now uploads a cover when it CREATES the list, or when an existing one is still unlocked; an uploaded cover sets `coverImageLocked` and Kavita stops regenerating, which is what makes this the one part of the artifact that is not rebuilt. Design picked out of four served over `devshare`: the app mark in **Kavita green**, not QueuePilot amber — an artifact inside Kavita wears the provider colour the queue already wears here. Rendered by **Satori to SVG**, which **Kavita rasterizes** — so no resvg/sharp/native addon enters a bundle that externalizes nothing. ⚠️ Two live traps, both probed on throwaway lists: the upload `url` must be **RAW base64** (the `data:image/png;base64,` spelling Kavita's own UI uses elsewhere is a **400**), and Kavita's rasterizer **ignores `@font-face` with a data-URI src**, which is why glyphs must arrive as PATHS. Fonts are inlined base64 in a source file because the runtime image carries only `server/dist/index.js`. Best-effort like the stale-item clear: a failed cover never costs the reader their lineup. **Costs ~956 KB of server bundle.** The list TITLE keeps the set id — it is how the list is found again, and renaming it would strand `/lists/153`.)* |
| 2026-08-17 | [No libraries checked means EVERY library, on every provider](2026-08-17-no-libraries-checked-means-every-library.md) *(ux / invariant / bug — "I'd rather select none and let it search all available games, but it's forcing me to select them. Honestly, those should always be optional no matter Kavita/Plex/Board Games." A library scope is now OPTIONAL on every provider and every shape, and an empty checkbox group means **all** — never none. All three `at least one library section required` throws are gone, as are the three client gates that mirrored them, and every CONSUMER widens an empty scope instead: the Plex search route and `/api/ratings` to every video library, `unwatchedBuckets` to every video library split by Plex's own types, Kavita's `pool()`/rule-based `buckets()` to every library, the picker to the whole shelf. ⚠️ The bug underneath was the picker's synthetic **Collection** checkbox: it meant "everything" and was silently DROPPED from the category filter, so ticking every box narrowed the search to `Roll 'n Write` and `cubitos` — `ownerCategories: []`, like most of the shelf — could not be found. Ticking both boxes was strictly worse than ticking one, which no UI can convey; the box is deleted, and a stored `collection` id now WIDENS the scope rather than being dropped. The gate was the other half: unticking the last box failed the save, so "all of it" was unsayable — that validator had already been patched once (2026-08-13) to stop rejecting Kavita-only queues, and the rule underneath it was the real defect. Widening happens at the consumer, never at save time, so an unscoped set picks up a library added to Plex tomorrow instead of freezing today's list. **Ratings are not libraries** — an empty rating list stays an error, being a cap of nothing rather than "any rating" — and a CURATED queue never widens, since `entries` beat `libraries` (the 93-entry reading-list bug). There is no board-game **sync** at all, which the report reasonably assumed: search asks the picker over HTTP per keystroke-batch and progress is its play log read on demand.)* |
| 2026-08-17 | [A Group is "who is watching", it lives in the path, and explicit membership beats derived](2026-08-17-a-group-is-who-is-watching-not-a-plex-profile.md) *(architecture / data model / ux — "I select 'Bob' from a list, and all my stuff is there." A Group is OURS and maps N-to-M onto provider accounts (`{plex: […], kavita: […]}`) — not a Plex profile and not a Kavita user, because the two backends disagree about who anyone is (Plex knows `sawtaytoes`, Kavita knows `Bob`) and there is otherwise nothing to hang "all of Bob's stuff" on. Bob is 1 Plex + 1 Kavita, Carol 2 Plex + 1 Kavita, Alice Kavita-only; a group may also be an audience (Bob & Alice) or neither (Demo). Profile and Group COLLAPSE into one concept, against the design doc's tentative "two concepts, one control" — the account mapping IS the membership rule, so the separate profile had no field of its own. ⚠️ **EXPLICIT-THEN-DERIVED** is the rule the feature lives or dies on: `sets:` claims outright, `accounts:` is consulted only for a set no group named. Nearly every curated queue is gated to `sawtaytoes`, so an account-first resolution sweeps every audience into "Bob" and the distinction disappears — while still LOOKING like a working feature with one over-full chip; the opposite failure (no derivation, so account-only groups Kids and Demo come out empty) is pinned in the same gate. The group lives in the PATH (`/g/<id>`, the design doc's Option C) with `localStorage` answering a bare `/` only, so it is bookmarkable and never overrides a URL you typed; ids are IMMUTABLE and labels rename freely, the same contract set ids have. Provider is a FILTER (`?only=kavita` chips), not a level of the hierarchy — provider-first costs a trip to the root every time the medium changes, which the owner talked himself out of mid-thought. A row inside a group drops the group's own name (`Bob — Anime` reads as `Anime`), since you just picked it. Gate: `e2e/groups-test.ts`, 14 offline checks in CI, plus 92 web unit tests. Everything in the design doc's §5 stays OPEN — settings screen, device ignore list, Plex-only Filtered Pools, the Pools navigation dead end — as does carrying the group into `/q/<id>`.)* |
| 2026-08-17 | [An editor may only send a key it renders a control for](2026-08-17-an-editor-may-only-send-a-key-it-renders-a-control-for.md) *(bug / invariant — "my excluded entries (Blocked ones) keep getting removed each time we reload the server." They were stored: `PATCH /api/sets/:id {blocklist}` writes `sets.yaml` correctly and survives a restart, verified against a running server BEFORE any code changed. The loss came from the other direction — `DynModal.onSubmit` carried a literal `blocklist: []` / `movie_excludes: []`, and that editor renders no control for either (Blocked lives in the inline Pool-filters panel), so it never read their stored values. Right default on a CREATE, silent data loss on an EDIT: every Save from ⚙ Configure blanked the lot. The reload was a coincidence; what changed that day is that the Lineup box gave him a reason to open that editor at all, so a wipe that had always been there finally fired. ⚠️ Absence is the only safe encoding for "I have no opinion" — `[]` is an opinion, and on a partial-update endpoint it is the destructive one. The binding half is the same bug one level down: the editor replaces the whole `profiles[]` array and `bindingWriteObj` writes `movie_excludes` only when sent, so the draft now round-trips it; a Save that RACES the binding seeding never sends `profiles[]` and looked innocent, which is why the gate waits for the binding card. Generalises without this history: the same editor's `length`/`refill`/`on_complete` are sent unconditionally and that is correct, because it renders controls for them — "renders it or round-trips it" is the test. Gate `e2e/pool-editor-keeps-blocked-test.ts` (browser, NO Plex) fails both halves pre-fix, and runs in CI's always-on block rather than the `PLEX_TOKEN`-gated one that is skipped on every PR — which is how this reached the owner.)* |
| 2026-08-17 | [A poster tap opens the entry sheet](2026-08-17-a-poster-tap-opens-the-entry-sheet.md) *(ui / gesture — "the button is so tiny… that or it zooms in on the poster". The tap filled a gesture that did **nothing**: drag still reorders, and a tap while something is selected still toggles it. `#entrymodal` becomes the sheet — 128px artwork, the next-up line, and full-size Play / Remove above the settings it already had. Play-on-tap was rejected: it collides with tap-to-select and long-press-to-drag. `.tileplay` is excluded in `onPointerDown` explicitly — its `stopPropagation` stops the click, not the window-bound pointer listeners, so one tap used to open the device menu **and** the sheet. 26px already clears WCAG 2.2 AA; the earlier "half of 44px" framing was wrong and charcuterie [dropped that floor](../../../charcuterie/docs/decisions/2026-08-05-controls-share-one-height-no-per-component-touch-floor.md).)* |
| 2026-08-17 | [The cramped layout is the "Narrow View", never "mobile"](../../../agentic/docs/decisions/2026-08-17-the-cramped-layout-is-the-narrow-view-not-mobile.md) *(cross-cutting, lives in the workspace root — name it for the **width**, which is what the media query asks about. `isNarrow` not `isMobile`, `#gslot-narrow`/`#gslot-wide`. Playwright's `isMobile`/`hasTouch` and CSS's `pointer: coarse` keep their own names.)* |
| 2026-08-17 | [The two kid Shows cards play curated queues](2026-08-17-the-kid-shows-cards-play-curated-queues.md) *(card model — supersedes the CARD half of the 2026-07-27 split; the channel split itself and `shorts` stand. The owner hand-picked two `source: queue` sets in the pool editor and named them, so `Plex: Older Kids Shows/Shorts` moves `shows_shorts` → **`older_kids_shorts_shows`** and `Plex: Younger Kids Shows` moves `shows` → **`younger_kids_shows`**. Nothing in this repo changed — it is one `tag_command_map` value per card in HA, because the scan path already handles a profile-gated curated queue. Each card KEEPS `kind: cartoons` rather than matching the new sets' `kind: anime` (what the editor writes for any non-movies queue): `buckets()` never reads `kind` for `source: queue`, `channelFor()` runs only on `set: "auto"`, and the AVR volume rule skips the kids' cards — so the field is free to keep describing what the kid asked for. ⚠️ The profile guarantee now rides **`requires_profile`** instead of a `profiles[]` binding, which is only equivalent because the 2026-08-16 curated-queue record made the LINEUP resolve as that profile too, not just the gate. **`shows_shorts` and `shows` stay enabled** though no card names them: `channelFor()` only considers `source: rotation`, so a curated queue can never be a `set: "auto"` target and deleting either would drop the UC3 Cartoons button through to `PROFILE_SET_MAP`. Cards 2/11/12 (`movies`, `movies`, `shorts`) were not named and were not touched.)* |
| 2026-08-17 | [A show can override its pool's `on_complete`, and "drop" never removes anything](2026-08-17-a-show-can-override-its-pools-on-complete.md) *(feature / ui / copy — `on_complete_by_show`, a map keyed by ratingKey (`section-<id>` for an item bucket), the same shape and keying `starts` / `weights` already use because a rule-derived show has no stored entry to hang a field on. **THREE states, not a boolean**: absent follows the pool, `restart` starts the show over, `drop` lets it finish even on a pool that restarts everything else — that third one is the whole ask ("it would be good for each show to override this set-level config"), and a boolean could only have expressed the other direction. An unrecognised value **follows the pool** rather than inverting it: sets.yaml is hand-edited over SMB and a typo that silently flips a show to the opposite of what its pool says is the failure that looks exactly like the feature working. Edited from the pool grid beside the weight picker on the same tile. ⚠️ Also corrects COPY that was actively misleading: the set-level option said "Drop it — it leaves this pool", and the owner caught it ("it doesn't 'drop it' in terms of removing it"). He is right — `select.ts` yields an empty `unwatched`, the `if (eps.length)` guard never pushes a bucket, and **nothing is written and nothing is deleted**; a filtered pool is recomputed from libraries every scan, so the show returns by itself when a new episode lands. Removal is the CURATED queue's `done` flag and TTL sweep, a different mechanism on a different kind of set. Now reads "Let it finish — nothing plays from it". A control that claims to delete something is a control nobody dares press.)* |
| 2026-08-17 | [Playback Length is the knob, and top-up is derived from it](2026-08-17-playback-length-is-the-knob-and-top-up-is-derived.md) *(feature / ui / architecture — supersedes the `refill:` FLAG from the same morning; the top-up mechanism stands. "I think we don't need 'Items queued ahead'. Just default to 12 and use top-up to fix it. What we _really_ need is a way to specify or configure the number of movies/episodes to watch in a given setting." So: **one control, 1 / 8 / Infinite / Custom**, and the queue window stops being configurable at all. **Top-up is DERIVED** — on exactly when the length exceeds one window — which deletes the only combination that could be wrong, `infinite` with top-up off, silently stopping at 12. `infinite` is a **named value**, never 0 and never 999, because a falsy count already reads as *uncapped* in resolve.ts's applyBatch and a typo would become a binge. Applies to EVERY kind of set, rewatch pools included — that was the ask ("Movies are gonna be 1 based on _my_ configuration today, but we _should_ be able to change that"), and `pickRewatch` now draws N times with each pick excluded so a sitting never repeats a film. ⚠️ **The unit differs on exactly one path and has to**: an ORDERED queue counts ENTRIES, not items, because a show entry's batch is already its own knob — counting items would make a length of 1 truncate a 2-episode entry to one episode, which the first cut did and `resume-in-queue-test` caught. Kavita keeps a WINDOW fallback: a reading list is a persistent artifact pulled from over days, not a sitting, and the ordered default of 1 would collapse it to one series. **No existing set moves on deploy** — every default is the behaviour that kind already had, and the live Shorts card's `refill: true` READS as infinite so it keeps refilling untouched, migrating to `length: infinite` on its next save. Also lands the power-off half: a set may ask for the room to be shut down when its sitting ends, and QueuePilot **announces** it on `queuepilot/resp/finished` rather than switching anything off — HA owns power cables, and `isComplete` separates "reached its length" from "ran dry" because a lights-out rule wants the first.)* |
| 2026-08-17 | [The lineup knobs live in the pool editor](2026-08-17-the-lineup-knobs-live-in-the-pool-editor.md) *(ui / storage — #118 shipped `length`, `refill` and `on_complete` as YAML + `PATCH /api/sets/:id` only, so the live Younger Kids — Shorts card ran `length: 12, refill: true, on_complete: restart` with nothing on screen saying so; the owner: "Ok, add the UI controls too then." They get a **Lineup** box in ⚙ Configure — the surface that already owns every rule-based knob — not the inline Pool-filters panel, which is the per-profile BINDING's data while these three are the channel's. ⚠️ **Equal to the default is stored by ABSENCE**, and that is load-bearing rather than tidiness: the editor posts every knob it renders on every Save, so without it renaming a pool would stamp `length: 12`, `refill: false` and `on_complete: drop` onto a channel that said none of them — and pin its length at 12 instead of leaving it following env `ROTATION_LENGTH`. Enforced server-side (`toLineupLength` / `toOnComplete`), so a hand-`PATCH` and a Save agree. `GET /api/sets` now sends `lineup: {length, max, topup_at}` from env: the Default chip, the ceiling and the hint's "tops up at 3" all read it, because a copy of 12/200/3 in the bundle would let a deployment that moves one silently disagree with its own editor. A **rewatch** pool does not get the box at all (it returns one film per scan and honours neither knob) — hidden with a note in its place, the same call `batch_stops_at` gets on a queue with no Plex source. `CountPicker` grew a `presets` prop: a lineup counts whole items in one sitting, so it offers **12 / 24 / 60 / Custom…** where the entry picker's 1 / 2 are not answers anyone would pick. Fixes in passing that `createSet` never handled these at all — a pool created from the editor with top-up on came back with it off, and the file was the only clue.)* |
| 2026-08-17 | [A lineup refills instead of ending, and HA ticks it](2026-08-17-a-lineup-refills-instead-of-ending.md) *(feature / architecture — phase 1 made the lineup length per-channel; it was still a NUMBER, and a number runs out: "I thought we programmed it to keep going forever. So it'd load up X number in the queue, and then add more as you started getting close to the end of the queue." `refill: true` makes `length` the **WINDOW** rather than the evening. **Not** `length: all` — one infinite lineup means queueing the whole eligible pool up front (442 items on the live Shorts channel), a slow scan on a card someone just tapped and stale the moment progress moves. `on_complete: restart|drop` for a finished series, **default drop** (what every channel has silently done since the beginning — flipping it would make every existing channel replay old episodes on deploy); `restart` fires only when a show is GENUINELY finished, never when the window merely stopped drawing from it, which is a different question `weightedInterleave`'s own "bucket exhausted" already answers. The queue is done only when every bucket is dropped or exhausted. Top-up ticks on `queuepilot/cmd/session/topup`, published by HA every 5 min while a session is live — **a wake-up, not an instruction**: the payload is empty because the automation cannot see the playQueue, the viewer's position, or whether the channel opted in, so every guard stays in the app (opt-in, TOPUP_AT, a 60s cooldown against a stuck automation walking the lineup to ROTATION_LENGTH_MAX). Measures the **LIVE playQueue, never `SESSION.queue`** — the session remembers what it SENT and the viewer has been skipping since. ⚠️ **Plex has no append-at-end**, spiked before anything was built on it: `PUT /playQueues/{id}?uri=…` grows the queue in place and successive adds chain in order, but it inserts after the CURRENTLY SELECTED item and `next=0`/`next=1`/omitted are identical — seed `[A,B]` + `C` gives **`[A,C,B]`**. That is why TOPUP_AT is 3: at three left there is almost no tail to jump, and a rotation's tail is a shuffle. **Do not "fix" it by rebuilding** — that restarts playback, the exact hiccup this avoids. Kavita tops up via a new optional `topupList()` as a **sliding window** (append, then `remove-read`), which is what keeps the 2026-08-15 rebuilt-not-appended decision intact rather than reopening it by another door; progress is still read on demand, the tick is simply a new kind of demand, narrowing the 2026-08-16 no-poll record without reversing it.)* |
| 2026-08-17 | [A filtered pool is locked to ONE account, so its row has no profile picker](2026-08-17-a-filtered-pool-is-locked-to-one-account.md) *(ui / data model — the Play landing's three groups get one grammar: name, meta, one start button. The per-row tier dropdown existed because a pool had to carry every kid's binding back when the app could not switch the Shield's Plex profile; it can now, and the live `sets.yaml` was verified single-binding on all four rotation sets before any code changed — so the UI was offering a choice the data had already stopped having. The account moves into the meta line as TEXT and goes FIRST, because "Shows" and "Shows & Shorts" are the same words until you know one is Younger Kids and the other Older Kids — dropping the control without printing the account would have lost information, not just a widget. ⚠️ The picker is made CONDITIONAL, not deleted: a pool still carrying two bindings keeps asking, because silently taking `profiles[0]` is the 2026-08-16 curated-queue bug from the other end. Fixes a live defect in passing — the selected tier was `useState` seeded once while the options re-derive every render, so a binding deleted in another tab left the row holding a value that no longer existed and it would have PLAYED as an account the pool no longer has; the value is now validated against the current options. No schema change: `profiles[]` stays a list and the pool editor still edits it, so nothing migrates if the owner's profile/grouping design lands elsewhere.)* |
| 2026-08-17 | [The start button wears its provider's ICON as well as its verb](2026-08-17-the-start-button-wears-its-providers-icon.md) *(ui / architecture — extends the vocabulary ADR. `ProviderVocabulary` gains `startIcon` (`▶` / `📖` / `🎲`) and the pull-queue button reads BOTH label halves off it, so a Kavita queue says `📖 Read ↗` instead of `▶ Open ↗`. "An icon is copy": fixing the verb alone left a play triangle promising a screen next to a word that had stopped promising one — two answers to "what medium is this?" in one button, one of them wrong. "Open" was independently wrong: it is what a *file* does, and the provider already declared `verb: "Read"` with nothing reading it. `↗` stays in the label rather than the vocabulary — it means "this leaves the app", true of every pull provider and not a word any of them chooses. Still branches only on `delivery`, never on a provider's name; `DEFAULT_VOCABULARY` supplies `▶` so an unknown kind renders today's button rather than a gap.)* |
| 2026-08-17 | [The pool editor wears its provider's colour like every other view](2026-08-17-the-pool-editor-wears-its-providers-colour.md) *(bug / ui — `/channels/<id>`'s "▶ Play on ▾" came out in the app's Charcuterie indigo while the SAME pool's row on the Play landing and its grid at `/q/<id>` came out Plex amber. The 2026-08-15 colour rule was applied at three call sites and `ChannelsView` was the fourth, missed. Violet is not a neutral here, it is a CLAIM — that decision made the app accent indigo precisely so a provider colour means "belongs to that service", so a Plex screen wearing it reads as belonging to none. Keyed on `provider_kind` like everywhere else; the no-pool-selected branch stays bare because there is no provider to name.)* |
| 2026-08-17 | [A live refresh commits each endpoint independently, and a failed one retries](2026-08-17-live-refresh-commits-each-endpoint-independently.md) *(bug / architecture — "if I make a change on a tab, it doesn't get reflected on another tab". The SSE machinery was FINE: reproduced two-tab against a copy of the live config, the `data` frame landed in ~300 ms and the receiving tab really did re-GET both endpoints. `liveRefresh()` joined them in one `Promise.all` and committed the result together — and `/api/sets` is a 10 ms YAML read while `/api/queues` resolves every entry against Plex and Kavita in **7–9 s warm**, so every settings change was gated behind a full content resolve. Measured on a rename: **6.9 s → 0.8 s**. ⚠️ The latency was the mild half — `livePending` is cleared at the top and the 2 s retry timer only fires while it is SET, so ONE thrown fetch (Plex asleep, a dropped socket) discarded both halves and left the tab stale until the next SSE event or a tab focus. A config edit produces exactly one `data` event, so a second window sitting visible beside the first had neither: it stayed wrong indefinitely. The old comment said "the next event retries" and there was no next event. The 304 path and both `uiBusy()` checks are untouched, just per-endpoint now.)* |
| 2026-08-17 | [The third provider kind is named `board-game-picker`, not `board-games`](2026-08-17-the-third-provider-kind-is-named-board-game-picker.md) *(provider seam / naming — supersedes the NAME chosen on 2026-08-16; the seam and the configured-by-URL rule stand. The picker app was renamed end to end that morning (repo, Yarn scope, `BOARD_GAME_PICKER_*` env, SQLite filename, App-Configs directory) and this kind was the last `board-games` in the fleet. It is not merely internal — it is a **wire value**, written into `queues.yaml` as a set's `provider:`, into `sets.yaml`'s `provider_kind` and into the runtime artifact's `kind` — so with saved data behind it this would be a data migration. There is none: `App-Configs/queuepilot/` was grepped first and `queues.yaml` / `sets.yaml` / `config.yaml` / `profile-order.json` / `.history.json` / `cache.sqlite` contain **zero** occurrences. The kind shipped 2026-08-16 and no board-game queue was ever created. ⚠️ **No alias and no read-side fallback, deliberately** — a wire value has no deprecation window once something writes it, so an accepted-on-read `board-games` would have to be accepted forever, and it would let a half-migrated config keep working silently, which is the failure mode `config.ts`'s header strictness was bought to prevent. The unknown-kind path already covers the theoretical case: an unrecognised kind is KEPT rather than dropped and reports unsupported, so a hand-written `provider: board-games` fails visibly rather than being silently misread. Env names and the `Board Game Picker` label were already correct; the accent selector, both provider modules and the e2e harness move with the kind.)* |
| 2026-08-17 | [A channel sets its own lineup length](2026-08-17-a-channel-sets-its-own-lineup-length.md) *(feature — the kids' **Younger Kids · Shorts** card "stopped too early" and they had to re-scan it. `ROTATION_LENGTH` was ONE global 12 shared by every rotation channel, and 12 is ~4 hours of Shows but ~35 minutes of Shorts — the number was never wrong, sharing one number across cards of different **runtime** was. The 2026-07-27 split that created the Shorts card fixed "a shorts tap can't turn into a 25-minute episode" and carried the count over unchanged. A set now takes `length:` — the **SIZE** to `episodes`'s per-entry share, independent knobs, absent = env so no existing card moves on deploy. Resolution is set > env and deliberately **tolerant** (blank/0/negative/junk falls back rather than throwing): this is read at scan time on a card someone just tapped, so failing turns a YAML typo into a **dead card on the wall**, while falling back is visible and self-corrects. ⚠️ **`0` is not infinite and no number is** — `docs/todos/batch-all-or-infinite.md` (parked 2026-08-16) already settled that an infinite form needs a NAMED sentinel (`all`), because a falsy batch reads as *uncapped* in `resolve.ts`'s `applyBatch` and a typo would become a binge. Clamped to `ROTATION_LENGTH_MAX` (200) in **both** the writer and the engine, since `sets.yaml` is hand-edited over SMB as often as it is saved through the UI. The gate pins BOTH halves — that `loadSets()` carries the field AND that `rotationLength()` reads it — because a forgotten passthrough does not throw, it reads `undefined` and **silently disables the feature** (the failure that ran 12 profile-gated sets ungated). Rotation only: a curated queue's length is its entry count, Kavita's is follow-up. **"Play infinite" is deliberately NOT here** — a Plex playQueue is fixed once created, so infinite is meaningless without the top-up loop; phases 2-3 in [todos/lineup-length-and-top-up.md](../todos/lineup-length-and-top-up.md).)* |
| 2026-08-17 | [A collection's members are read (and cached) per account](2026-08-17-a-collections-members-are-read-per-account.md) *(bug / cache schema — extends the 2026-08-16 curated-queue profile decision, reported the evening it deployed: the tile said **E36** but *Start from…* said **154/155 watched**, and the episode picker marked all 153 watched including E36 itself. Those are Bob's numbers; Older Kids is **45/155** and **0/291** — the modal contradicted the tile it was opened from. `collectionChildren()` cached under `rk` alone and read with the admin token, so THREE per-account fields on every member row were the owner's: a show member's viewedLeafCount (the "N/M watched" chip), and a movie member's `watched` + `viewOffset`. ⚠️ The movie one decides what PLAYS, not just what prints — `collectionNext()` SKIPS a member whose `watched` is true, so a collection whose next member is a film someone else saw skipped it for everyone; the old code conceded this in a comment instead of fixing it. `collection_children` gains an `account` column to its PK (SCHEMA_VERSION 2→3, drop-and-recreate — it is a cache), joining `leaves` and `section_listing`, which had one already and whose own comment says "the account column is load-bearing". Both start-editor routes now resolve the profile SERVER-side through one shared `pickerScope()`: `?uuid=` wins for a rotation channel, else `?set=`'s `requires_profile` — deriving it from the set rather than teaching the queue grid to carry a uuid keeps ONE definition of who a queue plays as. `/api/show/:rk/episodes` already took both parameters and only ever used the explicit uuid, which is why the episode marks were wrong too.)* |
| 2026-08-16 | ["Completed" is judged when playback ENDS, not only when it starts](2026-08-16-completed-is-judged-when-playback-ends-not-only-when-it-starts.md) *(bugfix / architecture — reported live: **2001: A Space Odyssey** finished at 22:34 on Bob & Alice — Movies and the tile showed nothing. Plex had `viewCount: 1` and a history row a minute later; `/api/queues` said `done: false` for **all 21** entries. `queues.markDone` had exactly ONE caller — `session.startSession` — so the finished rule ran on a session START and nowhere else, and there is no periodic scan by design (HA owns schedules). ⚠️ **The badge has therefore been one card tap behind for every queue since the flag shipped 2026-07-21** — it only ever looked right because he happened to check after a later scan. Two evaluations added: the write side moves to `finished.ts` and RE-RUNS when playback ends (the existing now-playing bridge going `idle`, debounced 5 s, set read off the retained `queuepilot/state` so a restart mid-movie still reconciles) playing nothing; and `/api/queues` reports `isFinished` live from the engine's OWN movie rule — `select.watchedForSet` history (memoized per accounts×sections) plus one batched `/library/metadata/<ids>` read. That batch is not belt-and-braces: a movie tile's `viewCount` comes from the 7-day `resolved` cache and **nothing busts it** (the now-playing invalidation only drops a SHOW's leaves), so "In Progress" on a title-string entry could be a week old. Movies only — a series already says "All watched" through `nextEp: null`, and re-deriving its remaining episodes here would be the second rule this exists to avoid. `isCompleted = done || isFinished` drives the badge, the greying and the filter; **"Remove all completed" still keys off `done` alone**, because it deletes lines from the file.)* |
| 2026-08-16 | [Routing is real paths, not `#/…`](2026-08-16-routing-is-paths-not-hashes.md) *(architecture / frontend — implements the fleet decision `2026-08-16-owned-web-apps-use-react-router-with-path-urls`. The hand-rolled `location.hash` store (a `hashchange` listener, a module-level `currentHash`, a listener `Set` and a `useSyncExternalStore` shim, inherited from the vanilla app) becomes react-router v8 `<BrowserRouter>`: `/`, `/queues`, `/q/<id>`, `/channels[/<id>]`. The IA is untouched — only the URL shape moved. **`hasSpaFallback: true` is part of this decision, not an adjacent tidy-up**: the browser really requests `/queues` now, so without it the first reload/bookmark/pasted deep link 404s — and that comment had said `false` was "load-bearing" for the whole hash era. The two halves sit in different packages and neither package's gates can see the other, so `e2e/routing-test.ts` pins them together and runs on every PR with no Plex token (18/18 here; 8 fail against `main`). The 2026-08-15 anchor decision is **reaffirmed**: the links are `<Link>`s, which render real `<a href>`s — but a plain click now needs interception, because a bare `<a href="/q/1">` would refetch the whole app where `<a href="#/q/1">` WAS the navigation. Origin tracking survives as the one thing react-router does not provide, moving from structurally-unclobberable to a guarded render-time `trackRouteOrigin`. `parsePath` strips a trailing slash, newly reachable now that paths are real.)* |
| 2026-08-16 | [A curated queue plays as the profile it is gated to](2026-08-16-a-curated-queue-plays-as-the-profile-it-is-gated-to.md) *(bug / architecture — reported live: the new **Carol 1** queue (`requires_profile: Older Kids`) showed its Dragon Ball collection as **"Z (1989) · E109"**. That is where **Bob** is; Older Kids has watched 45/155 Dragon Ball and **0/291** DBZ, so the right answer was **"Dragon Ball (1986) · E36"** — a different show, 246 episodes earlier. `requires_profile` shipped 2026-07-25 as a play GATE and stores a display name and nothing else, so a curated queue (no `profiles[]`, no binding fields) got an EMPTY binding — wrong twice over: `watch_count_accounts: null` fell through to the admin-first env union that decision 2026-07-16 already reverted, and `user_uuid: null` read every `viewCount` under the owner's token. `/api/queues` separately passed a hardcoded empty `AccountScope`. ⚠️ **Invisible for three weeks because every existing curated queue is the owner's own** (`sawtaytoes`, plus two `Demo` reels with no watched state) — the admin answer was right by accident, and `carol_1` is the first queue gated to a kid. The gate promised *"a card can never play under the wrong account"* and was enforcing half of it. Now: the grid scopes tiles per set via `plex.profileScope()` (one lookup per distinct profile, not per tile), and the scan fills the binding through a new optional `Provider.profileBinding()` — the name→accountID join stays on the Plex side of the seam because that is a plex.tv fact. `[id]`, never a union. **An explicit rotation `profiles[]` binding is returned untouched**, or this re-opens the 2026-08-14 silent-no-play bug from the other end; the owner resolves to `{id: 1, uuid: null}` = today's admin token, which is why diffing every set's tiles prod-vs-patched changed `carol_1` and **nothing else**. Follow-up left out: `collectionChildren()` is still admin-only cached, so a collection's MOVIE member's watched flag and the start editor's "N/M watched" stay the owner's view — that needs an `account` column on the cache table.)* |
| 2026-08-16 | [The three Play groups are Filtered Pools, Curated Pools and Ordered Queues](2026-08-16-filtered-pools-curated-pools-ordered-queues.md) *(naming / ui — supersedes the 2026-07-21 Dynamic/Curated categories. "Channel" leaves the UI for good: the knobs ADR deleted the concept four days earlier but shipped as design-only, so the live app still said "Dynamic Channels". "Dynamic" named the wrong axis — the 2026-07-21 record said out loud it was naming rule-vs-hand-picked and then chose a behaviour word; **Filtered** is what the app's own meta text already says ("ratings-filtered"). **Pool** is the right noun for an unordered set of shows, which forces the third group to say what distinguishes it: **Ordered** Queues. The configurator becomes the **Pools** screen. Also corrects the "random rotation" tile meta, which advertised a mode Correction 1 of the knobs ADR proved does not exist — rotation shuffles only which show LEADS, then round-robins. Scope is user-visible strings only; the `#/channels` route, `channel-mode` class and `ChannelsView.tsx` stay.)* |
| 2026-08-16 | [`board-games` is a third provider kind, configured by URL](2026-08-16-board-games-is-a-third-provider-kind.md) *(provider seam — Board Game Picker queues games the way this app queues shows and chapters: member = game, unit = **play**, progress = the play log, delivery pull, `handoff` 302s into the picker's `/play/:gameId`. `materialize()` returns a DESCRIPTOR and builds nothing, because the picker owns no lineup object and a "tonight's list" inside it would be a second queue. Configured by BASE URL — `KINDS_CONFIGURED_BY_URL`, a named one-member exception: the picker issues no credential, and Plex/Kavita still fail loudly without one. The provider reads games and play timestamps ONLY and never `/api/collection`, which carries players, groups and who was at the table — this repo is public, and the offline suite records every request and asserts on it. Exposed three leaks above the seam: `sets.ts` held its own `PULL_KINDS = new Set(['kavita'])` that made a board-game queue a PUSH target with a "Play on <device>" button for a backend with no devices; `EntrySettings` abbreviated the unit with a binary that tagged a game "3 ch"; `tileFace` had no branch for a unit counting towards a total. `tiles()` now takes the entry refs too, because a game's remaining plays are a property of the ENTRY, not the game.)* |
| 2026-08-16 | ["Plays before the next game" is the batch knob, counted from `queued_at`](2026-08-16-plays-before-the-next-game-is-the-batch-knob.md) *(queue semantics — no new field: an ENTRY's `episodes` is how many plays that game owes, the QUEUE's batch is how many one Open spends (default 1 — a game night is one game), and a night never spills into the next game. ⚠️ Progress counts plays since `queued_at`, **never** the backend's lifetime total: the picker's play log goes back years, so Wingspan at twenty plays with a batch of three would be finished the instant it was queued. The launcher stamps `queued_at` only for a provider that declares `stampsQueuedAt`, so no Plex or Kavita queue grows a key nothing reads, and a hand-written entry is stamped on first read rather than read as "since the beginning of time". A finished entry stays `buckets()`'s answer — `logProgress()` records a play and stops, rather than racing a second writer over queues.yaml.)* |
| 2026-08-16 | [A picker next to text gets its gap from the parent](2026-08-16-picker-gap-is-the-parents.md) *(ui — "we still need a space between this listbox and the text". Picker is flush; the field row owns a 10px column-gap, the same gutter `.fieldselect` already had. Do not put margin on the control.)* |
| 2026-08-16 | [A Kavita reading list must not cross libraries](2026-08-16-kavita-reading-list-stops-at-a-library.md) *(bug / workaround — finishing two Webtoon chapters rolled into a manga that kept the Webtoon library's reader: scroll + custom width, not paginated 100%. Both series use `/manga/`, so Kavita does not remount. The profile resolver runs only on first open; `loadChapter` is `replaceState` + `init()`. QueuePilot now writes only the first library's run onto the list; Open again starts the next library as a fresh navigation. Upstream: [Kareadita/Kavita#4859](https://github.com/Kareadita/Kavita/issues/4859).)* |
| 2026-08-16 | [An entry count picker follows the set default, and tags that option Default](2026-08-16-entry-count-follows-the-set-default.md) *(bug / ui — a missing per-entry `episodes:` was coerced to 1, so a queue sitting at "Chapters per series each visit: 2" still showed 1 on every item. The picker now shows the **effective** count and tags the set-default option **Default**. Sparse write is "equals this set's default", not `<= 1`: picking 1 against a default of 2 stores `episodes: 1`. Same for `volumes:`. Tile tags and the overrides filter only fire on a stored override. Also wires the `badge` StartModal already passed, so Watched chips finally render.)* |
| 2026-08-16 | [Kavita progress is read on demand — it is not pushed](2026-08-16-kavita-progress-is-read-on-demand-not-pushed.md) *(bug / architecture — tiles snapshot Kavita `pagesRead` when `/api/queues` actually runs; there is no webhook and no poll. The ETag is YAML + generation, so marking a chapter read in Kavita never busts it. `must-revalidate` made F5 send If-None-Match and 304 the stale "Ch 35 / In Progress". `no-store` + forced GET on tab-focus / reconnect / refreshData. Launch was already live.)* |
| 2026-08-16 | [A volume is not a chapter — it has its own read count](2026-08-16-a-volume-is-not-a-chapter.md) *(data model / playback — a volume is a collection of chapters, so the queue's `episodes:` / "Chapters per series each visit" must not apply to a volume-based series. Volumes get `volumes:`, precedence entry > set > **1**. Default is 1, never the chapter count, never `KAVITA_BATCH_DEFAULT`. The live reading queue at 3 chapters was dumping 3 whole manga volumes into one visit. Engine picks by the item's `unit`. Set editor on a reading queue grows a second count.)* |
| 2026-08-16 | [Copy is authored in Plex words and rewritten per provider](2026-08-16-copy-is-authored-in-plex-words-and-rewritten-per-provider.md) *(architecture / ui — extends the vocabulary ADR. Leftover complete sentences (`StartModal` hint, its "could not read from Plex" note, the tile-menu "Start from an episode…") are written once in Plex words; `applyVocab` swaps episode/chapter, watched/read, Plex/Kavita, Play/Read, Playback/Reading from the same `ProviderVocabulary`, plus a new `name` slot. Identity on Plex's words is a no-op. `a`/`an` is repaired so "an episode" becomes "a chapter". Also: `/api/show/:id/episodes?set=` dispatches to the set's `listUnits`, because the live Kavita picker was empty — it had been asking Plex about a Kavita series id. A stored start floor is honoured on launch the same way Plex already did.)* |
| 2026-08-15 | [The reading list is REBUILT on launch, not appended to](2026-08-15-the-reading-list-is-rebuilt-not-appended.md) *(bug — the owner's live list held **23** series, up from 12 that morning: *"It's got stuff in here I absolutely did not add. are we auto-adding anything?"*. TWO causes, only one of them the same-day entries-vs-libraries fix (which was merged but **not deployed** when he looked). This is the other: `materialize()` found the existing list by title and **appended**, forever — so the list is the UNION of every lineup ever built for that set. Independent of the first bug: even a perfectly correct lineup accumulates the whole library over a few weeks once the shuffle picks a different twelve each launch. ⚠️ **The method's own docstring already claimed the fixed behaviour** ("Rebuilt on launch… we reuse one list per set") — the code implemented the second sentence and not the first, and "reuse the list" got read as "find it and add to it". ⚠️ **The gate was true and insufficient**: it asserted `readingListId === 42` and `lists.length === 1`, which stayed green forever because the fault was never about how many lists exist but about what is IN one — *assert on the artifact's contents, not its identity*. Items are removed rather than the list deleted+recreated, because its id is user-visible (`/lists/153`); verified live that the id survives. `POST /api/ReadingList/delete-item` with the **ITEM's** id, not the chapter's — probed live on a throwaway list, since the surrounding client methods carry a comment saying they were spec-written and never exercised; the `DELETE`-with-query-params spelling is a 404. The new gate **fails on the pre-fix code** (checked by restoring the old file), and pins that every delete happens BEFORE the first add — clearing afterwards would erase the launch's own lineup. The live list self-heals on the next launch after deploy; purging it sooner is pointless because the running build would refill it)* |
| 2026-08-15 | [A queue wears its provider's colour; the app's own accent is Charcuterie's](2026-08-15-a-queue-wears-its-providers-colour.md) *(ui — the stylesheet hard-overrode the accent family to Plex amber for the WHOLE app, justified as "it reads as Plex on purpose" — true while Plex was the only backend, a false brand claim the moment it wasn't: a Kavita queue's button came out in Plex's colours. `:root` now inherits Charcuterie `daylight`'s indigo and each queue paints its own provider over it via `[data-provider]`; chrome belonging to no queue stays neutral. Scoped on the provider's **kind, not its id**, so a second Kavita added from the connector surface is still green. Kavita's `#4AC694` was read LIVE off `kavita.example.com`'s own stylesheet, not invented. Three options were rendered in the real app (only the accent variables differing) and served over `devshare`; the owner picked **B — the whole family follows the provider**, over his own stated leaning toward solids-only, which is exactly why the mock-then-pick loop exists — the two are identical on the landing page and diverge on the Queues page. Full seven-role override per provider, never a subset. ⚠️ **Kavita's own UI puts WHITE on that green at 2.1:1** and fails; on-solid ink stays near-black like Plex's, and light mode needs `#25624A` for TEXT while the fill stays `#4AC694`)* |
| 2026-08-15 | [A provider carries its own vocabulary — "Play" is not a universal verb](2026-08-15-a-provider-carries-its-own-vocabulary.md) *(architecture / ui — a provider declares `{verb, unit, units, member, done}`, served on `/api/providers` and carried on every SET beside `delivery` so no screen has to re-join a set to the provider list. `delivery` already existed and was already right (the queue button said `Open ↗`), and that is the instructive part: **`delivery` is how a lineup STARTS; the vocabulary is what the medium is CALLED**. A screen with only the first still says "Play “The Sword-Eating Swordmaster” now", asks a reading channel how many *episodes each show plays*, and tags a manga entry `3 eps`. An unrecognised backend gets Plex's words, so a stale registry degrades to today's rendering, never to `undefined`. `MediaUnit` gains `volume` as a PER-ITEM refinement of `chapter` — one Kavita library holds volume-based manga beside chapter-based webtoons, so the provider declares `chapter` and an item corrects it. Sentence templates use neutral verbs ("comes up", "contributes") so only the NOUNS vary)* |
| 2026-08-15 | [A curated reading queue is its ENTRIES, not its libraries](2026-08-15-a-curated-reading-queue-is-its-entries.md) *(bug / architecture — the live "Manga & Webtoons" queue holds **93** hand-added entries and its Kavita reading list came back holding **12** alphabetical series off the library shelf, exactly ONE of which had been added on purpose. `launchDescriptor()` passed only `libraries` and `kavita.buckets()` enumerated `seriesForLibrary()`; nothing ever opened `queues.yaml`. `entries` now beat `libraries` (a rule-based channel with no entries is unchanged) — which is also why `block.batch` and the per-entry `episodes:` had been dead: there was no entry list to hang them on. The `ROTATION_LENGTH` cap is what hid it: 12 plausible series looks like a working feature. Also fixes, found in the same queue: **a volume-based manga read as fully read** — Kavita puts NOTHING in `series-detail`'s `chapters`/`specials` for one (verified live: `chapters: 0, specials: 0, volumes: 9`), so "Alice in Borderland" at 0 of 328 pages rendered **"All read"**. ⚠️ A chapter-based WEBTOON returns the same chapters in BOTH places, so the union must **dedupe by chapter id** or every webtoon chapter queues twice; and every chapter of a volume-based series carries Kavita's `-100000` no-subdivision sentinel, so the sort is (volume, chapter) and the item presents as `Volume 1`, never `Ch -100000`. This is also the "97 vs 103" discrepancy `pool()` had already noticed and mis-attributed to "chapters reporting 0 pages")* |
| 2026-08-15 | [A queue sets its own batch; and the search box accepts a year](2026-08-15-a-queue-sets-its-own-batch-and-the-search-box-accepts-a-year.md) *(feature / bug, two reports from one pass — **(1)** a set-level `episodes:` is the COUNT to `batch_stops_at`'s WHERE, precedence entry > set > env, and `QUEUE_SERIES_DEFAULT` **stays 1**: the owner said it twice, unprompted the second time — *"3 chapters should not be the default, 1 chapter should be. I wanna be able to configure this per-queue."* One line in `nextQueue` (`resolveMember` has taken a `defaultBatch` since the port). Stored sparsely and clamped, so an untouched queue is byte-identical. An unusable value falls through to the env default, never to "uncapped" — `applyBatch` reads a falsy batch as UNCAPPED, so a typo would dump a whole series into one scan. **(2)** `Tekkaman Blade (1992)` returned ZERO results where `Tekkaman Blade` returned two — and `Title (Year)` is the app's OWN entry format, printed on the very tile under the box, so he was typing back what the app had shown him. The year is now split off for MATCHING and kept for RANKING, never filtering: measured live, the bare title ranks `Tekkaman Blade II` FIRST, so discarding the year would leave the right answer in second place on the query that named it. Reuses `parseTitleString()` rather than a second regex, so box and storage cannot drift — and a pasted `[anidb-16172]` suffix works for free)* |
| 2026-08-15 | [One source per queue — "+ Add another source" is removed](2026-08-15-one-source-per-queue-so-the-add-source-button-is-gone.md) *(ui / scope — supersedes the clause of the 2026-08-13 single-provider decision that KEPT the button for "two Plex profiles on one queue". Checked before answering: that case is not implemented. **Nothing reads a block's `profile`** — only `resolveSingle().provider` and `.libraries` are consumed, and `block.profile` appears solely in serialization — while the editor writes the set-level `requires_profile` from a state variable no control on the multi-block path ever sets. So a second block did exactly one thing: union its libraries into the same `sections` list, which more checkboxes on the first block already do. Storage stays a LIST (nothing migrates, nothing is rewritten) and existing multi-block sets still render with their Remove buttons; only the UI claim goes. Two Plex profiles per queue, if ever wanted, is engine work behind the golden parity corpus and deserves its own gates)* |
| 2026-08-15 | [A badge intent means exactly one thing, and TYPE is not a badge](2026-08-15-badge-intent-means-one-thing.md) *(frontend / visual language — a plain show or movie renders NO chip (the poster already said so), and each intent then means exactly one thing: **green = Completed**, **amber = In Progress**, **blue = Now playing** (the app's only `solid` badge, so it never needed the strongest colour), **red = Not in library**, **grey = every COUNT or NAME** (`N watches`, `N unwatched`, the Collection chip). Fixes the owner's "why is Collection a *success*?": FOUR unrelated axes were painted from ONE palette, so green meant "collection" on one chip and "now playing" on the next, and amber meant "series" here and "in progress" there. The type axis was what consumed the palette the state axis needed — so it had to go, not merely be recoloured. Survivors: **Not in library** (availability, not type) and the two-part **Collection** chip (it carries the collection NAME, which the tile has nowhere else). Four candidates were BUILT and served over `devshare`, not described; the owner picked A's colours with C's removal. **⚠️ The collection chip's colour is written in TWO places** (`.badge.collection:has(.badgename)` and `.badge .badgekind`) — the first pass changed one and left a green chip in a neutral row. **⚠️ Part of this shipped early by accident** in PR 87, whose message mentions only grid columns)* |
| 2026-08-15 | [A `done` entry revives whenever there is something to play — and a finished collection says "All watched"](2026-08-15-a-done-entry-revives-when-there-is-something-to-play.md) *(bugfix — `done` is a cache of an EMPTY resolution, so a done entry that now resolves to anything playable is stale: revive + `clearDone`, not just when the head is mid-playback. The 2026-08-07 `viewOffset > 0` test encoded the shape of one bug (the Prison School OAD) rather than the invariant, and left a returning show stranded — bob_anime's "Trapped in a Dating Sim" collection was marked done at S2E6 and would have stayed greyed and unplayable when S2E7 aired, forever: nothing else clears the flag and the TTL sweep defaults to `never`. A HAND-marked `done: true` is exempt from the new half — it carries no `done_at` (only `markDone` writes one), which is what marks it a deliberate skip; the live `Frieren` entry is exactly that, and would otherwise have jumped back into the channel. Also: a finished collection's tile now reads "All watched" / "All read" instead of `"N in order"` — the same state a finished show describes, and the size label is what an UNRESOLVED tile shows, so the one tile that had genuinely finished looked identical to one that failed to resolve. `resolveTile` reports `isNextEpFailed` so "nothing left" and "the lookup errored" stop being the same `null`, which also stops a show whose lookup failed from claiming "All watched")* |
| 2026-08-15 | [Anything that navigates is an `<a href>`, not a `<button onClick={navigate(…)}>`](2026-08-15-navigation-is-an-anchor-not-a-button.md) *(frontend / a11y — the Play landing's channel + queue names, its three "Configure ›" controls, the Queues shelf titles and the header back control become real anchors, so middle-click / ⌘-click / "open in new tab" / "copy link address" work. Routing is `location.hash`, so a plain click needs NO handler — setting the hash is what `navigate()` did; where a click had a side effect (the shelf stamps `homeScroll.y`) the handler stays and is deliberately NOT `preventDefault`ed. The owner recalled a Link component and was right: `@charcuterie/ui` ships `ButtonLink`, whose docstring names THIS app's "Configure ›" as the motivating example — and this app had never adopted it (`grep "Link" web/src/` returned nothing). Plain `<a>` rather than `ButtonLink`/`TextLink` because none of these four is a Button or link-text, and adopting a Charcuterie control means deleting its skin — which would turn "let me middle-click this" into a redesign of the landing. **Trap:** `.ghost` was written `button.ghost`, tag-qualified, so `<a class="ghost">` matched nothing and fell back to bare link text; any other `button`-qualified class here is the same landmine. Anchors are natively draggable — `homedrag-test` re-run and green)* |
| 2026-08-15 | [A modal's footer owns the space above it](2026-08-15-a-modal-footer-owns-the-space-above-it.md) *(frontend — `.modalbtns` gets `margin-top: 14px` on all four modals. There was NO rule reserving that space: `margin-top`/`padding-top` were 0 everywhere, and the three modals that looked right were only borrowing the 14px bottom margin of the `.idnote` line that happens to sit last in their bodies. `#entrymodal` has no `.idnote`, so its body ended flush against the footer — invisible while Done sat left under a short text line, and an obvious collision the moment Done moved right and landed directly under the start-point row's "Choose…". `.idnote` gives up its bottom margin so the other three are pixel-identical (measured 14/15px before and after); only `#entrymodal` moves, 0 → 14px)* |
| 2026-08-15 | [Tile controls are QUIET, and in `cards`/`list` they sit beside the poster, not on it](2026-08-15-tile-controls-are-quiet-and-sit-beside-the-poster.md) *(frontend — `.check`/`.remove` are transparent until `:hover`/`:focus-within` (and always on under `@media (hover: none)`, and always on a `.selected` tile, because a ticked ✓ is state not an affordance). In `cards` and `list` they stop overlaying entirely: those densities become four-column grids `✓ | poster | text | ✕` so neither control ever covers artwork — a 28px ✕ on a 40px row thumb IS the poster, and the earlier `transform: scale(.75)` only made a smaller thing sit on the same art. `posters` and the member grid keep the overlay. "Rows" is relabelled **List** (label only; the stored value stays `rows`, so persisted densities and every `ul.grid.rows` selector hold). Adds a hover ▶ per tile — which DOES stay on the artwork, deliberately, as Plex does — plus **▶ Play on ▾** at the head of the selection bar when exactly one entry is picked; both open the ordinary device menu. Playback narrows the ENTRY LIST at `provider.buckets` before the resolver, so a one-entry start is the normal start with a shorter list (same next-unwatched, same batch, same resume), and a finished/deleted entry plays NOTHING rather than falling back to the queue's head. **Costs** the 2026-08-14 "CSS-only over a fixed DOM" guarantee: `.check`/`.remove` moved out of `.thumb` to become its siblings — same classes, same tile, different tree. Gate: `e2e/play-one-entry-test.mjs`)* |
| 2026-08-15 | [The scrollbar gutter is reserved, so opening a modal cannot reflow the page behind it](2026-08-15-the-scrollbar-gutter-is-reserved.md) *(frontend / layout — `html { scrollbar-gutter: stable }`. Both `html.modal-open` and `@charcuterie/ui`'s `lockScrollBehind` freeze the page with `overflow: hidden` on `<html>`, which on a CLASSIC scrollbar deletes it and hands ~15px back to the layout viewport; against `repeat(auto-fill, minmax(340px, 1fr))` those 15px are a whole extra column, and the shorter 3-column document then CLAMPS the scroll position — so "the formatting changes" and "the scroll position resets" are one bug seen twice. Reserving the gutter makes the layout width invariant under the lock, which needs no scroll save/restore and no assumption about whose effect runs first (Charcuterie's `Modal` is a CHILD, so its effect fires before the app's — the app cannot snapshot `scrollY` ahead of the lock). Verified against `narrow-scroll-test` at 390px/320px. ⚠️ **The sandbox's headless Chromium uses OVERLAY scrollbars, so this bug is invisible to Playwright at any viewport** — reproduce by stepping the width 15px and watching the column count, not by opening a modal)* |
| 2026-08-14 | [The server is Hono + TypeScript, like the rest of the fleet](2026-08-14-the-server-is-hono-and-typescript-like-the-rest-of-the-fleet.md) *(architecture — extends the 2026-07-31 frontend decision to the other half of the app, which it never covered: `web/` was already on `@charcuterie/tsconfig`/`vite-config`/`vitest-config`, while `server/` was 35 files and ~10k lines of untyped Express whose only CI gate was `node --check`, a parser run over the code that decides what plays. Seven sibling apps had already converged on Hono + `@charcuterie/server`; queuepilot was the last SPA-serving app on Express and the only one still hand-rolling a static handler — `staticCompressed` covered `/assets/` for `.css`/`.js`/`.svg` only, a private reimplementation of the package written to delete exactly that. Dev runs `tsx`, prod runs an esbuild bundle under plain `node` (castkit's locked "never tsx in prod"), and the runtime image carries no `.ts`/tsx/typescript/esbuild. **Deliberately out of scope:** the Yarn-workspace migration the other apps have — queuepilot is npm with no root manifest, `web/` already consumes `@charcuterie/*` from public npm, and doing both at once doubles an already-large diff. **Translation, not refactor:** `e2e/fixtures/golden/` proves this engine matches the retired Python bit for bit, so five latent bugs found on the way — a numeric HTTP status colliding with Node's string errno `code`, an unguarded optional `profileToken`, Kavita items missing the `ratingKey`/`season`/`episode` `session.ts` reads, a `payload.target` that is a string or an object by caller, and three env vars read outside `env.ts` — were ENCODED IN THE TYPES with comments rather than repaired)* |
| 2026-08-14 | [A weight is SLOTS PER ROUND, not odds](2026-08-14-weights-are-slots-per-round-not-odds.md) *(feature / playback semantics — any entry a randomized set draws from can carry `weight: 1-20`, meaning how many slots it takes per round; 1 is never stored. Three carriers, each written like its neighbour: a queue entry's own key, a member mapping's key, and a channel's `weights: {ratingKey: n}` map (the mirror of `starts`). Slots not odds because a rotation exists so consecutive items are DIFFERENT shows — a 3/1/1 die deals the same show three times running, which is what the round-robin was built to prevent. Smooth weighted round-robin (nginx's): 3/1/1 deals A B A C A. With every weight 1 the interleave IS the old round-robin, asserted against the old loop kept as an oracle, so an unweighted channel cannot change. Two exceptions where a weight is odds: a curated CHANNEL (plays each member once, so a weighted shuffle) and the rewatch card (one film, so it multiplies the 1/n² bias). **Also fixes:** `nextQueue` only shuffles when handed an rng and the provider never handed it one — every curated channel had played in file order since the Python retirement)* |
| 2026-08-14 | [Entry settings are TAGS plus one panel, and the queue view has a density](2026-08-14-entry-settings-are-tags-plus-a-panel.md) *(frontend — supersedes the always-on per-tile `episodes` and `batch_stops_at` dropdowns. A default entry now shows nothing; every tag (`3 eps`, `3x as often`, `Ends at season`, a start point) is a deviation, and one panel edits all four. A View control switches Posters / Cards / Rows over the SAME `li.tile` DOM (CSS-only reshaping, so every e2e selector holds). Filtering a queue is split from searching Plex: the add box filters by type/library and calls out hits already in the queue (picking one jumps to it), while a new toolbar row filters what you already have — including "Completed / fully watched" for pruning passes — always showing "showing N of M" + Clear filters. Counts read `1 / 2 / Custom…` with a real number field, clamped to the engine's own cap. Density + filters persist per queue. Bulk episodes/weight/batch-stop/reset in ONE request, from a bar flush to the bottom edge. Mock-then-pick: the owner reviewed three served layouts and chose C's cards with B's tags)* |
| 2026-08-13 | [A queue draws from exactly ONE provider — mixing is not a goal](2026-08-13-a-queue-draws-from-exactly-one-provider.md) *(architecture / scope — **closes** the open "what does a mixed queue hand off" question rather than answering it: the owner scoped it away while live-testing (*"I only really care about Plex and Kavita for now, and it's either-or for me"*). `materialize`/`handoff` stop being ambiguous, and `resolveSingle()`'s throw + the launcher's 501 become the RIGHT behaviour instead of a provisional refuse-to-guess. Storage deliberately stays a LIST of blocks — two Plex profiles on one queue is not mixing and still needs N blocks, so `isMixed()` is what's forbidden, not `blocks.length > 1`. Jellyfin/Emby/Kodi are what the seam permits, not what anyone is asking for)* |
| 2026-08-13 | [`SelectListbox` adopts the shared Charcuterie `Picker`](2026-08-13-selectlistbox-adopts-the-shared-charcuterie-picker.md) *(UI / dependency — the hand-assembled trigger (`useVisibility` + `Button` + `Listbox` + chevron) becomes a thin adapter over `Picker`, new in `@charcuterie/ui@2.15.0`; requires `logic@^1.5.0` too, since the `ui` barrel now pulls `QueryBuilder`. Extends — does not reverse — the 2026-08-07 Listbox decision: only who assembles the trigger changes. Name and props kept, so all NINE call sites are untouched. Fixes a WCAG 2.5.3 failure ours had drifted into: the accessible name is now `"Add to: Top (plays next)"`, not a bare `"Add to"`. Renders are BYTE-IDENTICAL before/after in both states. Keeps two app-owned details: `data-value` inside option labels (what `e2e/pick.mjs` picks by) and the `id`→`data-testid` swap. ⚠️ The `pick.mjs` contract has NO CI coverage — the Playwright step is Plex-gated and skipped on every PR — so it was verified by hand against a scratch harness, which was then promoted to a permanent no-Plex CI step (`e2e/pick-contract-test.mjs`, 11 assertions on every PR))* |
| 2026-08-13 | [A PR merges as soon as CI is green](2026-08-13-prs-merge-as-soon-as-ci-is-green.md) *(process — repo-local restatement of the workspace decision "the AI merges its own PRs, the owner never merges"; green `ci` is the only gate. Squash + delete branch, and merge over `gh api -X PUT …/merge` because `gh pr merge` trips `'main' is already used by worktree` in this concurrently-worktree'd repo — a failure that reads like a merge block and isn't. Merging only builds/pushes the GHCR image; the live app still needs a deliberate `app.redeploy`, so green-merge cannot move the family's cards. Exception: an explicit hold)* |
| 2026-08-13 | [The provider block repeats, and its control is chosen by measurement](2026-08-13-provider-block-repeats-and-picks-its-control.md) *(ui / architecture — the whole `{provider, profile, libraries}` block repeats N times; mixing falls out of composition rather than out of a multi-select control. The owner withdrew his own Listbox/Combobox call, so the control went through the mock-then-pick procedure and came back **"Option B if they fit; otherwise Option A"** + **"C1 is good when only 1 provider"**. "If they fit" was MEASURED against the real `#setmodal` box, not eyeballed: segmented needs 264px at two providers (fits desktop AND a 390px phone), 371px at three (fits desktop, **overflows the phone**), 470px at four; `SelectListbox` is a flat 258px at any count. So the rule is evaluated at RUNTIME — providers are added from the connector surface — as none / segmented / listbox at 1 / 2 / 3+. One provider renders no control and no "Add another source" at all, so every existing Plex-only install sees no change whatsoever. The profile field is provider-scoped and branches on `delivery` (`push`|`pull`), never on the provider's name: "Plays under profile" + the Shield copy for Plex, "Reads as user" for a pull provider, where building a list as the wrong account fails SILENTLY. A single Plex block still writes through the legacy `sections`/`requires_profile` fields so an existing set is byte-identical on disk after an unrelated edit, and `sections` is kept in sync with the Plex blocks so the editor and playback can never disagree. What a MIXED queue hands off remains undecided and still refuses to run)* |
| 2026-08-12 | [`plex-channels` becomes **queuepilot**](2026-08-12-plex-channels-becomes-queuepilot.md) *(naming / product — `queue` is the data model, `pilot` is both autopilot and a first episode; free on npm/PyPI/GitHub (the HYPHENATED `queue-pilot` is taken on npm). The old name was misleading twice over: no longer Plex-only (Kavita verified, Jellyfin/Emby/Kodi wanted), and "channel" collides with real Live TV features in those same backends. Rejected + why: `tuner` (HDHomeRun OTA hardware in all three), `rabbit-ears`, `cue-stack`, the omakase family (DHH-branded AND inaccurate — nobody else is choosing), anything on `list` (Playlist/Reading List). Carries the full rename checklist, incl. three traps: `T_CMD_CAST_PLAY` is hardcoded publisher-side, `DISCOVERY_OBJECT_ID` renames `sensor.plex_channels_status`, and `PLEX_CLIENT_IDENTIFIER` must NOT change. **Decided, not executed**)* |
| 2026-08-12 | [A queue is ONE object with orthogonal mode knobs — not a named "queue vs channel" type](2026-08-12-queues-have-orthogonal-mode-knobs-not-named-types.md) *(data model / playback semantics — **supersedes the 2026-07-21 taxonomy** (point 1 only; its play-first IA and the anime rotation+batch behaviour stand). Five enum/count knobs — `pick_order` / `repeats` / `repeat_scope` / `batch` / `stop_after` — express all five requested behaviours as combinations, including the reading case (`rotate` + `batch 3`) with no new concept. **Corrects the record twice:** a channel is a strict ROUND-ROBIN, not random (the shuffle only picks which show leads); and it does not play forever — the lineup caps at `ROTATION_LENGTH=12` but `continuous: 1` lets the Plex CLIENT append its own Up Next. So "one each then stop" is reachable today via `max_items`; "stop after one pass" is not, because `max_items` is a count, not a pass. Design only)* |
| 2026-08-12 | [Backends are providers behind a media-neutral seam](2026-08-12-backends-are-providers-behind-a-media-neutral-seam.md) *(architecture — the engine's `client` (`container(path, token)` → a Plex `MediaContainer`) is Plex's wire format as an interface, which Kavita could only implement by emulating it. Widens at the same injection point into `buckets` / `progressState` / **`materialize` + `handoff`** — the split that makes Kavita fit, since Plex pushes a playQueue while Kavita returns a 302 URL. `buildRotation` + `routing.js` are already backend-neutral and reused as-is; `buildRotation` also gains the `batch` knob. **Corrects the drafted sequencing:** it assumed the port was parked at D2 behind `ENGINE=python`, but the port is CLOSED — `queue_builder/` is deleted, so the two-engine hazard cannot occur and the work is unblocked now. Design only)* |
| 2026-08-12 | [Provider definitions go in plaintext config; provider TOKENS go in a separate `/config` file](2026-08-12-provider-tokens-live-in-a-separate-config-file.md) *(architecture / security — connector definitions (id, label, kind, base_url) stay plaintext beside `sets.yaml` so they stay diffable and reviewable; tokens live in `/config/providers.secrets.yaml`, 0600, write-only in the UI, excluded from the undo-history and `.bak-*` machinery, resolved `env > file > NOT CONFIGURED`. An unset token must FAIL LOUDLY — the two placeholder outages (2026-08-06, 2026-08-10) both shipped a working-looking app that talked to nothing. **Corrects the drafted mechanism:** `SECRET_PATHS` is the agent-SANDBOX container's allowlist (`agent-sandbox-base/entrypoint.sh:56`) and does not exist for this TrueNAS app — only the *principle* transfers. Design only)* |
| 2026-08-12 | [Python is deleted — except the cast sidecar](2026-08-12-python-is-gone-except-the-cast-sidecar.md) *(architecture / cleanup — completes the 2026-08-03 retirement plan: `queue_builder/`, the Python tests, the corpus generator, the live-corpus recorder and the `ENGINE`/`PLAYBACK_ENGINE` switches are gone; Node is the only implementation. `cast_sidecar/` stays (owner's call) because `playback.js` delegates `PLAYBACK_MODE=cast` to pychromecast, so the image keeps python3 + 3 packages. The four Node↔Python parity gates keep running against the deleted engine's RECORDED answers in `e2e/fixtures/golden/` — a contract, not a snapshot to refresh — and the seven Python-only engine tests were ported to `.mjs` 1:1 via `node:module` resolve-hook stubs. Casualty: the unused soundtrack resolver)* |
| 2026-08-12 | [A multi-episode batch may be stopped at a season / member boundary (`batch_stops_at`)](2026-08-12-batch-stops-at-season-and-member-boundaries.md) *(feature — the batch cap was a plain COUNT, so `episodes: 2` at a season finale queued S1E12 + S2E01, and inside a `Collection:` it queued show A's finale + show B's episode 1. Adds an orthogonal knob: the count says how many, `batch_stops_at` says where the batch may END — `none` (default, unchanged) / `member` / `season`. Per-set in sets.yaml, per-entry override in queues.yaml, entry > set > global. Only ever shortens, never below ONE item (an empty batch is the FINISHED signal). The uncapped rotation callers are untouched. Gates: `e2e/batch-stops-at-test.{py,mjs}`)* |
| 2026-08-11 | [A `Collection:` entry contributes ONE batch, exactly like a show entry](2026-08-11-collection-entries-contribute-one-batch-like-show-entries.md) *(bugfix — `resolve_member`/`resolveMember` sliced the show branch to `episodes \|\| default_batch` but returned `collection_items()` raw, so a collection emptied its children's whole unwatched run into one scan while a plain show entry beside it yielded one episode. The anime channel built a 12-item "rotation" of 9 consecutive Chaika + 2 Nadesico + 1 Gleipnir; after the fix the same 12 slots hold 12 distinct shows. Capped EVERYWHERE (episode and movie collections alike, no per-type special case); the rotation/member-bucket callers pass no default batch and stay uncapped. Clarifies — does not reverse — the 2026-07-21 collections decision, whose stated intent was "the same footing as show entries". Gate: `e2e/collection-batch-cap-test.mjs`; a Node↔Python parity gate structurally cannot catch this class)* |
| 2026-08-08 | [Curated queue flags (`keep_completed` / `reel` / `remove_completed_after`) are editable in the Set editor via Charcuterie Checkbox](2026-08-08-set-modal-queue-flags.md) *(frontend + server — SetModal gains a Playback & completion fieldset; API normalize/create/update round-trip the three knobs for curated queues only; requires `@charcuterie/ui@^2.6.0`; supersedes the "Web UI needs no change" clause of the keep_completed decision for the flags themselves)* |
| 2026-08-07 | [The leaves cache re-validates on read against the show's live watch aggregate](2026-08-07-leaves-cache-revalidates-on-read.md) *(bugfix / caching — `plex.allLeaves` now validates cached episodes against the show-node `(updatedAt, viewedLeafCount)` every read, so an episode finished OUTSIDE the app's flow (manual Plex play / another client) self-heals the tile's next-up instead of staying stale up to the 24h TTL. Root cause: the display path passed no validator AND the `allLeaves` container omits those fields, so stored `viewedLeafCount`/`updatedAt` were always 0. One light show-node call (no episodes); Plex-down → the old TTL path unchanged. Real case: `.hack//SIGN` stuck on E19 In-Progress after a manual finish. Regression test `e2e/leaves-revalidate-test.mjs`)* |
| 2026-08-07 | [Curated queues can choose their `requires_profile` in the web editor](2026-08-07-choose-profile-for-queues.md) *(frontend + server — a "Plays under profile" select in `SetModal` writes the play-gate; options from `/api/profiles` with owner→plex.tv username, managed users→title, "Any"→ungated; rotation channels reject it since they are profile-driven)* |
| 2026-08-07 | [plex-channels pickers are a themed `Listbox`, not the native `Select`](2026-08-07-plex-channels-pickers-are-listbox-not-native-select.md) *(UI / component choice — EVERY picker becomes a Charcuterie `Listbox` via the `SelectListbox` adapter (tier picker, channel/profile, Add-to position, move-to, eps-per-play, and the in-modal set-kind/behavior/plex-profile/start-series·season·episode); reverses the app's native-`Select` default now that `@charcuterie/ui@2.x` ships `Listbox`. Required rebuilding the app `Modal` on Charcuterie's body-portalled base `Modal` (was native `<dialog>`+showModal, whose top layer occluded the dropdown) — API/DOM preserved, `[open]`→`data-open`. e2e drives via `data-testid` + `[role=option] [data-value]`)* |
| 2026-08-07 | [A channel can name a default profile the Play/Channels dropdowns start on](2026-08-07-default-profile-per-channel.md) *(UI / data model — a rotation channel's top-level `default_profile` (a binding's `plex_user`) seeds the Play row + Channels dropdown so it plays the right tier without re-picking; set from the ⚙ Configure editor when ≥2 bindings, blank clears it. A pure UI-seed hint: the Python engine ignores it and still plays what the play menu passes; a stale/renamed value falls back to `profiles[0]`)* |
| 2026-08-07 | [Episode counts exclude trailers/OP-ED by Season-0 index range; only real episodes + regular specials + "other" count](2026-08-07-specials-count-excludes-op-ed-trailer-extras.md) *(refines 2026-07-17 — one shared predicate `isExtraOrPromo`/`is_extra_or_promo` excludes Season-0 leaves at index 200–399 (trailers/OP-ED) plus Plex clips/extraType, keeping index 1–99 specials + 400+ "other". Node counts computed from `allLeaves` (missing `viewCount` = unwatched); `collectionNext` now advances past an ED-only series (Saiki → S4 E1). Deterministic by index — NOT duration/title. ⚠️ index 100–199 unspecified → INCLUDE, owner to confirm; composes with PR #13's `_keep_episode` specials-only keep)* |
| 2026-08-07 | [An in-progress queued item is never "finished" — and a stale `done` is revived](2026-08-07-in-progress-queue-items-are-never-finished.md) *(bugfix — a queued item whose next-up leaf/movie is mid-playback (`viewOffset>0`, `viewCount` 0/absent) must never read finished/done: missing `viewCount`=unwatched everywhere, a specials-only OAD stays playable, an in-progress leaf survives a watched-history hit, a stale `done:true` is revived + cleared (`queues.clear_done`) before the TTL sweep, shuffled anime channels lead with it, and the web tile shows an "In Progress" badge. Root case: the "Prison School: Mad Wax" Season-0 OAD read "Completed" while mid-episode)* |
| 2026-08-07 | [A `keep_completed` queue flag makes a set non-consuming (a re-showable playlist)](2026-08-07-non-consuming-keep-completed-queue-flag.md) *(feature — `source: queue` sets may set `keep_completed: true`; `plex.next_queue` then never marks entries `done`, so the whole lineup re-shows every scan (e.g. the Demo Reel). Decoupled from `reel`, which governs play-all-every-scan; `reel: true` implies `keep_completed`. Writes no state → inherently sweep-exempt; web "Completed" badge mirrored by construction)* |
| 2026-08-07 | [Completed queue entries auto-remove after a TTL (kept, then swept)](2026-08-07-ttl-auto-remove-completed-queue-entries.md) *(feature — `mark_done` stamps `done_at`; `queues.sweep_completed` removes entries past the set's window on each scan (wired into `plex.next_queue`), reusing `prune`. Default is OPT-IN/keep-forever (`REMOVE_COMPLETED_AFTER=never`); a set turns it on with `remove_completed_after: 24h` — movie queues opt in, anime channels stay kept; `keep_completed`/`reel` exempt. Node mirror in `server/src/queues.js`)* |
| 2026-08-03 | [Crossing `@charcuterie/ui` 2.x is clean; the scheme switcher goes neutral](2026-08-03-charcuterie-ui-2-crosses-clean-neutral-switcher.md) *(dependency — bump ui `^1.1.0`→`^2.2.0` for the neutral `intent` default; the 2.0.0 `Modal`→`Dialog` overlay rebuild touches nothing we import (own modal; Accordion/Select/Badge/EmptyState/Switcher unchanged); measured clean — typecheck/tests/build green, no source change)* |
| 2026-08-03 | [Follow the OS light/dark scheme via Charcuterie's `ColorSchemeSwitcher`](2026-08-03-follow-the-os-colour-scheme-via-charcuterie-switcher.md) *(frontend behavior — `<ColorSchemeSwitcher>` in the header, default `system`, cycle light→dark→system; `buildFirstPaintScript` injected head-prepend for no-flash; inline sun/moon/monitor SVGs, no new icon dep. **OPEN:** owner must decide whether the kids' always-on wall display should pin dark before this ships — do NOT merge/deploy until then)* |
| 2026-08-03 | [The `app.css` `@layer` fix is a separate, screenshot-gated change — not yet shipped](2026-08-03-app-css-layer-fix-is-a-separate-screenshot-gated-change.md) *(frontend convention — F6 reorders the whole cascade below Tailwind's utilities; deferred because its only real gate is a scheme/density screenshot review, and the batch it would ride in had none)* |
| 2026-08-03 | [SQLite is a derived Plex cache, not the store — YAML stays the source of truth](2026-08-03-sqlite-is-a-derived-plex-cache-not-the-store.md) *(architecture / storage — `/config/cache.sqlite` is deletable, gitignored, schema-versioned and wiped on mismatch; clarifies the 2026-07-21 "no SQLite" decision without reversing it, and forbids a later config migration into it)* |
| 2026-08-03 | [Retire `queue_builder/` to Node — keep a minimal Python cast sidecar](2026-08-03-retiring-python-except-the-cast-sidecar.md) *(architecture / scope — supersedes in part the 2026-07-21 deferral, whose blocker was account-token minting; that is already ported. MQTT stays for HA; the internal round trip goes)* |
| 2026-08-02 | [CI runs on GitHub Actions and the image is built-on-push to GHCR](2026-08-02-ci-and-deploy-on-github-actions-to-ghcr.md) *(infrastructure — ported `.gitea` CI to `.github/workflows/`; added a mux-magic-style `workflow_run` deploy that builds+pushes `ghcr.io/sawtaytoes/plex-channels`; the LAN-registry→GHCR production cutover is DONE — the live app pulls `ghcr.io/...:latest` and serves the Vite build)* |
| 2026-08-02 | [Adopting a `@charcuterie/ui` component means DELETING the app's skin for it](2026-08-02-adopting-a-component-means-deleting-its-skin.md) *(frontend convention — `app.css` is unlayered and outranks every Tailwind utility, so a kept `padding`/`color` makes the component's props a silent no-op; app keeps layout + selectors only)* |
| 2026-08-02 | [An uncontrolled `@charcuterie/ui` control is keyed on its SECOND writer, never on its value](2026-08-02-uncontrolled-components-are-keyed-on-their-second-writer.md) *(frontend convention — `value`/`expandedKeys` are initial-only; keying on the value remounts under the user's own pick)* |
| 2026-08-02 | [Five `@charcuterie/ui` components plex-channels cannot adopt yet, and why each is a library gap](2026-08-02-components-that-cannot-be-adopted-yet.md) *(migration scope — `Field` overwrites the control's `id`; `Menu` has no point anchor and no non-item states; `Modal` has no form; `Badge` has no `asChild` and no two-part shape)* |
| 2026-07-31 | [The web frontend is React 19 + TypeScript + Vite + Tailwind v4 on `@charcuterie/tokens`](2026-07-31-frontend-is-react-typescript-vite-tailwind-on-charcuterie-tokens.md) *(architecture / build — charcuterie M6d phase 1; adds a build step, keeps the hash routing + the whole DOM contract, applies Plex amber as the accent intent)* |
| 2026-07-31 | [The start episode is PICKED in a modal — no inline control on the tile](2026-07-31-start-episode-is-picked-in-a-modal.md) *(UI — supersedes the inline "Start" field; adds the collection series+season+episode picker and the tile context menu)* |
| 2026-07-31 | [A Collection tile shows the MEMBER that plays next — the collection moves to the badge](2026-07-31-collection-tiles-are-member-first.md) *(UI — member poster/name + episode-only line; also drops the "Play" label before the eps dropdown)* |
| 2026-07-31 | [A show entry can pin a manual START episode — a floor for "next", never a watched-write](2026-07-31-per-entry-start-episode-override.md) *(direction — implementation pending; season+episode, single-season → episode-only, collection → series+season+episode)* |
| 2026-07-31 | [Curated members are additive includes on top of the rule pool — a channel can be a mix](2026-07-31-curated-members-are-additive-includes.md) *(behavior — supersedes the "curated vs dynamic, no convergence" point; member search spans all libraries)* |
| 2026-07-29 | [Each binding card renders its OWN ratings, not the active profile's scoped list](2026-07-29-binding-ratings-render-per-profile-not-shared-scope.md) *(bug fix — a non-active profile's binding came up blank in Configure)* |
| 2026-07-29 | [A rewatch channel's pool follows the libraries IT names — and a show library contributes its one-episode films](2026-07-29-rewatch-pool-follows-the-channels-own-libraries.md) *(selection logic — drops the SEC_MOVIES hardwire; anime films reachable; supersedes the "hide the inert pickers" clause)* |
| 2026-07-29 | [The eligible pool lists every short by name — a library bucket is not a show](2026-07-29-shorts-preview-lists-each-short.md) *(UI / preview payload — per-short tiles + per-short Exclude)* |
| 2026-07-29 | [Every dynamic channel is a first-class UI entry, and rotation channels are deletable](2026-07-29-dynamic-channels-first-class-and-deletable.md) *(UI / data model — per-channel rows + Delete button; rewatch hides its inert library pickers; supersedes the no-delete clause)* |
| 2026-07-29 | [Drop `set:"auto"` ("Shield pick") from the UI — every play is explicit](2026-07-29-drop-set-auto-from-ui-every-play-explicit.md) *(UI / interaction — backend auto path unchanged)* |
| 2026-07-27 | [Younger Kids gets Shows and Shorts as two cards; Older Kids keeps them combined](2026-07-27-younger-kids-shows-and-shorts-are-two-cards.md) *(channel + card model — adds the `shows`/`shorts` channels; a rotation channel may now have no show library)* |
| 2026-07-26 | [Spoken status is a sentence, not a diagnostic — and it only speaks when something is actually wrong](2026-07-26-spoken-status-is-a-sentence-not-a-diagnostic.md) *(UX — `state.error` is read aloud verbatim; no timeouts/jargon, and the wait announcement fires at 25s not 6s)* |
| 2026-07-26 | [A card names a Plex profile, and the scan waits for the Shield to really be on it](2026-07-26-cards-name-a-profile-and-the-scan-waits-for-it.md) *(playback gating — the per-tier cards; extends `requires_profile` from per-SET to per-SCAN, plus the ADB auto-switch)* |
| 2026-07-25 | [A set can require a Plex Home profile (`requires_profile`); the scan waits for the on-screen switch](2026-07-25-sets-can-require-a-plex-profile.md) *(playback gating — the demo reel's libraries are invisible to the kid profiles)* |
| 2026-07-23 | [Live migration: `younger`/`older` tiers → `shows_shorts` + `movies` function channels (soak + `channel_for` routing)](2026-07-23-live-tier-migration-to-function-channels.md) *(migration — v3 PR 4)* |
| 2026-07-23 | [`sets.yaml` rotation channels carry a `profiles[]` binding array + a `behavior` field (back-compat reader)](2026-07-23-sets-yaml-profiles-array-schema.md) *(schema — v3 PR 2a)* |
| 2026-07-21 | [Human-readable YAML is no longer a goal; stable IDs (ratingKeys) are canonical entry identity, not title strings](2026-07-21-drop-human-readable-yaml-canonical-ids.md) *(direction; supersedes the hand-editable goal)* |
| 2026-07-21 | [Every interactive element must have obvious hover, focus (keyboard), and motion states](2026-07-21-ui-interaction-states-standard.md) *(UI standard)* |
| 2026-07-21 | [Defer the queue_builder Python → Node port (revisit later, not now)](2026-07-21-defer-python-to-node-port.md) *(scope)* |
| 2026-07-21 | [Channels are function-first with a profile selector; members generalize (show/collection/movie/short) and behavior is progress-vs-rewatch](2026-07-21-channels-function-first-generalized-members.md) *(direction — v3)* |
| 2026-07-21 | [Dynamic (rotation) channels are created + fully configured in Node, not hardcoded in Python](2026-07-21-dynamic-channels-fully-configurable-in-node.md) *(v2)* |
| 2026-07-21 | [Channels split into "Dynamic Channels" vs "Curated Channels"; consistent nav + config surface, fix the "queue" noun](2026-07-21-dynamic-vs-curated-channel-categories.md) *(v2)* |
| 2026-07-21 | [Plex Collections are first-class queue entries, expanded in collection order](2026-07-21-plex-collections-as-ordered-queue-entries.md) *(v2)* |
| 2026-07-21 | [Finished queue entries are kept + marked `done`, not pruned — removed only on "remove all completed"](2026-07-21-finished-queue-entries-marked-done-not-pruned.md) *(v2; supersedes the prune half of the movie-queue wishlist)* |
| 2026-07-21 | [Queues vs channels taxonomy (anime sets are channels, random rotation; Movies is its own channel) + play-first entry IA](2026-07-21-queues-vs-channels-taxonomy-play-first-ia.md) *(shipped; **taxonomy superseded 2026-08-12** by [orthogonal mode knobs](2026-08-12-queues-have-orthogonal-mode-knobs-not-named-types.md) — the play-first IA and the anime rotation+batch behaviour stand)* |
| 2026-07-21 | [Shelf/tile UI conventions: reserved scrollbar space, edge shadows, zoom-proof glyphs, 480px posters](2026-07-21-shelf-ui-conventions.md) *(shipped)* |
| 2026-07-21 | [Sets are data (sets.yaml): immutable ids, renameable labels, per-set libraries, global excludes](2026-07-21-sets-registry-immutable-ids.md) *(shipped)* |
| 2026-07-21 | [The editor is a real live webapp (SSE, no Refresh); monorepo server/+web/; YAML stays, no SQLite](2026-07-21-real-webapp-sse-yaml-not-sqlite.md) *(shipped)* |
| 2026-07-20 | [`queues.yaml` carries no per-set label comments; audience→key map lives in the header](2026-07-20-queues-yaml-no-per-set-label-comments.md) |
| 2026-07-20 | [Queue web UI: Plex-style UX, ratingKey writes, docs count as movies](2026-07-20-queue-web-ui-ux-and-write-format.md) *(shipped)* |
| 2026-07-20 | [The queue web UI is a monorepo in one container, not a separate app](2026-07-20-queue-web-ui-monorepo-single-container.md) *(shipped)* |
| 2026-07-20 | [Queue entries are human-readable title strings, not ratingKeys](2026-07-20-queue-entries-are-title-strings.md) *(shipped)* |
| 2026-07-20 | [The queue config web UI is a Node.js app (like mux-magic), not embedded Flask](2026-07-20-queue-web-ui-is-nodejs-not-flask.md) *(packaging amended → monorepo/one container)* |
| 2026-07-17 | [A queued/rotated series never plays specials — Season 0 is excluded entirely](2026-07-17-anime-series-never-open-on-specials-exclude-season-0.md) |
| 2026-07-16 | [The queue config web app is embedded in plex-channels, and edits the same YAML](2026-07-16-config-web-app-embedded-in-plex-channels.md) *(stack + process model amended 2026-07-20 → Node.js)* |
| 2026-07-16 | [Anime becomes three curated queues; the On Deck set is retired](2026-07-16-anime-queues-retire-ondeck-set.md) |
| 2026-07-16 | [Movie-queue sets are a hand-edited YAML wishlist, pruned on finish, played as Bob](2026-07-16-movie-queue-sets-yaml-wishlist.md) |
| 2026-07-09 | [Bob's "anime" set = On Deck (Continue Watching), not whole-library](2026-07-09-anime-continue-watching-set.md) (superseded; specials rule survives) |
| 2026-07-08 | [Kids' channels draw from Shows + Shorts only — no Anime](2026-07-08-kids-channels-shows-and-shorts-only-no-anime.md) |
