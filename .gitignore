__pycache__/
*.pyc
node_modules/
web/node_modules/
.env
.env.local
*.local
__screenshots__/
web/dist/
# The derived Plex cache (decision 2026-08-03-sqlite-is-a-derived-plex-cache-not-the-store):
# regenerable from Plex, never backed up, safe to delete.
cache.sqlite
cache.sqlite-wal
cache.sqlite-shm

# D3 engine-parity corpus (docs/d3-engine-parity-corpus.md): real library data, never committed.
__corpus__/

# NOTE: the SYNTHETIC engine-parity corpus (e2e/fixtures/engine-corpus/ + engine.sets.yaml +
# engine.queues.yaml) used to be generated per-run by a Python script. It is COMMITTED since
# 2026-08-12 — see e2e/fixtures/README.md. Only a REAL recorded corpus stays ignored (above).
