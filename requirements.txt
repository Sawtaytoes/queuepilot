# The cast_sidecar is the ONLY Python left in this image (decision
# 2026-08-12-python-is-gone-except-the-cast-sidecar): a pychromecast bridge for
# PLAYBACK_MODE=cast, which Node cannot do. Everything else is Node.
paho-mqtt>=2.1,<3
# Cast playback builds the PlayQueue as the account token (correct-account attribution).
PlexAPI>=4.15,<5
PyChromecast>=14,<15
