# Entries for the harness fixture's curated queues. Every entry is a MAPPING — a bare
# string is the pre-2026-08-21 form and `loadEntries()` refuses it by entry.
linus_shows:
- {ratingKey: "701", title: "A Series (2019)"}
manga_webtoons:
- {title: "A Long-Running Manga"}
grace_comics:
- {title: "A Comic Run"}
game_night: []
# Two video-game queues with something in them, so a Video Games pick has a lineup to name
# rather than a queue its provider reports finished. The ids are the PROVIDER's own — an
# appid for Steam, a ROM path for MiSTer — which is safe because a queue draws from exactly
# one provider.
steam_night:
- {ratingKey: "400", title: "A Puzzle Game"}
arcade:
- {ratingKey: "/games/SNES/A Platformer.sfc", title: "A Platformer"}
