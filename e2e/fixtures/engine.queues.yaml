# SYNTHETIC engine-parity queues.yaml — a COMMITTED fixture (was generated by the retired
# Python corpus script). Changing it changes what e2e/fixtures/golden/* expects: see
# e2e/fixtures/README.md before editing.
#
# Every entry is a MAPPING (2026-08-21). The shapes here are the SAME LINES they were as bare
# strings — `entryKey()` returns `title:Movie A (2001)`, `rk:1001` and
# `title:Collection: Queue Picks` for these three exactly as it did for the scalars they
# replace — which is why `golden/engine.json` and `golden/curated.json` still pass unchanged.
# Written FLOW (`- {…}`) rather than block so each entry keeps its explanation on its own line.
bobq:
- {title: "Movie A (2001)"}         # title string → movie 2001 (unwatched by admin → plays)
- {ratingKey: 1001}                 # ratingKey show Alpha → next unwatched ep (S0 specials dropped)
- {collection: "Queue Picks"}       # collection → unwatched children (Movie C is watched → dropped)
- {title: "No Such Title"}          # matches nothing → kept + flagged unresolved
- {ratingKey: 2002, done: true}     # already-done, not in-progress → stays done (flagged "2002")
- {title: "Movie C (2003)"}         # movie admin already finished → newly done
demo:
- {ratingKey: 2001}                 # movie, always plays (reel ignores watched-state)
- {ratingKey: 1004}                 # show Delta → first episode, ignoring history/specials rules
- {collection: "Queue Picks"}       # whole collection in order (both members, no watched filter)
